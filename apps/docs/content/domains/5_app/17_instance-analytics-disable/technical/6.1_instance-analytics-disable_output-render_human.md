# Human Renderer: `orbit instance:analytics disable`

[Back to `instance:analytics disable` technical contract.](1_instance-analytics-disable.md)

**Renderer:** Human renderer.

## Primitive

- Progress: [Progress tree](../../../../ux/commands/progress/progress-tree.md)

## Selection

Use this renderer by default when `--json` is absent.

## Progress Tree

Render an in-progress tree after input resolution and before route cleanup.

```text
┌ Disabling Instance Analytics
○ Resolve instance analytics binding
○ Remove ingress tracking routes
○ Remove router tracking routes
○ Disable analytics binding
└ Working...
```

## Success Render

```text
binding:
  app: docs
  instance: production
  serving_node: app-prod-1
  enabled: false
  internal_host: analytics.orbit
  public_hosts: []
```

## Error Render

Errors are rendered as concise prose messages.

## Test Mapping

| Path | Coverage |
| --- | --- |
| `apps/gateway/tests/Feature/Http/Api/AppAnalyticsControllerTest.php` | Gateway response fields that human output renders and binding-missing payloads. |
| `apps/cli/tests/Feature/Commands/App/AppAnalyticsDisableCommandTest.php` | Human renderer success output and binding-missing prose. |
