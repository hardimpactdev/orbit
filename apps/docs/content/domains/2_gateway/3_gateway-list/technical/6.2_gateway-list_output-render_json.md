# JSON Renderer: `orbit gateway:list`

[Back to `gateway:list` technical contract.](1_gateway-list.md)

This page describes the JSON-rendered output for `gateway:list`.

**Renderer:** JSON renderer.

## Primitive

None. JSON output is emitted as a machine-readable envelope via `$this->line(...)`.

## Selection

Use this renderer when `--json` is present.

## Envelope

This renderer uses the shared [JSON Envelope](../../../README.md#json-envelope).

## Success Shape

```json
{
  "success": {
    "data": {
      "active_gateway": "default",
      "gateways": [
        {
          "name": "default",
          "active": true,
          "url": "https://10.6.0.2",
          "wireguard_ip": "10.6.0.2",
          "ca_fingerprint": "sha256:abc123"
        }
      ]
    },
    "meta": []
  }
}
```

`active_gateway` is `null` when no gateway is active.

## Error Shapes

### No gateways configured

```json
{
  "error": {
    "code": "validation_failed",
    "message": "No gateways configured. Run orbit gateway:add first.",
    "meta": {
      "reason": "missing"
    }
  }
}
```

## Test Mapping

| Path | Coverage |
| --- | --- |
| `apps/cli/tests/Feature/Commands/Gateway/GatewaySelectionCommandTest.php` | JSON `gateway:list` success envelope, active gateway field, gateway list payload, null active gateway, and no-gateways failure envelope. |

There is no gateway-side coverage for this command-local mapping: input handling and renderer behavior live in `apps/cli`. Gateway API behavior is mapped in the command contract file when a gateway-side surface exists.
