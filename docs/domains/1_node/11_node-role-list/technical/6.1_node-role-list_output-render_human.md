# Human Renderer: `orbit node role:list [node]`

[Back to `node role:list` technical contract.](1_node-role-list.md)

## Primitive

None. The human renderer uses a table because this is a fast read command.

## Progress Tree

No progress tree. This command is a local or gateway-forwarded read.

## Success Output

- Render a table with `ROLE`, `STATUS`, `SETTINGS`, `LAST ERROR`, and `CONVERGED AT`.
- If the node has no hosted roles, print that the node has no role assignments.

## Failure Output

- Validation and lookup failures render plain prose errors.

## Test Mapping

| Path | Coverage |
| --- | --- |
| `tests/Feature/Commands/Nodes/NodeRoleListCommandTest.php` | Human table shape and empty/missing-node prose. |
