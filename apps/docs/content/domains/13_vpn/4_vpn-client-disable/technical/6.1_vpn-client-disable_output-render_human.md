# Human Renderer: `orbit vpn-client:disable <name>`

[Back to `vpn-client:disable` technical contract.](1_vpn-client-disable.md)

**Renderer:** Human terminal renderer.

## Primitive

- Progress: [Progress tree](../../../../ux/commands/progress/progress-tree.md)

## Selection

Use this renderer when `--json` is not present.

## Progress Tree

`vpn-client:disable` resolves the active `vpn` role runtime, performs backend
authentication, and writes backend peer state, so the human renderer must
render a progress tree after input resolution and before side effects begin:

```text
┌ Disabling VPN client
○ Resolve VPN runtime
○ Authenticate VPN backend
○ Disable VPN client
└ Working...
```

## Success Output

```text
VPN client `laptop` disabled
```

If the client was already disabled, render:

```text
VPN client `laptop` already disabled
```

## Failure Output

Human-rendered failures use one concise error line and no JSON envelope.

## Test Mapping

| Path | Coverage |
| --- | --- |
| `apps/cli/tests/Feature/Commands/Vpn/VpnCommandsTest.php` | Progress tree, disabled success prose, already-disabled prose, validation failure prose, active-node protection prose, backend failure prose, and absence of JSON envelopes in human mode. |
There is no gateway-side coverage for this command-local mapping: input handling and renderer behavior live in `apps/cli`. Gateway API behavior is mapped in the command contract file when a gateway-side surface exists.
