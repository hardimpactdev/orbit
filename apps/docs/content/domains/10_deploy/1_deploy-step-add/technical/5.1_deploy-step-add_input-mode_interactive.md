# Interactive Input Mode: `orbit deploy:step-add`

[Back to `deploy:step-add` technical contract.](1_deploy-step-add.md)

**Input mode:** Interactive.

Applies when the command is attached to a TTY and `--json` is not present.

## Prompt Behavior

Interactive input mode prompts only for missing required fields after gateway
reachability and provided options are resolved.

| Prompt ID | Primitive | Field | Shown when | Validation |
| --- | --- | --- | --- | --- |
| `deploy.step_add.app` | `search` | `app` | No app argument was supplied. | Visible production app-instance selector the caller may manage. |
| `deploy.step_add.command` | `text` | `command` | No command argument was supplied. | Non-empty shell command string. |
| `deploy.step_add.title` | `text` | `title` | The derived command title is ambiguous or too long. | Non-empty display label. |

`order`, `timeout`, and `retention` are optional flags and are not prompted by
default. Invalid supplied values fail as soon as the field is known.

## Primitive References

- [`search`](../../../../ux/commands/inputs/search-prompt.md)
- [`text`](../../../../ux/commands/inputs/text-prompt.md)

## Abort Behavior

Prompt aborts exit with the shared Orbit command failure status and perform no
gateway writes.

## Test Mapping

| Path | Coverage |
| --- | --- |
| `apps/cli/tests/Feature/Commands/Deploy/DeployInteractiveInputModeTest.php` | Interactive `deploy:step-add` app and command prompts before POSTing to `/api/deploy/steps`. |

There is no gateway-side coverage for this mapped surface; linked CLI tests cover only the mapped assertions above. Remaining behavior stays a coverage gap until focused tests land.

Coverage gaps until focused tests land: documented prompt IDs, production app search validation, title prompt behavior, abort before gateway writes, and invalid optional flag handling during interactive resolution.
