# Human Renderer: `orbit node:show [name]`

[Back to `node:show` technical contract.](1_node-show.md)

This page describes the human-rendered terminal output for `node:show`.

**Renderer:** Human renderer.

## Primitive

- Detail: [Show detail](../../../../ux/commands/details/show-detail.md)

## Selection

Use this renderer when `--json` is not present.

The human renderer does not own input prompting. Prompt behavior belongs to
[`interactive input mode`](5.1_node-show_input-mode_interactive.md).
Non-interactive validation failures are rendered here as prose errors when
`--json` is absent.

## Progress Tree

`node:show` is a registry read. Execution is expected to complete below one
second and does not perform slow external work or live node probes. No progress
tree is rendered.

## Success Output

When the node is found:

```text
┌  Node: app-1
│
├  Role          app
│
├  Roles         app-development, database (error)
│
├  Environment   development
│
├  Platform      ubuntu_24-04
│
├  WireGuard     10.6.0.7
│
├  Consuming     control-1
│
└  Serving       —
```

The legacy `Role:` line always renders the registry role (`gateway`, `app`, or
`control`) for v1 compatibility. When composable role assignments exist, the
human renderer also emits a separate `Roles:` line with the ordered assignment
labels, such as `app-development, database`, and annotates non-active
assignments inline, for example `database (error)`.

When the node has no role assignments, no `Roles:` line is rendered:

```text
┌  Node: gateway-1
│
├  Role          gateway
│
├  Environment   —
│
├  Platform      ubuntu_24-04
│
├  WireGuard     10.6.0.2
│
├  Consuming     —
│
└  Serving       —
```

## Failure Output

Human-rendered failures must show:

- the failing phase or field;
- a clear prose error;
- no JSON envelope;
- next-step guidance only when the operator can act on it.

### Missing node

```text
Node 'app-1' not found or not visible.
```

### Not authorized

```text
This node is not authorized to inspect 'app-1'.
```

### Gateway unavailable

```text
Gateway connection is required to show node details.
```

## Test Mapping

Primary test owners:

| Path | Coverage |
| --- | --- |
| `tests/Feature/Commands/Nodes/NodeShowCommandTest.php` | Human output for registry success, no-progress-tree behavior, node-not-found prose, not-authorized prose, and gateway-unavailable prose. |

Required missing-file coverage:

| Path | Coverage |
| --- | --- |
| `tests/Feature/Commands/Nodes/NodeShowHumanRendererTest.php` | Human renderer selection, no-progress-tree explanation, registry success prose, empty-role omission behavior, failure prose, and exact error messages. |
