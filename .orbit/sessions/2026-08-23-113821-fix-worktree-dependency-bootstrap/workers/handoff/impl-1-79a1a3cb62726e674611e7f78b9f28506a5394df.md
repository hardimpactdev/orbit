# impl-1 handoff

candidate=79a1a3cb62726e674611e7f78b9f28506a5394df

## Result

Prepared worktrees no longer inherit a stale default `main` silently, and Composer bootstrap is fail-closed.

## Changes

- `bin/orbit-prepare-worktree`
- `bin/orbit-prepare-worktree-test`

With default `--base=main`, preparation fails before `git worktree add` when local `main` and an existing `origin/main` are different commits. The error prints both exact SHAs and says local main must be updated. It does not fetch, pull, reset, or mutate main. Reuse and explicit non-main `--base` stay allowed.

After each `composer install` (root, then every one-level `packages/*/composer.json`, then `apps/*/composer.json`), preparation fails unless that project contains `vendor/autoload.php`, and the error names the exact project path. Packages still install before apps. Frontend npm stays behind `--frontend`. TypeScript SDK npm bootstrap is unchanged.

## Proof

- `bash -n bin/orbit-prepare-worktree`
- `bash -n bin/orbit-prepare-worktree-test`
- `bin/orbit-prepare-worktree-test`
- `bin/orbit-gateway-pest --compact tests/Feature/E2ESupport/WorkerToolsTest.php`
- `composer quality-check` (exit 0, artifact `.orbit/quality-gates/quality-check-2026-08-23T085341Z-529eba256a18.json`)
- `bin/orbit-feature-proof-receipt` ok for candidate `79a1a3cb62726e674611e7f78b9f28506a5394df`

The WorkerToolsTest composer stub now creates `vendor/autoload.php` after `composer install`, matching the fail-closed postcondition.

The fixture proves composer install for the root project and every tracked one-level package/app manifest, including `apps/gateway`. It does not grep the implementation for that contract.

## Leftover

None. Combined tip includes the WorkerToolsTest fixture fix.
