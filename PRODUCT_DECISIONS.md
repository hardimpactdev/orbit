# Product Decisions

This is Orbit's **intent ledger**: a chronological, append-only record of
*direction-change* decisions. It sits above the authority-doc chain
(mission → architecture → concepts → tech-stack → domains) as the anchor for
**current intent**. It does not restate contracts — detailed behavior still
lives in the authority docs.

## How to use it

- **Find current intent on a topic:** `grep` this file for the topic noun
  (e.g. `scheduler`, `gateway`, `swarm`, `php`, `s3`). The matching entry with
  the **latest date is the current direction**; older entries on the same topic
  are superseded.
- **Resolving drift:** when two docs disagree, the latest dated decision here is
  current intent. The stale doc is the side that contradicts it, unless a
  *later* decision reaffirms the doc. This pre-fills the fix direction; it does
  not authorize silent edits.

## What gets a line (the bar)

Only a decision that **establishes a new product direction** or
**changes/reverses a previously-documented one**. Not a feature changelog: no
flags, bug fixes, refactors, test-lane tweaks, or gap-filling within an existing
direction.

## Entry format

`- YYYY-MM-DD — <decision, present tense, current direction; include the topic noun>. (solo todo #NNNN)`

- Present tense ("Gateway runs as…"); the date carries when it became intent.
- Put the topic noun in the line so `grep` finds it.
- The `(solo todo #NNNN)` link is optional — include it when a Solo todo drove
  the decision (it is the context trail and timeline anchor); omit it otherwise.
- Newest entry first.

## Decisions

<!-- newest first; add new entries directly under this comment -->
- 2026-08-16 — The App record is logical-only in persistence, not only in contract: the `apps` table drops its six retained placement shadow columns (`node_id`, `environment`, `domain`, `path`, `document_root`, `adopted`) and the `App::node` relation is removed. Every placement fact — serving node, environment, domain, source path, document root, and adoption state — is authoritative on the concrete Instance (`instance.driver_config` plus the durable `instance.adopted` flag), read through `WorkspacePlacement` and `AppSelectorResolver`; no code path reads placement from an app, and an app selector that could match more than one instance stops for explicit resolution rather than falling back to app-owned placement. The only value an instance still copies from its app is the PHP creation template (`apps.php_version`), which instances snapshot at creation and own thereafter, so dropping the placement shadows leaves php-version snapshot semantics unchanged. This completes the 2026-07-19 logical-app direction by removing the physical shadow fields it had retained as instance-creation defaults. (solo todo #748)
- 2026-08-15 — DeploymentRun is the sole durable owner of latest deployment state. Instance payloads and health readers derive the latest status from the greatest instance-owned run ID, terminal writes update only that run, and the redundant Instance snapshot columns are removed only after migration-time equivalence validation. (solo todo #749)
- 2026-08-13 — A hash-verified role-image archive is the runtime authority when `update:all` uses a private candidate image without distributing registry credentials. Orbit loads the archive, deploys its digest-free local tag with registry resolution disabled, and verifies both the expected service image and its replica before reporting convergence. The manifest retains the registry digest as release identity, but runtime inspection and workload verification use the archive-backed local tag. This supersedes the attempted registry-pull fallback after archive loading.
- 2026-08-13 — Node removal commits peer-row, grant, firewall-row, and node deletion only when private DNS projection reconciliation succeeds in the same gateway transaction. DNS activation failure returns `node.dns_reconciliation_failed`, rolls all registry deletion back, and permits the operator to retry the same command; the already-absent durable and live WireGuard peer remains an accepted retry state. This extends the retry-safe teardown contract to the complete gateway-owned removal boundary.
- 2026-08-13 — Node removal detaches a managed peer from wg-easy durable state and the live gateway WireGuard interface before deleting Orbit registry state. Any non-absent teardown failure returns a retryable error and retains the node, peer, grants, and firewall rows. A caller cannot remove its own node while that managed peer exists; another authorized node or the gateway must perform the removal so Orbit can return a reliable result and retain retry authority. Self-removal remains allowed when no managed peer row exists. This supersedes the warning-and-continue peer cleanup contract.
- 2026-08-13 — Operation terminal outcome is shared across Core, gateway persistence, SDK transport, and CLI renderers: a `complete` event with zero or absent `exit_code` succeeds, a `complete` event with nonzero or invalid `exit_code` is a completed failure that preserves its partial result, and every `error` event fails.
- 2026-08-13 — Doctor rehydrates an unrenderable node-owned managed-service process from its stored service intent, including existing encrypted credentials, service options, image, and bind selectors. Rehydration does not rotate credentials; credential generation remains creation-only, and non-managed processes retain their existing restore behavior.
- 2026-08-13 — Ordinary tool capability probes use one ordered JSONL observation contract for single-tool Doctor and batched convergence checks. A verified `installed=false` observation remains restorable `tool.capability_missing`; command failure or empty, malformed, missing, duplicate, unexpectedly named, or incorrectly typed evidence is per-tool blocked inspection `tool.capability_probe_failed`, never inferred absence and never a restore input. A failed batch command marks every requested tool unverifiable, while one invalid named line does not discard valid sibling observations.
- 2026-08-13 — `process.runtime_unit_unloaded` is a report-only runtime incident for an Orbit-owned launchd label that should stay loaded: node-owned and non-`app-dev` units are checked, while unloaded `app-dev` units remain normal on-demand state. Doctor does not start, restore, or adopt the unit; `process:start` remains the explicit lifecycle command. This replaces the stale Process Doctor fix-map promise of automatic launchd lifecycle repair.
- 2026-08-11 — Doctor uses one canonical catalog-backed issue contract across verify, convergence, fleet subprocess reports, and selected-issue intake: probes supply observation identity and detail, while the catalog alone derives disposition, restore action, restorable, and adoptable behavior. Convergence context selects execution but never rewrites classification. Resource-scoped keys may carry a separate same-family catalog code; a catalogued key paired with an unrelated code fails before mutation, with `node.updates` explicitly retaining its documented `node.updates_*` code family.
- 2026-08-09 — `update:all` owns one continuously renewed fleet lease from start admission through terminal cleanup: the gateway atomically reserves `fleet:update-all` before returning 202, rejects a concurrent start before launching another runner, and the detached runner claims that reservation exactly once before deferred migrations, manifest resolution, fleet checks, or mutations. A dedicated heartbeat renews the fleet lease and the operation's active nested leases behind the claimed owner-token fence; runner launch failure releases an unclaimed reservation, while process loss remains recoverable through bounded lease expiry. Supersedes the prior contract that acquired the fleet lease only after the read-only check steps.
- 2026-08-07 — PHP runtime version is snapshot-inherited, not live-inherited: the app-level `php_version` is a creation-time template that new instances copy at creation and new workspaces copy from their owning instance, and every instance and workspace thereafter owns its concrete version outright. Changing an app default never reaches an existing instance or workspace; `php:use --instance` writes that instance alone, `instance:register --php-version` is instance-scoped and no longer re-stamps the app default, and `app:new --php-version` sets the template. A migration backfills every instance and every null-valued workspace with the version it already resolved to, so no running runtime changes version on deploy. The `instance.shared_php_policy_applied` sibling fan-out warning is removed because an instance write no longer affects siblings, while `instance:register`'s `instance.shared_runtime_policy_applied` narrows to runtime proxy transport, which remains app-owned. Consequences recorded deliberately: until an app-default write surface exists, the creation template is frozen after `app:new`, so an instance registered later starts from the app's original version and is corrected with `php:use --instance`; app-owned processes such as queue workers continue to use the app value and therefore no longer track the default instance's PHP; and `workspace.php_inherited` is `false` for every workspace created under this model, retained only for payload compatibility. Every instance-creation site stamps the template at creation — `app:new`, `instance:register`, and `instance:add` alike — so no instance row is ever stored without its own version; an empty row would resolve through the app at runtime and reintroduce exactly the live inheritance this decision removes. The app-wide workspace fan-out precondition on `php:use --instance` is removed with the fan-out itself: an instance write is no longer gated by an unrelated workspace's unresolved placement or by the target node's ability to operate workspaces, so an `app-prod` instance can change its own PHP version. Workspace setup steps keep a pre-existing split that this decision does not change: `ORBIT_PHP_VERSION` carries the workspace effective version while the host toolchain path still follows the app creation template. Supersedes the 2026-07-19 direction that apps own a shared PHP runtime policy every instance consumes, and the 2026-08-06 fan-out reporting decision built on it.
- 2026-08-06 — The managed-service catalog supports PostgreSQL major 17 (`17-alpine`, shared `/var/lib/postgresql/data` layout with 16; 18 keeps `/var/lib/postgresql`), and every non-postgres single-port managed service (for example `valkey`) accepts an optional `service_options.published_port` / `--published-port` override of its catalog default port so additional instances of one service can coexist on a node instead of failing the endpoint-conflict check. `--database` and `--username` stay PostgreSQL-only, multi-port managed services accept no published-port override, and host-command processes continue to reject service options.
- 2026-08-06 — Grant-permission removal is registry-tolerant and invalid grants are doctor-restorable: `node:permissions --remove` accepts permission strings the registry no longer knows (additive and replacement modes still reject unknown names), and `node.access_permission_invalid` is reclassified `genuine_drift` with a deterministic NodesProbe restorer that strips unrecognised strings and re-normalizes the remaining known permissions. A terminal failed development-runtime wake is bounded, not sticky: the failed wake page surfaces the recorded failure reason beside the explicit retry link, and after a configurable backoff (default 60 seconds) an ordinary request begins a fresh wake run without `?orbit-wake-retry=1`. Supersedes the 2026-08-03 listing of `node.access_permission_invalid` as a non-restorable reclassification and the indefinite terminal-failed wake page retained by the 2026-08-04 boot-presentation decision.
- 2026-08-05 — For `app:log`, an explicit bare hostname is always eligible for exact registered proxy-route resolution and never requires an implicit `https://` scheme. When the same text is both a registered proxy domain and a canonical `app.instance` selector, the exact registered proxy hostname wins for `app:log`; `instance:log` remains the explicit instance-selector command. This reverses the earlier collision precedence that rejected bare hostnames solely because they matched a registered instance selector before proxy matching.

- 2026-08-05 — Laravel runtime application logs are first-class for App → Instance → Workspace targets: public CLI commands are `app:log`, `instance:log`, and `workspace:log` reading only the fixed on-node path `<resolved authorized application root>/storage/logs/laravel.log` with public logical path always `storage/logs/laravel.log` (never absolute host paths). Application-log flags are `--lines=100`, `--follow`, `--json`, and `--node` (serving-node placement constraint only); `--json` and `--follow` are mutually exclusive. Selectors use strict URL/hostname shapes or `app.instance` / workspace name with optional CLI `--instance`; no numeric public IDs. Missing bounded log files succeed empty with `file_exists=false`; follow waits for creation, preserves history-before-live ordering, and survives rotation. Authorization for application logs is only `instance:read` / `workspace:read` for the resolved target. Colliding public surfaces rename with no aliases: `process:logs` → `process:log` (permission `process:log`), lifecycle history `workspace:log` → `workspace:run:log` (permission `workspace:run:log`); stored grants rewrite atomically and fail closed with no dual old tokens. Gateway routes are exactly `GET|POST /api/instances/{instance}/log(-stream)` and `GET|POST /api/workspaces/{workspace}/log(-stream)` with request fields `lines`/`node` and required workspace `instance`; lifecycle `GET /api/workspaces/runs/{run}/log` path stays; process HTTP route shape stays. PHP/TS SDKs, OpenAPI, catalogs, docs, and the bundled Orbit skill match. Loki and retention are out of scope.

- 2026-08-05 — Orbit's canonical workload identity hierarchy is App → Instance → Workspace: an App is the global logical web-application registry record and shared runtime-policy owner (no placement defaults); an Instance is one concrete local or deployed runtime of exactly one App; a Workspace remains a development working copy owned by exactly one Instance (`app-dev` only). Public CLI (`app:*`), API (`/api/apps`), permissions (`app:read`/`app:write`), selectors (`app.instance`, `--app` for logical filters), errors, Doctor public metadata, SDKs, OpenAPI, macOS UI, internal models/types (`App`, `Instance`, `Workspace` only—no active `Project` workload model or `AppInstance`), persistence (`apps`/`app_id` retained; `app_instances`→`instances`, `app_instance_id`→`instance_id`), product docs (domain `5_app`), generated catalogs, and the bundled Orbit skill use App vocabulary for the logical entity with no aliases, redirects, dual-read, compatibility payloads, or retained Project workload schema. Node roles `app-dev`/`app-prod`, Laravel `App\`/`app()`/`APP_*`, monorepo `apps/`, Codex App/`codex:app` and its project argument/routes/payloads, Solo `solo:project:*` and Solo project APIs, git/repository/generic project-management wording, historical migrations, dated ledger entries, and archived plans remain unchanged. Supersedes the 2026-07-20 Project → Instance → Workspace hierarchy decision and every active Project workload contract that depended on it.

- 2026-07-29 — Node-owned Docker managed-service processes accept a repeatable
  public `--bind=<wireguard|loopback>` selector on `process:add` and
  `process:update` instead of WireGuard-only published ports: omission on add
  still defaults to WireGuard-only, explicit selectors replace that default,
  loopback publishes only `127.0.0.1`, both selectors publish every service
  target port on both hosts with the same published port, host-command /
  instance / workspace / docker-swarm binds are rejected, and existing rows
  without bind intent continue to infer WireGuard-only.

- 2026-08-04 — Each process definition persists a non-null human display `label` beside the stable internal identity slug still stored as `Process.name`. Migration backfills existing rows `label=name`. New processes default `label` to the identity key/name when `--label` / body `label` is omitted; `process:update --label` (and API body `label`) changes only the display label. Renaming identity via existing `--name` / body `name` never rewrites a persisted label, whether that label was defaulted or custom. Labels are trimmed non-empty strings with max length 255. Public process list and SSE snapshot items expose `key` (= current name slug) and `label`, and retain `name` as a clearly deprecated compatibility alias equal to `key`. SSE lifecycle `update` frames expose `key` and `name` aliases for the durable process identity; they include current `label` when the matching process row is reliably available, otherwise fall back to the durable key and treat the snapshot as authoritative for display labels. CLI/API inputs stay backward compatible (positional lifecycle name remains); optional label inputs are added on process:add and process:update across CLI, gateway, OpenAPI, and PHP/TypeScript SDKs. New SDK consumers must use `key` and `label`. TypeScript package `@hardimpactdev/orbit-sdk-typescript` is prepared to `0.3.0` in-tree (`private=true`, not published here). Supersedes process list/SSE payloads that exposed only `name` as the public identity field without a durable display label.

- 2026-08-04 — Development runtime wake process starts are concurrent on the internal activation path only: soft runners initiate every configured lifecycle process concurrently; cold runners still restore and verify dependencies first, then use the same concurrent process-start phase. After concurrent starts resolve, activation performs a bounded aggregate readiness check of the full expected process set (preferring an existing batched/runtime probe) and marks the scope awake only when every expected runtime unit is observed running—start-command exit alone is insufficient. Any start or readiness failure keeps the scope asleep, retains current wake failure behavior, and stops/rolls back the group as appropriate. Activation serialization/fencing and the already-awake fast path remain. The mounted activation page's non-overlapping same-origin background probe cadence is one second (not five). Public `process:start` keeps documented process-order semantics; this optimization does not introduce process-dependency configuration, graphs, levels, or related abstractions. Supersedes serial internal wake process starts and the five-second wake-page probe cadence from the earlier 2026-08-04 boot-presentation decision while retaining soft/cold backend split, indeterminate mark presentation, and activation-state header contract.

- 2026-08-04 — Browser process status uses gateway-origin SSE at `GET /api/processes/stream?app=<hostname>` (app-only selector; never `url`), not client polling and not a Laravel Toolbar PHP/filesystem watcher. Auth and Origin/CORS match existing browser process list/lifecycle (WireGuard peer identity + `process:read` grants; `X-Orbit-Client` optional and never required; native EventSource cannot set custom headers). Gateway `process_events` are authoritative: every connect emits a fresh full canonical snapshot at a transactionally consistent durable high-water mark as the snapshot SSE id (0 when no events), then ordered lifecycle updates after that mark only; `Last-Event-ID` is accepted for EventSource reconnect and never causes post-snapshot historical regression. Cross-worker behavior is DB cursor tailing scoped by app_instance + workspace|null + node (not a frozen process-id list and not process-local pub/sub), so processes added/converged after connect still stream. Durable event types include transitional `starting`/`stopping`/`restarting` recorded before runtime work, terminal `started`/`stopped`/`crashed`, and `failed` (status `unknown`) when a backend returns false or throws—never leave latest status stuck transitional and never fabricate crashed/running/stopped on action failure. Status values are `starting`|`running`|`stopping`|`stopped`|`restarting`|`crashed`|`unknown`. Heartbeat SSE comments are independent of the faster DB poll cadence. The TypeScript package `@hardimpactdev/orbit-sdk-typescript` gains a durable native EventSource subscriber (version prepared to `0.2.0` in-tree, private, not released here); `createOrbitGatewayClient` remains for commands. Supersedes browser process polling and stale internal `process-event:stream` live-probe framing for toolbar consumers.

- 2026-08-04 — Development runtime wake boot presentation uses continuous page mount with one-second non-overlapping same-origin background fetch probes of the original path/query (credentials same-origin, cache no-store, redirect manual so application redirects—including cross-origin login/OAuth—never leave fetch stuck without an Orbit pending header), not document meta-refresh. Soft (recent-idle) and cold (seven-day) still share one indeterminate animated Orbit mark page with no soft/cold UI distinction, copy, diagnostics, or progress UI. Responses set an explicit safe `X-Orbit-Runtime-Activation-State` header (`pending` while waking, `failed` when terminal) so the client does not treat application HTTP status alone—including application 503—as Orbit pending. Network errors are nonterminal and retry after one second; pending keeps the page mounted; failed stops polling and presents the existing retry experience (one terminal navigation to the failed page is acceptable); any response without the Orbit activation-state header means Caddy has handed off to the application and the browser navigates once to the original URI regardless of status. CSP stays restrictive and authorizes only the minimal same-origin fetch script. No new public stream/operation endpoint or grants/auth change; Caddy serving-node WireGuard pre-check and ordinary browser GET-only scope remain. Supersedes the 2026-08-03 document meta-refresh / auto-refresh clause for this wake page while retaining soft/cold backend split and mark presentation. The five-second probe cadence is superseded by the same-day concurrent-wake decision.

- 2026-08-04 — OpenCode, PolyScope, and Agent IDE / ADE are removed product surface: no public commands, routes, permissions, adapters, Doctor issues, config fields, SDK types, tool catalog membership, process templates, or active docs present them as available Orbit behavior. Generic workspaces remain first-class and always create through WorktreeWorkspaceDriver only (path-based Git worktrees with no editor ownership). Codex remains a direct non-ADE integration under its current public contract with no rename. `opencode-cli` and `polyscope-server` are unsupported for install/update/reconfigure/credentials/catalog; only `tool:remove` remains as removal-only legacy teardown of Orbit-managed residue. Release A clears legacy ADE config, workspace adapter metadata, agent_ide crash policies, removed permissions, and OpenCode/PolyScope tool/process intent while retaining physical rollback-shadow columns; Release B column drops wait until the rollback floor is proven. Supersedes active ADE/OpenCode/PolyScope intent from prior adapter-default, tool-catalog, related-process, and ADE message-delivery decisions.

- 2026-08-04 — First npm publication of `@hardimpactdev/orbit-sdk-typescript` uses a fail-closed, one-time **bootstrap** path on the package repository only: `workflow_dispatch` with exact version input `0.1.0` matching `package.json`; registry absence is confirmed only by npm `E404` (via `scripts/npm-bootstrap-registry-absent.sh`), successful `npm view` refuses as already existing, and any non-E404 registry/DNS/TLS/rate/auth error fails closed; may use a short-lived least-privilege `NPM_BOOTSTRAP_TOKEN` secret scoped to `hardimpactdev/orbit-sdk-typescript` for that single create. After the package exists, configure npm Trusted Publisher (`npm trust github @hardimpactdev/orbit-sdk-typescript --repo hardimpactdev/orbit-sdk-typescript --file publish.yml --allow-publish` or equivalent UI), delete/revoke the bootstrap secret/token, and all subsequent `release: published` tags use the Craft-style OIDC path with **no** token. Monorepo `orbit-release.yml` never holds or uses an npm publish secret. Supersedes treating every nonzero `npm view` as absence and the assumption that Trusted Publisher can authorize the first creation of a package that does not yet exist on the registry.
- 2026-08-04 — `@hardimpactdev/orbit-sdk-typescript` is independently versioned from monorepo root `VERSION` and is not published by `.github/workflows/orbit-release.yml`. Canonical source remains `packages/sdk-typescript` in `hardimpactdev/orbit` (`private=true` in-tree as accidental-publish protection). Durable consumers install from npm and the generated package repository `hardimpactdev/orbit-sdk-typescript`. Initial public version is `0.1.0` from `packages/sdk-typescript/package.json`. Release path: prepare split tree with `bin/orbit-prepare-release-package --package=sdk-typescript` (version from package.json, never root VERSION; prepared tree sets `private=false`), push main to the split repo, publish a GitHub Release tag `v0.1.0` in that repository, and let the split repo’s Craft-style OIDC workflow (`.github/workflows/publish.yml` on `release: published`, Node 24, `id-token: write`, `npm publish --provenance --access public`) publish npm. No monorepo `NPM_TOKEN` / bearer publish path. Supersedes coupling the TypeScript SDK version and npm publication to root Orbit release tags and `orbit-release.yml`.
- 2026-08-04 — OpenClaw remains unsupported product surface (no install/update/reconfigure/credentials/catalog support), but `tool:remove openclaw` is a durable removal-only migration: it always runs LegacyOpenClawRuntimeCleanup on the target agent/tool-host node even when no NodeTool row remains. Cleanup stops Orbit and native OpenClaw units, uses privileged `sudo ss` to kill listeners on historical port `18789` (including agent-owned PIDs invisible to unprivileged ss), kills residual agent-owned openclaw processes, deletes `/home/agent/.openclaw`, clears process intent named `openclaw-gateway`/`tool=openclaw`, verifies port/process/home absence before success, and only then removes tool-owned proxy backend/TLS plus registry rows. A failed verification returns `tool.remote_action_failed` without deleting those rows after the script step. Hermes is unchanged. This supersedes the prior assumption that process-unit removal plus `rm -rf ~/.openclaw` alone guarantees runtime teardown.

- 2026-08-03 — Hermes is the only first-party autonomous agent tool supported by the `agent` role. OpenClaw is removed from the tool catalog, `node:new --agent-tool` supported values, agent proxy/endpoint contracts, install/update/reconfigure/credentials paths, and product docs; requests that name OpenClaw fail as an unsupported tool/agent-tool. Hermes remains installable, removable, updateable, reconfigurable, credential-bearing, process-backed, and proxied, and its install/update path tracks the official NousResearch/hermes-agent latest channel (`install.sh` / `hermes update`) rather than a hard-coded version pin. Tool-owned proxy routes are part of tool lifecycle: successful `tool:remove` removes matching tool-owned proxy backend/TLS through `ProxyRouteFixer::removeExtra` then deletes the registry row (same ordering as `proxy:remove --force`), and proxy rows whose tool owner is missing are classified as `proxy.owner_invalid` so `proxy:remove --force` can still clean residual orphans. Supersedes the 2026-08-03 OpenClaw local-prefix installer decision, the 2026-08-02 OpenClaw/Hermes co-hosting and port decisions, and the multi-tool OpenClaw+Hermes first-party agent runtime direction.

- 2026-08-03 — Every pending soft (recent-idle) and cold (seven-day) development runtime activation presents one common indeterminate boot screen: the approved animated Orbit mark only (dark exact compound track, clipped white conic trail, polar-ellipse-compensated transform-only CSS spin, no soft/cold UI distinction, no aggregate progress bar, no step rows, no diagnostics, and no prefers-reduced-motion branch). Soft and cold remain distinct internal plans and runners; the page auto-refreshes the original URI until the pre-check succeeds and keeps the existing failed-operation retry link without redesigning recovery. Supersedes the 2026-07-28 and 2026-08-03 progress-bar presentation clauses for this wake page while retaining their soft/cold backend split.

- 2026-08-03 — DoctorRestoreSupport is the aggregated support decision from family/handler-owned restore maps actually consumed by apply/dispatch (NodesProbe, SchedulesFixer, ToolsFixer, AppsFixer, ProxyRouteFixer, DnsRuntimeProbe, specialized proxy probes, runner-owned process/DNS-projection descriptors, DatabaseConnectionRestorer) — not a second hand-maintained allowlist; catalog genuine_drift and restorable flags require that real support; codes without a guaranteed restorer are reclassified rather than overclaimed (including app/instance production_user_* and tool.seaweedfs.credentials_missing as runtime_incident).

- 2026-08-03 — Doctor genuine_drift requires DoctorRestoreSupport dispatch: catalog disposition and restore_action are bound to a shared restorer registry that apply routing consumes; codes without a deterministic restorer are reclassified (blocked_inspection / invalid_intent / runtime_incident), including workspace auto-fix (report-only), schedule.heartbeat_stale, node.access_permission_invalid, and node.wireguard_peer_extra; schedule runtime-hibernator restore is routed without schedule_key. Retained Incus current-checkout install runs gateway role before dependent roles so operator CA fetch is not raced against gateway overlay setup.

- 2026-08-03 — Doctor node-scoped restore is convergence-complete: given valid gateway intent and a reachable selected node, `--restore` runs bounded multi-pass genuine-drift repair under the exact `--key`/`--family`/`--instance`/`--workspace` fence until no further supported genuine drift remains, then proves absence with a fresh authoritative probe; no-progress/repeated findings and max-pass caps terminate with structured unresolved outcomes (no infinite retry). Every emitted issue code has an explicit family-owned catalog disposition (`genuine_drift`, `blocked_inspection`, `invalid_intent`, `runtime_incident`) and family ownership; genuine drift always declares a restore action; unknown codes fail closed and never invent disposition from name heuristics. Generic `kind` remains for compatibility, but human/JSON/stream payloads also expose stable `disposition`, `restore_action`, and restore `convergence`/`summary` stop metadata so agents need not parse prose. `--all` stays verify-only; `--adopt` remains explicit disaster-recovery and is not widened; invalid gateway intent is never auto-repaired by guessing; restore never uses SSH or arbitrary-shell repair paths. Supersedes architecture prose that mixed non-restorable conditions into undifferentiated “drift” as if all findings were restorable.

- 2026-08-03 — Owner-only Orbit CLI config keeps the narrow managed-agent consumer exception durable across atomic saves: when the `agent` runtime user exists (or the file already carries the exact allowed ACL), `OrbitConfigStore::save` re-applies `u:agent:--x` on the config directory after owner-only `chmod 0700` (so directory mask is not left effective-none and agent can still `test -e` the file) and `u:agent:r--` on `config.json` after rename; both restores fail closed. Permission validation accepts the file’s exact extended ACL shape when traditional group bits only reflect the ACL mask, and still tightens ordinary group/other exposure without broadly allowing mode `0640` or other named-user grants.

- 2026-08-03 — Drift-resolution contract package: (1) dotted `project.instance` selectors resolve exact instance names before hostname/domain/node/path/TLD aliases so siblings that share those facts cannot make selectors such as `mealou.nmbp` / `mealou.development` ambiguous; bare logical-project shorthand remains ambiguous when multiple instances exist. (2) Doctor single-node and fleet payloads expose additive `roles` (all active canonical roles in stable order) while retaining legacy primary `role` and `scope.role=fleet` for fleet runs; human fleet panels surface multi-role sets without breaking the bordered panel. (3) `tool:list` / `tool:show` derive non-secret autonomous-agent consumer endpoints from catalog + node TLD + proxy contract (`https://openclaw.agent`, `https://hermes.agent`; internal upstream ports OpenClaw 18789 / Hermes 8080) instead of persisting redundant endpoint copies; JSON uses the canonical `{name, kind, url, host, port}` shape and human output prefers `url`. (4) Tool-family Doctor adds a read-only gateway trust-path HTTPS health check of each installed autonomous-agent tool’s exact consumer URL (`tool.agent_consumer_url_unreachable`); proxy family still owns route rows/artifacts/TLS and this check does not invent unsafe route restore (details point at `doctor --family=proxy`). (5) Gateway-to-CLI `internal:tool:run-script` actions share one `Orbit\Core\Tools\ToolRunScriptAction` contract (no CLI-only ALLOWED_ACTIONS duplicate); dispatchers validate and inventory tests prove every dispatched action is accepted while unknown actions fail closed. (6) Retained `operator_gateway_agent` readiness prepares the managed agent runtime user and required source CLI/home accessibility before product proof so tool install/probe does not need ad hoc role repair.

- 2026-08-03 — OpenClaw host install/update uses the official local-prefix installer (`https://openclaw.ai/install-cli.sh` with HTTPS-only curl flags, `--no-onboard`, and `--prefix "$HOME/.openclaw"`) as the unprivileged `agent` user so Node and the OpenClaw CLI live under the agent home without system Node or agent self-sudo; Orbit process/configure/version paths call the absolute local-prefix binary `/home/agent/.openclaw/bin/openclaw` (not ambient PATH or a world shim). Public capability observation declares `binary_as_user=agent` so single/batch probes run owner-scoped `sudo -u agent -H test -x` against that absolute path and still report the real PREFIX_BIN path and owner-scoped version — deliberately no Hermes-style world probe shim and no pointing `binary` at `sudo`. Port `18789`, process ownership, and file-backed gateway token semantics are unchanged. Supersedes install.sh / `npm install -g openclaw` as Orbit’s managed install/update path.

- 2026-08-03 — `tool:logs` is a shipped tool lifecycle verb for tools that declare the `logs` capability (and callers authorized for `tool:logs` / implying `tool:read`); product docs under `domains/3_tool` and the CLI/gateway command surface are current. The 2026-07-02 claim that `tool:logs` remains removed is stale for tools that declare logs (orbstack still does not expose logs). `tool:reload` remains capability-gated the same way. Supersedes the “tool:logs remain removed” clause of the 2026-07-02 lifecycle restoration entry and the 2026-06-06 full tool-lifecycle removal for logs only.

- 2026-08-03 — Soft (recent-idle) and cold development runtime wake share one serialized activation operation and the same minimal auto-refreshing progress page; already-awake scopes still return success immediately with no new operation. Soft runners only fence process activation; only cold runners restore/verify dependencies and clear that scope's cold marker. This refines the 2026-07-28 on-demand and cold-tier wake directions so soft wake no longer blocks the pre-check on inline process start.

- 2026-08-03 — Node Doctor `node.security.home_perms` accepts owner `0700`-equivalent managed home posture plus the narrowly managed Linux Agent consumer traversal ACL (`u:agent:--x` with POSIX mask `--x`) applied by agent role baseline repair; it still rejects broader named users, agent read/write, named groups, default ACLs, and group/world access. Restore remains mode-`0700` for weak base drift only and must not report/repair a correctly ACL-hardened Agent home (avoids the ACL vs chmod repair loop with `node.role_baseline_mismatch`).

- 2026-08-03 — Custom `proxy:add` and `proxy:remove --force` are one-step lifecycle commands: add persists then enacts backend/TLS through the canonical ProxyRouteFixer path (failed apply keeps partial intent and returns `proxy.enactment_failed` with `next_command`); force-remove cleans backend and `/etc/orbit/certs/<domain>.{crt,key}` through ProxyRouteFixer::removeExtra before registry deletion (failed cleanup returns `proxy.cleanup_failed` without deleting the row). Doctor remains the repair path for partial failures and orphan extras; it does not silently delete custom routes. Supersedes the deferred-to-doctor custom proxy enactment/cleanup direction.

- 2026-08-03 — Machine-mode Doctor streams (`--json` and `--stream-json`) request `compact_progress=true` so intermediate gateway progress frames omit the full aggregate doctor report; terminal complete/error frames remain full. Human Doctor mode continues to receive full intermediate snapshots for the bordered panel.

- 2026-08-03 — Node Doctor may restore `node.security.home_perms` by setting only the managed runtime user's home directory (`/home/{nodes.user}`) to mode `0700` through the authenticated node execution path after ownership and passwd-home validation; runtime-user absence remains report-only and re-bake is no longer required for chmod-safe home mode drift.

- 2026-08-02 — OpenClaw's managed web runtime uses OpenClaw's documented default gateway port `18789` (not Orbit Caddy private backend `8081`); when OpenClaw and Hermes co-host on one `agent` node, Hermes keeps `8080` and OpenClaw stays on `18789` with tool-owned proxy routes `hermes.<tld>` → `host.docker.internal:8080` and `openclaw.<tld>` → `host.docker.internal:18789`. OpenClaw lifecycle remains Orbit process-owned (`openclaw-gateway` via `openclaw gateway run` with `OPENCLAW_SUPERVISOR_MODE=external`); the gateway token is file/env-only (`OPENCLAW_GATEWAY_TOKEN`), not stored in process argv or full-config rewrites. Supersedes the same-day `8081` OpenClaw port choice.

- 2026-08-02 — When OpenClaw and Hermes co-host on one `agent` node, Hermes keeps default web port `8080` and OpenClaw's managed web runtime uses port `8081` with tool-owned proxy routes `hermes.<tld>` and `openclaw.<tld>`. OpenClaw lifecycle is Orbit process-owned (`openclaw-gateway` via `openclaw gateway run` with `OPENCLAW_SUPERVISOR_MODE=external`); the gateway token is file/env-only (`OPENCLAW_GATEWAY_TOKEN`), not stored in process argv or full-config rewrites.


- 2026-07-29 — Orbit-owned php-cli production artifacts are fleet-scoped (9 cells), not a full OS/arch cross-product: for each pinned patch 8.3.31/8.4.21/8.5.8 Orbit publishes coverage on linux-x86_64 and macos-aarch64 plus standard on linux-x86_64 only, matching the real fleet (Ubuntu app-dev, macOS ARM app-dev, Ubuntu app-prod). Production matrix no longer includes linux-aarch64, macos-x86_64, or standard macOS artifacts; role variant authority (`app-dev` coverage, `app-prod` standard) and compatibility install behavior until matrix promotion remain unchanged.
- 2026-07-29 — Orbit-owned php-cli matrix object keys under a fixed versioned prefix are truly immutable: before `aws s3 cp`, publication runs `s3api head-object`; an existing key is re-verified via the unauthenticated consumer URL SHA-256 and skipped when identical, or fails closed (new versioned prefix/pin required) when the SHA differs — never overwrite a different artifact at an existing fixed version/variant/platform key.
- 2026-07-29 — While the runtime php-cli catalog remains on `install_contract=compatibility`, doctor classifies against the effective standard compatibility runtime (not desired coverage alone), still records desired variant + pending matrix cutover in evidence, and only enforces `tool.php_cli_coverage_missing` after matrix promotion when the effective coverage runtime lacks working PCOV; `tool:update`/`updateAll` and the role backfill migration apply role-owned variant authority (overwrite stale coverage on app-prod and stale standard on app-dev); matrix publication verifies the exact unauthenticated consumer URL + SHA-256 after each upload (not only authenticated S3 metadata), prefers runner AWS CLI v2 over pip user installs, and handoff/catalog validation require tool/filename/unique cells, known catalog_role/install_contract values, and every nested fleet-scoped matrix slot key.
- 2026-07-29 — The host `php-cli` tool remains a single slug with a persisted `NodeTool.config.variant` of `coverage` or `standard`: `app-dev` baselines select coverage (statically linked PCOV with `pcov.enabled=1` for Pest TIA), `app-prod` baselines select standard (no PCOV), Orbit owns fleet-scoped matrix artifacts for PHP 8.3/8.4/8.5 (coverage on linux-x86_64 and macos-aarch64; standard on linux-x86_64) as immutable checksummed artifacts once the matrix is published, and install/update stage and verify every minor before replacing installed binaries. Until full matrix publication the runtime catalog uses `install_contract=compatibility` (Orbit-owned checksummed 8.5 plus intentional unchecksummed historical bulk static-php.dev for 8.3/8.4). Matrix mode fails closed on missing checksums, while compatibility temporarily preserves the legacy bulk path until full publication. Doctor under compatibility reports desired/effective/pending cutover evidence without false `tool.php_cli_coverage_missing` drift on a healthy standard compatibility runtime, and only enforces coverage/PCOV missing after matrix cutover when the effective coverage runtime lacks working PCOV.
- 2026-07-29 — Orbit-owned PHP 8.5 host artifacts and the PHP 8.5 FrankenPHP image pin a checksum-verified official SQLite release containing the WAL-reset fix, verify both the SQLite3 extension and SQL query version before publication or installation, and use a new immutable runtime-image family whenever that embedded SQLite contract changes so cached nodes cannot retain an unsafe image.
- 2026-07-28 — Development runtime hibernation has a seven-day cold tier for app instances and workspaces: after the latest HTTP, process-lifecycle, and source-tree activity is old enough, Orbit may remove safely reconstructable Composer and JavaScript dependency directories while retaining lockfiles and caches; the next request starts one serialized restore-and-wake operation and the stock Caddy pre-check immediately returns a minimal auto-refreshing page with the Orbit mark and one aggregate progress bar that smoothly advances through the work required for that scope. A failed or uncertain prune remains cold, each runner is claimed once, dependency restoration is single-flight per node and source path, process activation remains scope-fenced, and each scope clears its own cold marker only after its dependencies and configured processes are ready. Production and node-owned runtimes remain outside this policy.
- 2026-07-28 — Development app-instance and workspace process runtimes are on demand: their app-dev runtime units remain stopped across host boot, the unmodified official `caddy:2-alpine` route wakes the owning process group through an exact serving-node-authorized gateway pre-check on the first ordinary HTTP request, and the gateway scheduler hibernates that group after one hour without HTTP traffic; node-owned and production process runtimes remain persistent. Restart policy continues to govern an active unit and is separate from this development-scope hibernation lifecycle.
- 2026-07-20 — Orbit's canonical workload identity hierarchy is Project → Instance → Workspace: a project is the global logical repository and shared runtime-policy record, an instance is one concrete deployed or local app runtime implemented internally as `AppInstance`, and a workspace belongs to exactly one instance. Public commands, API paths and payloads, permissions, selectors, and diagnostics use project/instance vocabulary without app-named compatibility aliases; infrastructure names such as `app-dev`, `app-prod`, `APP_URL`, runtime containers, upstream application terminology, and private app-named database compatibility identifiers remain unchanged. This supersedes the logical-app public vocabulary while preserving its ownership semantics and a recoverable gateway update path.
- 2026-07-20 — Logical app removal is selected by logical app slug and deletes the app plus every owned instance as one confirmed cascade; interactive mode names the affected instances and asks before effects, while `--force` supplies destructive consent and is required in non-interactive mode, including `--json`. Instance hostnames and dotted selectors remain targets for `app:instance remove`, not logical app removal.
- 2026-07-20 — Ubuntu is Orbit's only supported Linux host platform in v1; the `metrics` role no longer supports Debian hosts or Debian gateway/router co-location. Adopted or self-managed macOS support remains governed by its platform contract. This supersedes the 2026-06-17 metrics-on-Debian direction.
- 2026-07-19 — Logical apps own shared project identity and runtime policy but no server, path, document root, URL, domain, or environment defaults; every placement fact belongs to one concrete app instance, `app:new` creates the logical app and first instance together, workspace ownership is exclusively app-instance-owned, and ambiguous historical placement or workspace ownership stops for explicit resolution. This supersedes the 2026-07-19 temporary direction that retained logical-app placement fields as instance-creation defaults.
- 2026-07-19 — User authorization for schedules is evaluated only when a caller lists, inspects, adds, removes, reads logs for, or manually starts a schedule; recurring execution runs under gateway implicit authority, resolves the schedule's persisted app instance and current serving node, and does not re-check a historical caller grant when the instance moves.
- 2026-07-19 — Existing fleet analytics remains enabled while its role assignment is migrated to persist the exact PostgreSQL process already consumed by Plausible; the migration neither disables nor recreates analytics, fails closed when the existing process cannot be identified unambiguously, and future analytics creation requires PostgreSQL node, PostgreSQL process, and ClickHouse node before effects.
- 2026-07-19 — `postgres` remains Orbit's version-agnostic managed-service identifier while every PostgreSQL process retains an explicit supported version family and concrete image version, owns typed initialization settings plus its WireGuard-bound published port, and rejects endpoint conflicts before effects; analytics persists the selected PostgreSQL process identity and fails closed when legacy state has multiple candidates.
- 2026-07-19 — Private DNS is projected through three non-overlapping artifacts: the node family owns concrete records for every active node plus `app-dev`/`agent` wildcard records in `dnsmasq.d/10-node-records.conf`, the proxy family owns router/private `.orbit` and exact backend records in `dnsmasq.d/20-proxy-records.conf`, and the `dns` tool owns only the base `dnsmasq.conf` plus DNS runtime, listener, forwarding, and client-DNS capability. The node TLD `orbit` is reserved for the proxy-owned `.orbit` namespace. Node identity create/update/remove/activation reconciles both record fragments, while role add/remove that changes only wildcard eligibility reconciles the node fragment. The shared materializer and reload path is ownership-neutral, the `vpn` role requires the DNS tool capability, and Orbit creates neither a `dns` role nor a DNS state family.
- 2026-07-19 — App doctor, worker policy, setup-step policy, and setup runs operate on one concrete app instance and its serving node; dotted selectors address that instance, bare logical-app shorthand succeeds only when exactly one instance exists, and ambiguous historical worker or setup ownership stops for explicit resolution. Logical app placement fields remain instance-creation defaults only.
- 2026-07-19 — Workspaces are an `app-dev`-only development surface: `app-prod` nodes never create, register, own, set up, remove, inspect, diagnose, or execute workspaces or workspace lifecycle policy, including through indirect app inventory, process, PHP, database, proxy, doctor, or Agent IDE aliases; broad production reads omit workspace facts, explicit workspace targets fail before effects, and role activation plus fleet upgrades fail closed while production workspace ownership remains. `app-prod` receives no workspace permissions as either a consuming or serving grant endpoint. This supersedes the 2026-07-08 `app-prod` workspace self-grant direction.
- 2026-07-19 — Schedules belong to one concrete app instance and execute on that instance's serving node; dotted selectors address that instance, bare logical-app shorthand succeeds only when exactly one eligible instance is visible for the requested permission, and ambiguous historical ownership stops for explicit resolution.
- 2026-07-17 — Orbit has exactly one fleet analytics role assignment: deploying it converges the verified Plausible runtime and the router-owned `analytics.orbit` route plus TLS before the assignment succeeds, proxy doctor restores route drift, and role removal removes the route and its artifacts.
- 2026-07-17 — Valkey is Orbit's only supported managed Redis-protocol service: the `redis` managed-service catalog entry and command support are removed, while upstream compatibility names such as Reverb's `REDIS_*` environment variables and PHP's `redis` extension remain where required. The app-facing `websocket` role requires managed Valkey and persists its provider as `valkey_node_id`; Reverb listens on the isolated runtime container interface and Docker publishes port `8080` only on the node's WireGuard address. The gateway operations Reverb service remains separate and brokerless in v1.
- 2026-07-16 — Environment intent has concrete runtime ownership: `app:env` authorizes and applies only against the selected app instance's serving node and path; `workspace:env` stores and applies a workspace-owned non-secret overlay only to that workspace path; workspace setup initializes a missing `.env` from the workspace's own `.env.example` without copying the parent app `.env`; existing workspace `.env` files are preserved; unsafe stored setup or teardown steps that consume the parent `.env` are removed during migration and rejected or skipped at execution boundaries.
- 2026-07-16 — The hosted `s3` role is live through `node:new`: the initiating client provisions the Ubuntu host over client-owned SSH, then role convergence installs and verifies Docker, writes the encrypted gateway-owned SeaweedFS configuration as a mode-`0600` managed file, and applies and starts the WireGuard-bound SeaweedFS process. S3 data paths must be canonical paths under Orbit-approved persistent-data roots. The analytics baseline follows the official Plausible CE 3.2.1 deployment pairing of PostgreSQL 16 Alpine and ClickHouse 24.12 Alpine; all three services are node-owned Docker processes bound directly to their nodes' WireGuard addresses, PostgreSQL and ClickHouse use generated encrypted credentials, and the role does not become active until Plausible applies and starts successfully. Removing either role removes its live runtime before Orbit deletes the process and role records.
- 2026-07-16 — Orbit command UX uses “data list” to mean the interactive `Laravel\Prompts\datatable` component with explicit columns and row selection; custom grouped read-only `Label: value` output is a property list and must not be substituted. `app:list` renders Name, Repository, Instances, and Workspaces columns and opens the selected app's `app:show` placement drill-down.
- 2026-07-16 — App inventory separates logical identity from placement detail: `app:list` renders one compact logical-app item with repository plus visible instance and workspace counts, while `app:show` renders the caller-visible concrete instances and their nested workspaces with placement URLs; dependency posture remains a clearly labeled logical-app aggregate rather than instance or workspace state.
- 2026-07-16 — Apps are global logical gateway records and `app:list` has no node scope: it lists each visible logical app once, non-gateway visibility is established through `app:read` on at least one concrete Orbit app instance's serving node, and instance plus workspace details remain placement-scoped. The `app:list --node` filter and default/caller-node resolution are removed. Supersedes the 2026-07-08 `app:list` serving-node-scoping decision.
- 2026-07-15 — Before workload bootstrap reservation, the initiating client observes and validates the target Ubuntu version and CPU architecture over its local SSH edge; the gateway persists that observed platform, chooses the matching CLI and Agent artifacts, commits node/peer/bootstrap reservation and node/bootstrap completion as atomic state transitions, and applies home, SSH, sysctl, unattended-upgrades, and public-SSH-deny policy through Agent push. Target SSH host keys remain client-owned bootstrap evidence and are not gateway steady-state doctor state.
- 2026-07-15 — A repeated workload `node:new` asks the gateway to resume its initiating-client-bound request before opening SSH: Agent-ready pending and completed bootstraps continue directly through the completion endpoint, while only an unreserved or not-yet-Agent-ready bootstrap returns to client-local SSH. WireGuard address allocation and reservation are gateway-serialized and database-unique, and `node.created` activity is emitted once only after terminal completion commits.
- 2026-07-15 — Workload `node:new` bootstrap SSH is initiated and owned by the configured client CLI: the gateway authenticates that client over existing WireGuard, reserves the new node identity and WireGuard peer, and returns a node-specific bootstrap bundle for the client to stream to the target; after the bundle starts WireGuard, the Orbit CLI, and the WireGuard-bound Agent, the gateway completes provisioning through Agent push and never opens target SSH or offers a public/manual no-SSH enrollment fallback. First-gateway bootstrap remains a separate pre-gateway flow.
- 2026-07-15 — App- and workspace-scoped process definitions, their derived runtime units, and their process events belong to one concrete app instance; dotted app selectors address that instance, bare logical-app shorthand succeeds only when exactly one instance exists, ambiguous existing rows stop for explicit migration, and process reads, writes, lifecycle, logs, doctor, and authorization resolve the instance's serving node.
- 2026-07-13 — `node:manage` persists the operator-selected user, platform, and managed Agent intent before convergence; Agent reachability failures report repairable drift without rolling back that intent so doctor can converge it later.
- 2026-07-12 — Deployment policy, ordered steps, warmup paths, runs, history, logs, and latest deployment status belong to one concrete production app instance; bare app selectors are shorthand only when exactly one instance exists, ambiguous historical ownership stops for explicit resolution, and logical-app deployment columns and read fallbacks are removed. Supersedes the temporary app-level deployment direction recorded on 2026-06-17.
- 2026-07-12 — Orbit maintains no supported legacy tier: ownership changes use explicit forward fleet migrations, ambiguous state stops for operator resolution, and superseded rows, columns, fallbacks, aliases, launchers, code, tests, and active compatibility prose are removed after verified convergence.
- 2026-07-12 — Workspace records, workspace lifecycle steps, runtime mounts, and database attachments persist concrete app-instance ownership; database targets are app instances or workspaces, and logical-app compatibility rows are migrated or removed rather than retained as read fallback.
- 2026-07-12 — Every new active node identity requires an explicitly supplied unique node TLD, including separate gateway and initiating-operator TLDs during first-gateway bootstrap; `nodes.tld` is the sole persisted TLD and role assignments only derive role-specific artifacts from it.
- 2026-07-12 — Orbit Agent installation intent derives from either an active workload role or the node's explicit `managed` opt-in; Agent eligibility is derived from that intent plus supported platform, non-gateway identity, and a WireGuard-bound command listener, while gateway work always executes gateway-locally.
- 2026-07-12 — Orbit-managed SSH is permanent only for provisioning and bootstrap; all workload execution uses gateway-local handling or authenticated Agent-push `binary + argv` envelopes, remaining non-provisioning SSH consumers carry the exact `transitional-ssh` marker until removed, and break-glass SSH stays outside Orbit commands.
- 2026-07-12 — Long-running gateway operations persist progress before publishing it through the operations WebSocket/Reverb plane and replay journal gaps by cursor; direct SSE is transitional and is removed as each operation-backed command migrates.
- 2026-07-12 — Remote actions against another node or gateway-owned state use WireGuard identity, gateway-owned authorization policy, and gateway audit: stored grants are the default gate, with named gateway-implicit-authority, pre-grants-bootstrap, local-only, and identity-gated-self-management classes and the applicable grant, gateway-role, or peer revocation lever.
- 2026-07-12 — `orbit profile` is a read-only local-only direct HTTP probe: it uses an explicit absolute URL, otherwise the nearest project `.env` `APP_URL`, otherwise a local interactive URL prompt, and it never resolves gateway state, checks Orbit grants, calls a gateway endpoint, dispatches remote work, or records gateway activity.
- 2026-07-12 — Tool eligibility is declared through explicit platform, container-provider, runtime-user, isolation, and route/TLD constraints rather than role gates; VitePlus is optional observational runtime inventory, and a tool may declare start, stop, restart, reload, or logs only for one unambiguous tool-owned runtime or exactly one owning process row.
- 2026-07-12 — Solo commands resolve an explicit target node first, otherwise the configured default node, otherwise the caller node; gateway targets use gateway loopback, non-gateway targets use Agent push to target-local loopback, and Solo ports and SSH transport are never exposed.
- 2026-07-11 — `app:new` resolves an explicit source plan before any side effect: operators either create a private GitHub repository from a named template or clone an existing repository, and empty-directory adoption remains the responsibility of `app:register`.
- 2026-07-09 — `workspace:setup` resolves a missing workspace name locally from structured Codex Git-worktree metadata for `~/.codex/worktrees/<key>/<repo>` paths: a synced branch reference becomes its valid slug and thread-backed worktrees fall back to `codex-<key>`; Codex remains outside the Agent IDE adapter model.
- 2026-07-09 — `workspace:setup` may adopt adapter-managed external worktrees from explicit `--app=<app.instance>` plus `--path=<absolute-worktree>` input without a positional workspace name; when no `[name]` is supplied, Orbit asks the app's effective Agent IDE adapter for workspace identity before falling back to path basename.
- 2026-07-09 — Orbit CLI and Orbit Agent self-updates are owner-user local by default: each node has one configured Orbit owner user whose `$HOME/.local/bin/orbit`, `$HOME/.local/bin/orbit-agent`, `$HOME/.config/orbit/config.json`, and `$HOME/.config/orbit/install.json` are the update targets; the `agent` Unix user is a consumer reached through a read-only owner-config shim, and stale protected `/usr/local/bin/orbit` launchers may be reported or explicitly adopted but are not mutated by normal `update` or `update:all` flows.
- 2026-07-09 — Workspace setup and teardown step policy belongs to app instances, not logical apps: `workspace-setup-step:*` and `workspace-teardown-step:*` add/remove writes require dotted selectors such as `hauser.nmbp`, list and `workspace:setup`/`workspace:remove` resolve instance-scoped rows first, and legacy app-level `workspace_steps` rows remain read-only compatibility fallback for list and lifecycle execution only. (solo scratchpad workspace-setup-step--264)
- 2026-07-09 — Configurable FrankenPHP runtime mounts are writable only on app instances: `app:mount add` and `app:mount remove` require an instance selector such as `hauser.nmbp`, while legacy app-level mount rows remain read-only compatibility data and renderer fallback until migrated.
- 2026-07-08 — Configurable FrankenPHP runtime mounts belong to app instances, not logical apps: `app:mount` accepts instance selectors such as `hauser.nmbp`, app and workspace runtime containers use selected-instance mounts, and legacy app-level mounts remain compatibility fallback only until migrated.
- 2026-07-08 — Gateway operations streaming uses a gateway-owned operations Reverb service rendered on the gateway role with the existing `orbit-reverb` runtime image; it is separate from app-facing `websocket` role traffic, remains single-instance without Redis, database-role, or scale-out dependency in v1, and only future streaming payloads move to this WebSocket surface while non-stream commands stay gateway API plus agent-push. (solo todo #190)
- 2026-07-08 — `app-dev` role self-grants include scoped `app:register`, `process:read`, `process:add`, `process:update`, and `process:remove` so a local CLI on an app-dev node can register apps on that node and manage app-owned process definitions for apps hosted by that node; `app-prod` role self-grants remain app-registry-read plus `workspace:setup` only, and neither app role receives app writes, credentials, deploy, runtime lifecycle process start/stop/restart, or cross-node mutation authority. (solo todo #163)
- 2026-07-08 — `app-dev` and `app-prod` role self-grants include scoped `app:read` so local CLIs on app-role nodes can read their own owning-node app registry records through `app:list` and `app:show`; app registry reads remain serving-node scoped and do not grant cross-node visibility, app writes, credentials, setup, deploy, process, proxy, or workspace reads. (solo todo #162)
- 2026-07-08 — Internal-executor operation tokens are dedicated-key, argument-bound, not-before/expiry-checked, and single-use; gateway self-execution may authenticate the verify endpoint by a valid gateway-targeted token when service-network NAT hides WireGuard or loopback peer identity, fleet executor self-updates bridge old Agent verifier payloads by staging and executing the candidate CLI over explicit transitional SSH fallback instead of accepting unbound verify payloads, and non-gateway nodes plus all other gateway API routes continue to require WireGuard peer identity. (solo todo #181)
- 2026-07-08 — Gateway-relayed agent-push streamed process output uses HTTP-chunked Orbit process-stream v1 binary framing on the node-to-gateway hop; WebSocket/Reverb broadcast remains the deferred end-state. (solo todo #179)
- 2026-07-07 — macOS host-command process support uses a new `launchd` process runtime rendered as Orbit-owned user LaunchAgents under the configured node user's `~/Library/LaunchAgents`; `launchctl` is only the node-local command adapter. App-, workspace-, and node-scoped host-command processes default to `launchd` on macOS and `systemd` on Linux. Docker remains the macOS managed-service runtime through the node's reachable Docker-compatible provider, `docker-swarm` remains Linux-only, Supervisor remains removed, and system LaunchDaemons plus general third-party background-process inventory are deferred.
- 2026-07-06 — Orbit Agent managed transport is push-only: the gateway reaches agent-capable nodes through authenticated agent-push `binary + argv` envelopes over Orbit/WireGuard, the Agent runs no default polling or claim loop, `/api/orbit-agent/jobs/claim` and job event endpoints are retired from the normal gateway API, and `node role:add` must not create queued Orbit Agent pull jobs. Future compatibility polling would require a new explicit product decision and opt-in configuration.
- 2026-07-06 — macOS app-dev latency does not introduce a host-native PHP runtime: even though native host PHP-FPM or native host FrankenPHP could avoid Docker/OrbStack bind-mount metadata overhead for local app requests, Orbit keeps Docker-backed FrankenPHP as the single PHP app/workspace runtime so file changes remain immediate and runtime support stays simple; the small macOS bind-mount latency penalty is accepted for now.
- 2026-07-04 — Agent-push V1 uses gateway-authorized structured `binary + argv` requests to the node Agent listener; the gateway builds the argv and owns caller authorization, while the Agent executes only node-local allowlisted binaries, starting with `orbit`, through no-shell process APIs.
- 2026-07-04 — Orbit managed execution converges on two normal paths: `gateway-only` for gateway-owned reads/writes and `agent-push` for node-local execution through typed allowlisted command envelopes with scoped operation tokens. SSH/RemoteShell is transitional migration and recovery infrastructure only; long-term break-glass SSH is operator-owned super-admin recovery outside normal Orbit command execution.
- 2026-07-04 — Orbit Agent transport is gateway-pushed HTTP over the Orbit/WireGuard network for reachable agent-capable workload nodes: the gateway opens an authenticated request to the node's Agent listener carrying a typed command envelope and scoped operation token; polling is fallback or deferred compatibility, not the primary target architecture. SSH/RemoteShell remains bootstrap, recovery, and v1 fallback.
- 2026-07-03 — Orbit Agent is split into two Rust surfaces: `apps/agent` is the headless Axum service binary that owns the node-local polling/job execution loop and builds on Linux and macOS, while `apps/macos` is the macOS-only Tauri tray UI that can launch or quit independently and must not own the long-running background service loop.
- 2026-07-02 — Orbit Agent is the future node-local execution lane for gateway-owned typed jobs on supported nodes, starting with macOS `app-dev` and self-managed workload nodes; the gateway remains the source of truth for intent, authorization, release manifests, operation history, activity logs, and update plans, while SSH/RemoteShell remains bootstrap, recovery, fallback, and the current path where the Orbit Agent lane does not exist.
- 2026-07-02 — Tool-owned lifecycle commands are restored only for lifecycle-capable tools: `tool:start`, `tool:stop`, and `tool:restart` dispatch a tool definition's explicit lifecycle capability, starting with macOS-only `orbstack`, while unsupported tools and unsupported operating systems fail through Orbit-handled errors without host side effects. `tool:logs` and `tool:reload` remain removed, and this direction supersedes the 2026-06-06 lifecycle removal only for start/stop/restart without resurrecting the 2026-06-04 related-process compatibility-adapter semantics.
- 2026-07-02 — `orbstack` is a macOS managed tool/runtime-provider capability; Orbit installs, updates, probes, safely adopts, and dispatches explicit tool-owned start/stop/restart lifecycle for OrbStack through `tool:*` and `doctor --family=tool`, while OrbStack still creates no Orbit process row and exposes no `tool:logs` or `tool:reload` surface.
- 2026-07-02 — The `app-dev` and `database` workload roles support macOS in addition to Ubuntu on adopted/self-managed workload nodes: macOS role support requires a reachable Docker-compatible container provider, Orbit uses an already-working Docker provider first, and remediation recommends Colima (`brew install docker colima`, `colima start --runtime docker`) when no provider is reachable. OrbStack and Docker Desktop remain compatible providers when already installed and licensed/allowed for the user's context, but OrbStack is not the default recommendation because it requires a license for commercial, business, nonprofit, government, and freelance use after evaluation. Hosted `node:new` provisioning templates stay Ubuntu-only, other roles keep their existing platform support, and macOS firewall mutation and WireGuard self-route mutation remain unsupported/no-op.
- 2026-07-02 — App dependency security posture is tracked from lockfile-aware audit command summaries on the gateway, not from full package inventory or SBOM storage. (solo process #2216)
- 2026-07-01 — OpenCode's canonical managed tool identity is `opencode-cli`: `opencode-server` remains the node-owned process/runtime unit name with command `opencode serve -a`, and its process dependency is `tool=opencode-cli`. Existing `node_tools.name=opencode-server` rows and OpenCode process rows using `tool=opencode` are backfilled to the canonical tool slug.
- 2026-07-01 — Agent coding CLIs `codex-cli`, `grok-cli`, `antigravity-cli`, and `cursor-cli` are user-scoped runtime tools managed through `tool:*` and `doctor --family=tool`; Orbit installs, updates, probes, and safely adopts the CLI binaries where source-backed channels are available, while provider auth/session/account state remains outside Orbit ownership. `codex-app` remains the separate macOS app bridge, and outdated `gemini-cli` catalog support is not added.
- 2026-07-01 — Git is a managed runtime tool on `app-dev`, `app-prod`, and `agent` role nodes: Orbit installs, updates, probes, and safely adopts Ubuntu host Git through apt using Git's official upstream stable PPA (`ppa:git-core/ppa`), and Linux Git management does not use Homebrew or Linuxbrew.
- 2026-06-28 — Solo extension commands use Orbit-style flat colon names such as `solo:project:list`, `solo:process:list`, `solo:scratchpad:list`, and `solo:todo:list`; grouped noun/subcommand signatures remain out of scope for the Solo catalog.
- 2026-06-28 — Optional Orbit command and gateway API families ship first as built-in extensions with separate local node and gateway enablement; external downloadable extensions remain out of scope.
- 2026-06-28 — Codex App project registration belongs to the optional `codex` extension command family as `codex:app`; `app:codex` is removed from the public command surface and app permissions no longer imply Codex App registration.
- 2026-06-25 — Fleet doctor --all node progress now reflects each node's own intra-node family check progress (family-weighted), reversing the prior out-of-scope deferral; per-family rows remain hidden in fleet output.
- 2026-06-25 — `process:edit` is removed; `process:update` is the sole public process mutation command, and supported process identity rename flows use `process:update --name=<new-slug>` while preserving managed database connection coherence. Supersedes the same-day backward-compatible alias decision.
- 2026-06-25 — SeaweedFS runtime lifecycle is process-owned: the `s3` role's SeaweedFS container start/stop/restart/logs and runtime drift belong to the `process` family (`doctor --family=process`), matching the metrics Prometheus/Grafana model, while the `seaweedfs` tool row, service credentials, and capability inventory remain `tool`-owned (`doctor --family=tool`). Supersedes the "generic SeaweedFS lifecycle remains under `tool:*`" wording.
- 2026-06-25 — Process mutation is named `process:update`; `process:edit` remains a backward-compatible alias, and supported process identity rename flows use `process:update --name=<new-slug>` while preserving managed database connection coherence.
- 2026-06-24 — `orbit doctor` human output uses one bordered doctor panel for single-node and `--all` fleet runs: in-progress title `D O C T O R`, terminal verify title `D O C T O R - R E S U L T`, no in-progress `S U M M A R Y`, fleet rows keyed by node name (not `Check <node>` or a separate `F L E E T  D O C T O R  R E S U L T` block), flat per-node issue bullets with a human-only 10-bullet cap and `+ <n> more issues` overflow, while `--json` and `--stream-json` payloads stay complete.
- 2026-06-24 — App setup is a lifecycle-specific pipeline: `app-setup-step:*` records ordered host-toolchain setup commands for an app and `app:setup` runs that pipeline idempotently; Orbit still has no generic `app:exec` or `workspace:exec` surface.
- 2026-06-24 — Managed MySQL database user provisioning belongs to the database family through `database:add-user <connection> --service=<process>`, while MySQL service installation, runtime, start, stop, restart, and logs remain process-owned.
- 2026-06-24 — Node-owned managed service process creation uses `process:add --service=<identifier>` with public `--version=<version>` and optional Docker `--image=<image>` override; Orbit resolves official catalog images for known Docker services from service, runtime, and version, starts processes by default, and retains legacy service-definition storage keys only as read compatibility fallback. Supersedes the prior `--definition` service selector wording.
- 2026-06-23 — `app-dev` PHP app and workspace FrankenPHP runtimes terminate inner HTTPS on port `8443` with the same Orbit-issued site certificate the public `orbit-caddy` route already uses; outer `orbit-caddy` reverse-proxies those backends over HTTPS with gateway CA trust and `tls_server_name`, while `app-prod` PHP runtimes stay plain HTTP on `8080` and static runtimes are unchanged.
- 2026-06-23 — `orbit doctor` defaults to exactly one target node: the configured local default node first, then the caller identity when no default is selected; fleet doctor verification is explicit `--all` only, and `--node=all` is invalid.
- 2026-06-23 — Workspace FrankenPHP runtime containers mount the workspace source both at `/app` and at the workspace's original absolute node path, matching app runtimes, so absolute source-local configuration paths such as SQLite database files continue to resolve without storing Orbit-specific state in the checkout.
- 2026-06-22 — App and workspace FrankenPHP runtimes use Orbit-owned `ghcr.io/hardimpactdev/orbit-frankenphp:1-php<version>-bookworm` images built from upstream FrankenPHP with Orbit's baseline Laravel app extensions; upstream `dunglas/frankenphp` remains the image base, not the managed runtime catalog target.
- 2026-06-22 — App and workspace FrankenPHP runtimes keep Caddy/FrankenPHP XDG state ephemeral inside the container at `/tmp/orbit-frankenphp`; only `orbit-caddy` owns durable Caddy storage under `/var/lib/orbit/caddy`, and app/workspace checkouts, `~/.config/orbit`, and `/var/lib/orbit` must not store per-runtime XDG state.
- 2026-06-22 — Managed production nodes that expose a system-wide Orbit launcher such as `/usr/local/bin/orbit` must point it at a shared root-owned executable outside private runtime homes. The node user's install-root copy remains the update record, but agent tools and other unprivileged runtimes must be able to execute the system launcher without gaining `orbit` user access.
- 2026-06-22 — Codex App project registration uses the operator tool category for catalog UX: roleless active nodes opt into gateway SSH through `node:manage`, `codex-app` targets OS-supported non-gateway nodes over WireGuard SSH, and Codex App is not an Agent IDE adapter.
- 2026-06-22 — Tool command targeting is node-wide except the gateway: explicit tool targets may be any active visible non-gateway node, tool definitions declare supported operating systems, and roles may include baseline tools without being the general eligibility gate for user-directed tool commands.
- 2026-06-22 — Gateway release manifest source selection is managed by `orbit manifest:update <url>` and `orbit manifest:remove`: gateways store one custom manifest URL override for release-candidate live testing, `update:all` consumes that gateway-selected source during `Checking for updates`, and clearing the override returns the gateway to the configured default GitHub release manifest source. Supersedes environment-only candidate-channel selection.
- 2026-06-22 — Release-candidate live testing uses stable named artifact channels: immutable candidate assets remain under `candidates/<BUILD_ID>/`, an explicit activation step copies the candidate manifest to `channels/<CHANNEL>/orbit-release-manifest.json`, and live-test gateways consume that stable channel instead of changing source per candidate.
- 2026-06-22 — `update:all` compares desired artifact identity instead of only semantic versions: gateway-owned node DTOs track installed CLI hashes on workload nodes and installed gateway-image digests on the gateway node, candidate manifests update only when the desired artifact differs or tracked state is missing, and workload CLI binaries are staged once on the gateway for nodes to download from the gateway instead of each node downloading the CLI artifact from GitHub or the candidate source.
- 2026-06-21 — Orbit releases promote tested assets instead of rebuilding them at GitHub publication time: release candidates are built once, exposed through a topology-reachable `topology-candidate` manifest, proven with live `update:all` acceptance, then the exact tested CLI assets, digest-pinned gateway image, and final `github-release` manifest are attached to the monorepo GitHub release.
- 2026-06-21 — Every active Orbit node carries a mandatory unique node TLD assigned during setup; gateway VPN DNS publishes concrete `orbit.<node-tld>` host records to each node's WireGuard address while preserving router-owned `.orbit` private service routes and existing wildcard development TLD mappings for `app-dev` and `agent` roles. (solo todo #158)
- 2026-06-20 — `update:all` returns the start POST's 202 envelope before launching the durable update runner, so the CLI can connect to the event stream before the runner is allowed to restart the gateway. Launch failures after the response are recorded in the operation journal as terminal errors instead of changing the already-sent start response. Fleet version checks and verification use local-only `orbit --version --local --json` or `orbit --version --local` probes so internal update liveness never depends on public release metadata.
- 2026-06-20 — `update:all` defers release-manifest resolution from the start POST to the durable runner's `Checking for updates` step when the request omits an inline manifest, returns the event stream URL promptly for CLI liveness, persists the immutable plan before fleet probing or any update side effect, and forbids fetching a substitute manifest after that point; deferred runners bootstrap from a configured digest-pinned gateway image or the currently running digest-pinned `orbit_orbit-gateway` service image, and fleet version checks use bounded-concurrency local-only `orbit --version --local --json` probes. Supersedes the prior rule that plan persistence happened only at start time.
- 2026-06-18 — `update:all` restructures its update ordering and implements previously-deferred refinements: (a) gateway-first ordering is reversed so the gateway updates first as the version ceiling, then the caller-local CLI updates as a fan-out target alongside the remote workload nodes; the local update now runs after the gateway phase, so a local update failure is a partial failure (fleet updated, operator-local install failed — operator re-runs `orbit update`); (b) the previously-deferred all-current short-circuit is now implemented — when the fleet-version probe finds 0 outdated nodes, the gateway/workload/verification phases are skipped entirely and the footer settles to `Skipped: <version> is already installed on all nodes`; (c) the previously-deferred per-node sub-stages are now implemented — each node advances through `Downloading <v>` → `Replacing cli binary` → `Running doctor` → `Done` (gateway: `Downloading <v> assets` → `Updating gateway app` → `Replacing cli binary` → `Running doctor` → `Done`). Supersedes the earlier 2026-06-18 `update:all` v1 deferred-items entry.
- 2026-06-18 — `update:all` v1 implements the version-aware refinements as additive, conservative changes on the durable runner: two check steps (`Checking for updates`, `Checking fleet versions`), a read-only per-node `orbit --version` fleet probe, per-node skip of already-current nodes, and a non-fatal per-node post-update `orbit doctor` verify. Two parts are deferred to keep the durable path conservative: (1) a whole-run short-circuit when nothing is outdated (today the gateway re-converges idempotently and each current node skips individually); (2) finer in-row per-node sub-stages (`Downloading` → `Replacing cli binary` → `Running doctor`) — the gateway emits one `running`→`done` journal step per node, so each node row animates a single stage. Both are tracked future refinements.
- 2026-06-18 — `orbit update` and `orbit update:all` adopt version-aware, gateway-first update flows. `update` always runs a `Checking for updates` step first; the local CLI is never updated past its gateway's version — it reads the gateway version as a read-only gate and skips with "please update your gateway first" when the gateway is behind a newer release. Applying an update renders distinct `Downloading binary` → `Replacing binary` → `Running doctor` steps and runs `orbit doctor` verify on the local node as a non-fatal final step (reporting the issue count). This supersedes the prior `update` scope boundaries that forbade querying the gateway version and forbade running doctor after an update. `update:all` adds explicit `Checking for updates` and `Checking fleet versions` steps, probes each node's current orbit version to skip already-current nodes, updates the gateway first as the fleet version ceiling, then updates the local and remaining workload nodes in parallel (≤4), each running a per-node doctor verify.
- 2026-06-18 — Laravel Cloud production-driver flows reuse an existing default, `main`, or sole Cloud environment by default; creating another Cloud environment requires explicit intent. (solo todo #735)
- 2026-06-17 — FrankenPHP remains Orbit's single first-class PHP app/workspace web runtime; host PHP-FPM is not reintroduced, app instances record required PHP extensions, and app doctor reports runtime extension drift for Orbit-driven instances. (solo todo #733)
- 2026-06-17 — Apps are logical gateway records with one or more concrete app instances; each instance owns its driver, driver configuration, runtime requirements, env values, and database targets, while legacy app-level deployment commands remain a compatibility surface until deployments move to instances. (solo todo #734)
- 2026-06-17 — Metrics enablement converges and starts its owned Prometheus, Grafana, and node-exporter runtime units immediately; doctor remains the repair path for later drift.
- 2026-06-17 — Doctor includes the `process` family in the baseline category set for every node with at least one active role assignment; role-less client/operator identities remain node-family only.
- 2026-06-17 — Metrics baseline records node-exporter host binary tool intent alongside node-exporter process intent on the metrics node and active workload nodes; tool doctor repairs the binary, while process doctor owns the systemd lifecycle.
- 2026-06-17 — Metrics may run on Ubuntu or Debian hosts, including the Debian gateway, and its baseline records node-exporter process intent for the metrics host plus every active workload node selected by the fleet update target selector; Prometheus and Grafana remain Docker Swarm processes on the selected metrics role node.
- 2026-06-17 — Orbit analytics is a private workload role backed by process-owned Plausible CE: PostgreSQL and ClickHouse run as service processes on active database role nodes, `analytics.orbit` is the private dashboard/admin endpoint, and per-app public `analytics.<app-domain>` hosts proxy tracking paths only. (solo todo #731)
- 2026-06-17 — Production Orbit CLI installs use a user-local host launcher by default: `bin/install-orbit` and `orbit update` link the native CLI binary at `$HOME/.local/bin/orbit` unless `ORBIT_BIN_PATH` or `--bin` chooses another path, and `orbit update` must never invoke `sudo`.
- 2026-06-17 — Metrics is an optional host-resource observability role backed by node-owned Prometheus, Grafana, and node-exporter process intent; `metrics.orbit` is the private Grafana route and credentials are exposed through `metrics:credentials`.
- 2026-06-17 — Orbit releases are monorepo-versioned: the root `VERSION` file is the single release number, a `v<VERSION>` tag on `hardimpactdev/orbit` publishes the core, CLI, and gateway split package repos plus CLI binaries, gateway image, and release manifest, and `update:all` consumes that immutable manifest for fleet updates.
- 2026-06-16 — `orbit-caddy` persists Caddy-generated storage through Orbit-owned host paths under `/var/lib/orbit/caddy`, not host Caddy service directories under `/var/lib/caddy`. Restore/adopt must not replay stale host Caddy data into the managed container. (solo todo #725)
- 2026-06-16 — App-level runtime mounts are the current package-symlink support direction for PHP apps on `app-dev` nodes: `app:mount` stores explicit safe bind-mount intent on the app, app and workspace FrankenPHP containers inherit it, mounts default to read-only, and PHP-FPM runtime selection remains deferred. (solo todo #729)
- 2026-06-15 — App-development `orbit-caddy` binds HTTP/HTTPS to a configured node caller-facing private IPv4 in addition to the WireGuard address, while the private backend port remains WireGuard-only. This lets locally resolved development TLDs use trusted LAN addresses without exposing backend routing. (solo todo #724)
- 2026-06-15 — `profile` CLI requests are always measured from the caller machine: the gateway may resolve and authorize the target, but it must not perform or fall back to gateway-origin HTTP profiling for `orbit profile`. (solo todo #722)
- 2026-06-15 — `database` role nodes are private hosted workload nodes when created through `node:new`: a database-only node provisions an Ubuntu host over SSH, carries a node-level TLD, installs the Docker tool baseline, and may use a per-node WireGuard endpoint override for private-provider gateway reachability.
- 2026-06-13 — Orbit removes Supervisor as a supported tool and process runtime. Host-command process units use `runtime=systemd` on Linux, existing `runtime=supervisor` rows migrate to `runtime=systemd`, and future macOS support should add a `launchd` runtime instead of retaining Supervisor for cross-platform uniformity.
- 2026-06-13 — The `s3` role uses SeaweedFS, not RustFS, as its S3-compatible object-storage backend. The v1 implementation remains a single `s3` role node with one SeaweedFS head container, service-level credentials on the `seaweedfs` tool row, router-owned `s3.orbit` routing, and no RustFS data migration.
- 2026-06-11 — Fleet update activity is gateway-recorded: the `update:all` start API route logs the attempt and the durable update runner records the `completed`/`failed` outcome entry; the CLI emits no activity entries. Supersedes the CLI-emission activity wording in the update:all contract.
- 2026-06-11 — `node:list` carries no `--doctor` enrichment flag; doctor reporting belongs to the doctor command surface. The node-list contract documents the implemented surface only, and a future node-list doctor enrichment needs its own decision before returning to the docs.
- 2026-06-10 — Firewall rule eligibility drops the per-role allowlist: any registered active Ubuntu managed node with at least one active role assignment is an eligible firewall target, including `websocket` and `s3` nodes. Clients, unsupported platforms, inactive nodes, and role-less identities remain ineligible.

- 2026-06-06 — `tool:install` of a tool that backs a singleton service (starting with `opencode-server`) also configures that tool's related process by default, so installing the capability yields a running service. A tool definition declares the related process (runtime, command, `--tool` dependency); install converges it idempotently through the process family. `--no-process` installs the capability only; `--with-process` is the explicit default. Lifecycle stays process-owned. (solo todo #704)
- 2026-06-06 — Orbit removes the `tool:start`, `tool:stop`, `tool:restart`, `tool:logs`, and `tool:reload` commands. Tool commands cover capability lifecycle only (install, update, remove, plus show, credentials, reconfigure); start/stop/restart/logs belong exclusively to the process family. A process may reference a tool via `--tool=`, but the process row owns runtime lifecycle. Supersedes the tool-lifecycle compatibility-adapter direction from solo todos #699 and #700. (solo todo #703)
- 2026-06-06 — VPN/DNS Swarm migration keeps `wg-easy` and `orbit-dns` as separate Swarm-managed services, co-located with the router/vpn/dns gateway edge roles. They share a private Swarm network, DNS stays unpublished publicly, and VPN-side DNS traffic is forwarded from the WireGuard namespace to the DNS service instead of merging both runtimes into one container. (solo todo #702)
- 2026-06-05 — Incus E2E topology preparation defaults to the synced source checkout plus VM-local ext4 mirrors from `orbit-base-ubuntu-26.04-runtime`; native CLI binary and packaged gateway runtime artifacts are opt-in through `--use-build-artifacts`.
- 2026-06-05 — Incus E2E topology preparation uses `orbit-base-ubuntu-26.04-runtime` by default. Base-image preparation builds that runtime image from the non-cloud Ubuntu 26.04 VM image through direct Incus-agent bootstrap, and runtime VM readiness starts from the Incus agent, SSH, and role runtime checks.
- 2026-06-04 — Production app FrankenPHP runtimes are currently rendered as per-app Docker runtime containers on the owning app node, exposed only on internal port `8080` to the app-role backend `orbit-caddy`. The fully baked app-runtime Docker Swarm service phase remains deferred; current app runtime intent is the Docker container renderer and process-backed lifecycle model. (solo todo #662)
- 2026-06-04 — Public app/workspace host-command process definitions default to `supervisor` and only accept `supervisor` through `process:add` and `process:edit`. App/workspace `docker` process rows are reserved for Orbit-managed runtime processes such as FrankenPHP web-runtime units, while node-owned service definitions continue to support Docker and Docker Swarm runtimes. (solo todo #642)
- 2026-06-04 — WireGuard service-address self-routes are node provisioning/topology infrastructure, not app, process, tool, or database runtime prerequisites. Linux nodes may be diagnosed with `ip route get <wireguard-ip>` when a dependency endpoint points back at the same node's WireGuard service address; macOS is explicitly unsupported for this self-route optimization and Orbit must not mutate routes during this diagnostic. (solo todo #651)
- 2026-06-04 — Orbit keeps a strict tool/process boundary: tools are installed host capabilities with no lifecycle ownership, while runnable services such as Redis, MySQL, PostgreSQL, FrankenPHP, Horizon, OpenCode Server, and PolyScope Server are processes. A process may reference a host tool when its command depends on one, but service version, runtime, start/stop/restart/logs, and state belong to the process row or process definition, not to a tool runtime row. Docker Swarm is admitted as a process runtime for service processes; tool install/runtime intent is not the model for Redis, MySQL, or PostgreSQL. (solo todo #681)
- 2026-06-04 — OpenCode Server and PolyScope Server tool definitions are capability-only: install, remove, update, reconfigure, credentials, and safe doctor boundaries remain tool-owned, but real start, stop, restart, and logs for their long-running services are process-owned through node-level `runtime=systemd` rows. Compatibility `tool:*` lifecycle adapters may only route to an existing related process and must not install, probe, or simulate Supervisor-owned service lifecycle for these tools. (solo todo #700)
- 2026-06-04 — E2E lane ownership follows the process runtime contract: real `systemd` lifecycle for node-level Linux services such as OpenCode Server and PolyScope Server belongs in Incus only. Docker may cover command contracts, registry behavior, validation, Docker-runtime processes, and scoped seeded-drift repair, but it must not simulate `systemd` lifecycle with Supervisor or use broad tool restore coverage that can collect unrelated `systemd` process drift. (solo todo #700)
- 2026-06-04 — Superseded by the later strict tool/process boundary decision in solo todo #681: managed service tool rows are not the model for MySQL, PostgreSQL, or Redis. OpenCode Server and PolyScope Server lifecycle still belongs to node-owned `runtime=systemd` process rows related to their installed host tools. (solo todo #698)
- 2026-06-04 — Tool lifecycle commands are compatibility adapters over exactly one related process row. `tool:start`, `tool:stop`, `tool:restart`, and `tool:logs` fail explicitly when no related process exists, fail with `tool.process_ambiguous` when multiple related processes exist, and do not make `tool:update` restart processes implicitly. Tool install, update, adopt, and remove remain capability-owned. (solo todo #699)
- 2026-06-03 — Orbit names the Linux service process runtime `systemd`, not `systemctl`. `systemctl` is only the node command adapter. Node-level services such as `opencode-server` and `polyscope-server` are process rows with `runtime=systemd` and optional `--tool=opencode` or `--tool=polyscope` dependencies; tools remain installed capabilities and do not own service lifecycle. Docker E2E does not verify real `systemd` lifecycle; Incus owns VM-backed `systemd` process lifecycle coverage. (solo todo #696)
- 2026-06-03 — Orbit separates tools from processes. A tool is a node-level capability that roles may require and Orbit may install, update, adopt, or remove. A process is the lifecycle-managed long-running unit; processes own start/stop/restart/logs, runtime backend, restart policy, environment, command or image configuration, and optional node/app/workspace scope. Tools do not own lifecycle because one tool can back many processes. Managed database/cache/agent/web runtimes move toward process-backed lifecycle while tool rows remain capability and expected-state records during migration. (solo todo #694)
- 2026-06-03 — Orbit drops the `app:exec` and `workspace:exec` commands; there is no Orbit command-`exec` surface. FrankenPHP is the app/workspace PHP web runtime (the php-fpm replacement); ad-hoc `php`/`artisan`/`composer` run directly on the app node's host PHP toolchain in the app source path. This resolves the host-vs-container exec drift by removing exec entirely rather than choosing a side.
- 2026-06-03 — Host PHP CLI and Composer are installed on `app-dev` and `app-prod` nodes; the Laravel installer is installed on `app-dev` only (not `app-prod`). The host toolchain backs scaffolding (`laravel new`, `composer create-project`), Composer dependency management, and zero-downtime deployment. A contained app image built and deployed through Docker Swarm is a deferred later phase.
- 2026-06-03 — Scheduling is centralized on the gateway, which runs due schedules (tracked in the gateway DB) against each schedule's target nodes; no per-node scheduler.
- 2026-06-02 — The gateway ships as a packaged orbit-gateway Docker image; the gateway API and scheduler run as Docker Swarm services, and update:all runs as a durable operation via a one-shot runner. (solo todo #615)
