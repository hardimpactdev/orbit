# JSON Renderer: `orbit php:list`

[Back to `php:list` technical contract.](1_php-list.md)

**Renderer:** JSON renderer.

## Selection

Use this renderer when `--json` is present.

## Envelope

This renderer uses the shared [JSON Envelope](../../../README.md#json-envelope).

## Success Shape

```json
{
  "success": {
    "data": {
      "php": {
        "node": "app-1",
        "supported": ["8.4", "8.5"],
        "installed": ["8.5"],
        "cli": "8.5",
        "app": {
          "name": "docs",
          "php_version": "8.5"
        },
        "workspace": {
          "name": "feature-docs",
          "php_version": "8.4",
          "inherits": false
        }
      }
    },
    "meta": {}
  }
}
```

When `--live` is supplied, `success.meta.live=true`; otherwise `success.meta`
is empty.

## Error Shape

```json
{
  "error": {
    "code": "validation_failed",
    "message": "Required input is missing or invalid.",
    "meta": {
      "field": "node"
    }
  }
}
```

| Field | Type | Meaning |
| --- | --- | --- |
| `success.data.php` | object | PHP runtime view defined by the family README. |
| `success.meta.live` | boolean | Present and `true` only when `--live` supplied live installed-version facts. |
| `error.code` | `validation_failed`, `gateway_unavailable`, `authorization_failed`, `local_context_invalid` | Stable command failure code. |
| `error.message` | string | Human-readable failure summary. |
| `error.meta` | object | Field, target, caller, or live-inspection context for automation. |

## Test Mapping

| Path | Coverage |
| --- | --- |
| `tests/Feature/Commands/Php/PhpListJsonRendererTest.php` | JSON renderer selection, `success.data.php` shape, empty meta placement, `success.meta.live` with `--live`, and every `error.code` value documented in this renderer. |
