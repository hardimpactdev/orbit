candidate=1850023b95246803c7ed692af92ce93f0d8ae99a

Follow-up deadlock fix on the release-image reuse candidate.

Diagnosis: `fingerprint_owned_inputs` piped `git ls-files` into `sort` into a `while read` consumer whose stdout was the same redirected listing file. When that consumer stopped draining, `sort` blocked on a full pipe (observed as the stuck child). PID 3513329 was already gone when this worker inspected `/proc`; no leftover pest/candidate/sort process remained.

Fix: hash owned inputs with `git ls-tree -r HEAD` written directly to a temp file. Dockerfile `FROM`/`ARG` lines are already in those tree blobs, so the extra grep/sort pipeline is unnecessary.

RED (original reuse coverage): 12 failed, 46 passed.
GREEN after deadlock fix: `bin/orbit-gateway-pest --compact tests/Feature/E2ESupport/ReleaseCandidateHelperTest.php tests/Feature/Release/OrbitReleaseWorkflowTest.php` — 58 passed (753 assertions) in 10.67s.

Changed files since previous candidate: `bin/orbit-release-candidate`.

Proof receipt (`bin/orbit-feature-proof-receipt --json`):
```json
{
    "ok": true,
    "problem": null,
    "candidate": "1850023b95246803c7ed692af92ce93f0d8ae99a",
    "dirty": false,
    "docs_only": false,
    "gate": "quality-check",
    "artifact": "/home/nckrtl/orbit/.worktrees/codex-fix-worktree-package-dependencies/.orbit/quality-gates/quality-check-2026-08-23T101909Z-2cf554e8b8ef.json",
    "venue": "automated",
    "runtime": "not applicable"
}
```

Remaining risk: previous reusable metadata is still the latest local `candidate.env`. Worktree is clean.
