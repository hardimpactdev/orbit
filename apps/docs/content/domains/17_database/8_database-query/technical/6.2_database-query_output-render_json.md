# JSON Renderer: `orbit database:query`

[Back to `database:query` technical contract.](1_database-query.md)

## Primitive

None. JSON output is emitted as a machine-readable envelope via `$this->line(...)`.

## Envelope

This renderer uses the shared [JSON Envelope](../../../README.md#json-envelope).

## Success Shape

The gateway returns the generic query-runner result shape: `data` holds the
driver-reported result fields, and the resolved connection identity lives in
`meta` (`connection`, `driver`) alongside the read/write query metadata.

```json
{
  "success": {
    "data": {
      "columns": ["id", "email"],
      "rows": [
        { "id": 1, "email": "a@example.test" }
      ]
    },
    "meta": {
      "connection": "acme",
      "driver": "pgsql",
      "mode": "read",
      "limit": 50,
      "total_rows": 1,
      "returned_rows": 1,
      "truncated": false,
      "truncated_by": [],
      "max_json_bytes": 1048576,
      "duration_ms": 12
    }
  }
}
```

Write-mode successes put `affected_rows` under `data` and set `meta.mode` to
`write` with `duration_ms`.

## Error Shape

The top-level envelope is either `success` or `error`.

Exhaustive command-specific codes:

- `database_connection.not_found`
- `database_connection.target_not_found`
- `database_query.connection_ambiguous`
- `database_query.write_not_allowed`
- `database_query.execution_failed`

`--json` forces non-interactive input mode; this command never prompts for SQL.

## Test Mapping

| Path | Coverage |
| --- | --- |
| `apps/cli/tests/Feature/Commands/Database/DatabaseWriteCommandsTest.php` | Query success `success.data` row shape and SQL validation envelope. |
| `apps/gateway/tests/Feature/Http/Api/Database/DatabaseConnectionApiTest.php` | Query success payload, connection meta, and authorization failure envelopes. |

Truncation metadata and exhaustive documented query error codes remain coverage gaps until focused tests land.
