# Human Renderer: `orbit instance:websocket enable`

[Back to `instance:websocket enable` technical contract.](1_instance-websocket-enable.md)

This page describes the human-rendered output for `instance:websocket enable`.

**Renderer:** Human renderer.

## Primitive

None. Output is emitted as prose lines via `$this->line(...)`.

## Selection

Use this renderer by default when `--json` is absent.

## Progress Tree

No progress tree is rendered. The enable operation is a fast gateway write and
route sync; no slow external work is performed from the CLI side.

## Success Render

The success output presents the resulting binding fields as labeled key-value
output. The `binding:` label precedes the binding data, which includes the
`internal_host`, `public_hosts`, and `allowed_origins` values.

### Example output

```text
binding:
  project: docs
  instance: production
  serving_node: app-prod-1
  internal_host: websocket.orbit
  public_hosts:
    - ws.docs.example.com
  allowed_origins:
    - https://docs.example.com
```

## Error Render

Errors are rendered as concise prose messages.

## Test Mapping

| Path | Coverage |
| --- | --- |
| `apps/gateway/tests/Feature/Http/Api/AppWebSocketControllerTest.php` | Human renderer selection, concrete instance identity, serving node, and gateway-backed output coverage for the enable command. |
