# Interactive Input Mode: `orbit firewall:deny`

[Back to `firewall-deny` technical contract.](1_firewall-deny.md)

**Input mode:** Interactive.

## Mode Selection

This contract applies only when the process is attached to a TTY and `--json`
is not present.

## Prompt Flow

Prompts render after caller role and gateway reachability are known and before
any gateway write or backend enactment.

| Prompt ID | Primitive | Rendered when | Validation |
| --- | --- | --- | --- |
| `firewall.node` | `select` | No `--node` and no local default node resolves. | Visible active Ubuntu `gateway` or `app` node. |
| `firewall.name` | `text` | `name` argument omitted. | Unique rule name on the selected node. |
| `firewall.port` | `text` | `--port` omitted. | Destination port or supported port range. |
| `firewall.direction` | `select` | Operator did not pass `--direction` and the command needs confirmation of the default. | `incoming` or `outgoing`; default `incoming`. |

Invalid prompt answers re-prompt until the value is valid or the operator
aborts. Abort fails before side effects.

## Test Mapping

| Path | Coverage |
| --- | --- |
| `tests/Feature/Commands/Firewall/FirewallDenyCommandTest.php` | Interactive prompt rendering, prompt validation, abort behavior, and side-effect prevention before input resolution. |
