# Human Renderer: `orbit php:use`

[Back to `php:use` technical contract.](1_php-use.md)

**Renderer:** Human renderer.

## Primitive

- Progress: [Progress tree](../../../../ux/commands/progress/progress-tree.md)

## Selection

Use this renderer when `--json` is not present.

## Progress Tree

Render this progress tree after input resolution and before side effects begin:

```text
┌ Updating PHP runtime to PHP 8.5
○ Resolve affected instances
○ Authorize serving nodes
○ Validate images
○ Reconcile runtimes
└ Working...
```

Use the resolved version and target scope in the title:

- app/workspace version change: `Updating PHP runtime to PHP <version>`;
- workspace inheritance: `Restoring workspace PHP inheritance`;
- node CLI selection: `Updating node CLI PHP on <node> to PHP <version>`.

### App and workspace runtime

Use this label lifecycle:

| Pending | Active | Completed |
| --- | --- | --- |
| `Resolve affected instances` | `Resolving affected instances` | `Resolved affected instances` |
| `Authorize serving nodes` | `Authorizing serving nodes` | `Authorized serving nodes` |
| `Validate images` | `Validating images` | `Validated images` |
| `Reconcile runtimes` | `Reconciling runtimes` | `Reconciled runtimes` |

When the command completes, replace the footer with the result:

```text
┌ Updating PHP runtime to PHP 8.5
● Resolved affected instances
● Authorized serving nodes
● Validated images
● Reconciled runtimes
└ Successfully updated PHP runtime to PHP 8.5
```

For `--inherit`, the footer is
`Successfully restored workspace PHP inheritance`.

### Node CLI selection (`--cli`)

Render this progress tree after input resolution and before side effects begin:

```text
┌ Updating node CLI PHP on beast to PHP 8.5
│
○ Resolve target
│
○ Validate host PHP CLI version
│
○ Apply host PHP CLI default
│
└ Working...
```

Use this label lifecycle:

| Pending | Active | Completed |
| --- | --- | --- |
| `Resolve target` | `Resolving target` | `Resolved target <node>` |
| `Validate host PHP CLI version` | `Validating host PHP CLI version` | `Validated host PHP CLI version` |
| `Apply host PHP CLI default` | `Applying host PHP CLI default` | `Applied host PHP CLI default` |

When the command completes, replace the footer with the result:

```text
┌ Updating node CLI PHP on beast to PHP 8.5
│
● Resolved target beast
│
● Validated host PHP CLI version
│
● Applied host PHP CLI default
│
└ Successfully updated node CLI PHP on beast to PHP 8.5
```

Node CLI selection never uses FrankenPHP app/workspace runtime labels or
footers. Recovery prose points at the host `php-cli` toolchain, not image
catalogs.

## Success Output

For an app-policy write, human output renders one row per instance with App
instance, Driver, Node, Status, and Image columns. Orbit instances report
`reconciled` or `unchanged`; external-driver instances report `external` with
no node or image and are not claimed as reconciled. Workspace and node-CLI
results retain their single-target summaries. When the command clears a
workspace override, the output states the inherited app version.

## Failure Output

Human-rendered failures show the target, field, and recovery command when
available:

- Inspect available images with `orbit php:list --node=<node> --live`.
- Repair a failed app-instance reconciliation with the owning-family doctor
  command named beside that instance. App fan-out never renders partial
  success.
- Repair partial proxy backend application with
  `doctor --family=proxy --restore`.

## Test Mapping

| Path | Coverage |
| --- | --- |
| `apps/cli/tests/Feature/Commands/Php/PhpUseCommandTest.php` | CLI and app/workspace human progress tree shape, scope-specific step labels, success footers, JSON isolation, and absence of raw envelope key dumps. |
| `apps/gateway/tests/Unit/Services/Php/PhpRuntimeManagerTest.php` | Node CLI host `php-cli` validation, idempotent `changed=false` semantics, and absence of FrankenPHP image data in `node_cli` results. |
