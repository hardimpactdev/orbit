# `process:logs` Interactive Input Mode

[Back to `process:logs` technical contract.](1_process-logs.md)

This page describes how interactive input mode resolves `process:logs` input.

**Input mode:** Interactive.

## Selection

Use this input mode only when the process is attached to a TTY and `--json` is not present.

## Prompt Behavior

Prompt for missing required input in this order:

| Field | Prompt primitive | Notes |
| --- | --- | --- |
| `app` | Datatable | Show concrete app instances the caller is authorized to read when app-instance context is absent. A supplied ambiguous bare app slug fails with `validation_failed`, `field=app`, and `reason=app_instance_required` instead of prompting. |
| `name` | Datatable | Select from existing processes visible for the resolved app instance or across authorized instances. If no process exists, fail before prompting for an open value. |

Do not prompt for `lines` or `follow`; those remain explicit options with documented defaults.
`--workspace` is an explicit filter. If a supplied workspace name is ambiguous,
the command fails and asks the operator to pass `--app=<app.instance>`.

## Primitive References

- [`datatable`](../../../../ux/commands/lists/data-table-prompt.md)

## Test Mapping

Primary test owners:

| Path | Coverage |
| --- | --- |

There is no gateway-side coverage for this deferred mapping: no current routine gateway test proves this documented behavior. Keep it as a coverage gap until a focused test lands.
