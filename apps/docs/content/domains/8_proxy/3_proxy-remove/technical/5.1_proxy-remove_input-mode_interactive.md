# Interactive Input Mode: `orbit proxy:remove`

[Back to `proxy:remove` technical contract.](1_proxy-remove.md)

**Input mode:** Interactive.

## Mode Selection

This contract applies only when the process is attached to a TTY and `--json` is not present.

## Prompt Flow

Prompts render after gateway reachability is known and before any gateway write or backend cleanup.

| Prompt ID | Primitive | Rendered when | Validation |
| --- | --- | --- | --- |
| `proxy.domain` | `text` | `domain` argument omitted. | Existing custom route domain. |
| `proxy.confirm_remove` | `confirm` | Always after the custom route is resolved unless `--force` is present. | Explicit destructive consent. |

The confirmation text names the domain, route kind, serving node, target, and whether route-scoped TLS material will be cleaned up.

Rejected confirmation fails before side effects with `error.code=destructive_consent_required`.

## Primitive References

- [`text`](../../../../ux/commands/inputs/text-prompt.md)
- [`confirm`](../../../../ux/commands/inputs/confirm-prompt.md)

## Test Mapping

| Path | Coverage |
| --- | --- |
| `apps/cli/tests/Feature/Commands/Proxy/ProxyWriteCommandTest.php` | Interactive `proxy:remove` confirmation prompt, rejected confirmation behavior, and no gateway contact before consent. |

There is no gateway-side coverage for this command-local mapping: input handling and renderer behavior live in `apps/cli`. Gateway API behavior is mapped in the command contract file when a gateway-side surface exists.
