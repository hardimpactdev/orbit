interface:
  display_name: "Handling Feature Requests"
  short_description: "Frame an Orbit outcome and acceptance."
  default_prompt: "Use $handling-feature-requests to ask only docs-grounded, decision-changing questions; keep intake read-only and pre-worktree, stop when material ambiguity is none, then hand the settled frame to implementing-features."
