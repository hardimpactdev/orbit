# JSON Renderer: `orbit process:remove [name]`

[Back to `process:remove` technical contract.](1_process-remove.md)

This page describes the JSON-rendered output for `process:remove`.

**Renderer:** JSON renderer.

## Primitive

None. JSON output is emitted as a machine-readable envelope via `$this->line(...)`.

## Selection

Use this renderer when `--json` is present. `--json` also forces non-interactive input mode and never grants destructive consent.

## Envelope

This renderer uses the shared [JSON Envelope](../../../README.md#json-envelope).

Success data contains the removed process identity and cleanup summary. Partial post-configuration cleanup drift is reported under `success.meta.warnings[]`.

Warning objects contain:

| Field | Meaning |
| --- | --- |
| `code` | Singular product warning code, such as `process.runtime_unit_missing`, with the owning family recorded separately. |
| `family` | State family that owns the warning. |
| `message` | Human-readable warning summary. |
| `next_command` | Suggested command to inspect or repair the warning. |

## Success Example

```json
{
  "success": {
    "data": {
      "process": {
        "name": "vite",
        "app": "docs"
      },
      "removed_runtime_units": [
        "orbit_docs_main_vite"
      ]
    },
    "meta": {
      "warnings": []
    }
  }
}
```

## Error Example

```json
{
  "error": {
    "code": "validation_failed",
    "message": "Use --force to remove this process.",
    "meta": {
      "field": "force"
    }
  }
}
```

## Caller Role Error

```json
{
  "error": {
    "code": "caller_role_not_allowed",
    "message": "This command may only be run from a control or gateway node.",
    "meta": {
      "caller_role": "app"
    }
  }
}
```

## Error Code Vocabulary

- `validation_failed`: missing `name`, missing `app`, missing non-interactive `--force`, or declined interactive confirmation.
- `caller_role_not_allowed`: caller role is `app` or `unknown`.
- `authorization_failed`: caller cannot manage process configuration for the target app.
- `process.not_found`: the named process does not exist for the owning app.
- `gateway_unavailable`: the CLI cannot reach the gateway API before removal begins.

## Test Mapping

Primary test owners:

| Path | Coverage |
| --- | --- |
| `tests/Feature/Commands/Processes/ProcessRemoveJsonRendererTest.php` | JSON renderer selection, success envelope, cleanup data, warning object shape, destructive consent coverage, missing consent error envelope, caller-role error envelope, process-not-found error envelope, authorization error envelope, every documented error code value, and no mixed envelopes. |
