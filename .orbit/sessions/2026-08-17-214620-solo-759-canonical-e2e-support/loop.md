# Orbit Feature Loop

Copying this template is handled by `bin/orbit-prepare-worktree`. Keep it as the
single anchor for the active feature. Raw feedback belongs in the immutable
`.orbit/feedback.jsonl` stream, never in this file.

- Scratchpad: `solo://proj/2/todo/i08-b-give-the-exter--759`
- Worktree: `/Users/nckrtl/orbit/.worktrees/solo-759-canonical-e2e-support`
- Branch: `solo-759-canonical-e2e-support`

## Goal

The external E2E harness has ONE canonical Pest support tree: `apps/e2e/tests/E2E/Support` is the single source of truth (Pest.php with the suite-specific `e2eJsonCommandError` helper + SqliteDatabaseFixture.php), and the runner support tree consumed by `E2ETestCommand` staging is copied/generated from canonical rather than hand-maintained, so the two trees can never drift and every staged test (including `e2eJsonCommandError` consumers) resolves its helpers.

## Scope

- Owned: apps/e2e external E2E support trees (`tests/E2E/Support` canonical vs generated `tests/Feature/Commands/Support` snapshot), `E2ETestCommand` support-staging via `E2EPestSupportTree`, and a deterministic support-contract test proving byte-identity plus `e2eJsonCommandError` resolution.
- Constraints: apps/e2e PHP only; do NOT invent a new test framework; preserve the `e2eJsonCommandError` helper for its consumers (NodeUpdatesDoctorTest, WorkspacesDoctorTest); keep the staged runner support behavior-identical for manual E2E execution; deterministic support tests only (`composer --working-dir=apps/e2e test` group-excludes the human-only e2e-* lanes) — NEVER `composer test:e2e*`; declare(strict_types=1); Mago/Rector clean.
- Out of scope: the actual E2E lane tests' behavior, topology providers, non-support harness code, and any gateway/CLI changes.

## Proof

- Verification:
  - focused: passed - `apps/e2e` Pest `E2EPestSupportTreeContractTest` + `E2ETestCommandTest` + new `E2ESupportTreeSyncCommandTest` (RED first: `e2e:support-tree:sync` command absent + Pest.php drift; GREEN after)
  - broader: passed - `composer quality-check` exit 0 on 5e8b9ee2a28cc721adac27488e0a459f219a85f3, dirty=false, 45/45 subgates zero (its apps/e2e subgates are Mago analyze/format + Rector); the apps/e2e deterministic Pest lane 411 passed was run separately by the worker (`composer --working-dir=apps/e2e test`, not a quality-check subgate); receipt `.orbit/quality-gates/quality-check-2026-08-17T194115Z-25bcaf4359fc.json`
  - runtime: passed - candidate=5e8b9ee2a28cc721adac27488e0a459f219a85f3; venue=retained-incus; environment=dev-fixture; command=on retained topology dev-b797f5 operator node ran E2EPestSupportTree::copyTo staging and confirmed the new e2e:support-tree:sync command is registered (candidate bound by sha256); expected=runner tree generated from canonical with a regeneration command, staged tree byte-identical to canonical, e2eJsonCommandError resolves for staged runs; observed=staging checks all pass (file-list identical, staged Pest.php and fixture byte-identical to canonical, staged Pest.php resolves e2eJsonCommandError subprocess exit 0) and e2e:support-tree:sync registered/resolvable on the node; result=passed; evidence=`.orbit/evidence/solo-759-retained-incus-support-tree-proof.json`
- Blast radius: complete - evidence=repository-wide `rg "Feature/Commands/Support"` + `rg "function e2eJsonCommandError"` + `rg "copy.*Support|glob.*Support"` across apps/e2e (non-vendor); result=only one stager remains (E2ETestCommand -> E2EPestSupportTree::copyTo from canonical tests/E2E/Support); the runner Pest.php + SqliteDatabaseFixture.php are now a GENERATED snapshot of canonical, regenerated by `php artisan e2e:support-tree:sync` and guarded by the contract test (drift both fixable and detected); e2eJsonCommandError present in both trees; pre-existing E2ETestCommandTest:1177 expectation and mago-linter-baseline entries for the runner tree remain valid
- Review: passed - fresh Claude reviewer 2483 (2nd round, independent): trees byte-identical (canonical restores identity vs drifted base), regen direction correct (E2ESupportTreeSyncCommand copies canonical->runner), staging sources from canonical with a durable command-source guard, drift now DETECTED and FIXABLE via `e2e:support-tree:sync` (auto-discovered, hidden, tested), deterministic lane 408 passed (+4 new), quality receipt + runtime receipt hashes verified genuine, blast radius contained (no human-only lane touched). Three minor non-blocking findings (copyTo no-prune, #[\Override] on $hidden property nit, cosmetic Wrote output). Prior round's sole gap resolved. human-judgment=not-required. VERDICT: PASS
- Reviewed feature tip: 5e8b9ee2a28cc721adac27488e0a459f219a85f3
- Acceptance venue: retained-incus
- Acceptance: accepted - automated - reviewer-confirmed no-human-judgment
- Accepted feature tip: 5e8b9ee2a28cc721adac27488e0a459f219a85f3
- Accepted main tip: 7751aef8b1565bec92f19fbe2d79d1732becc2ab

## Status

- State: accepted
- Blocker: none

## Feedback

- Events: `.orbit/feedback.jsonl`

Allowed states are `frame`, `build`, `prove`, `accept`, `accepted`, `land`, and
`blocked`. Review must be `passed`, `fix`, or `escalate`. Acceptance must be
`pending`, `accepted`, `not-applicable`, or `changes-requested`. A terminal
review PASS must record `human-judgment=required|not-required` and the exact
committed HEAD in `Reviewed feature tip`. Blast radius must be `not-required -
concrete reason` or `complete - evidence=repository-wide search, inventory, or
lintable check; result=summary` before acceptance; `gaps` returns to BUILD.
For stateful, lifecycle, or concrete UX work, optionally append one compact
clause on the existing Scope `Owned` row (do not add a permanent new row):
`primitive=exact requested primitive; transitions=success:terminal success|failure:terminal failure|retry:retry behavior|stop-restart:stop or restart|stale:stale-state or n/a`.
Omit the clause for ordinary/local changes. When `primitive=` or `transitions=`
is present, deterministic lint requires both fields, the five known transition
keys without duplicates or empty values, and rejects template placeholders; it
does not grade prose or decide whether the feature is stateful. Explicit `n/a`
values are fine when a transition does not apply. After FRAME, run
`bin/orbit-feature-acceptance route` for the read-only
diff-derived venue before expensive PROVE work. For non-`automated` venues,
`Verification.runtime: passed` must use one candidate-bound structured receipt
on that same single line. Required fields are candidate=, venue=, environment=,
expected=, observed=, result=passed, and evidence= as one exact inline-code path
under the worktree evidence or quality-gates trees. Use exactly one of target= or command=.
Live/production claims require exact environment=live; ordinary retained
topology may use environment=dev-fixture. Semicolons separate fields,
so values must not embed raw semicolon-delimited pseudo-fields. Known keys
only. Example evidence citation: write a real receipt and cite one exact regular
file below the worktree evidence tree (not a directory root). A failed,
excluded, still-required, or deferred final hop cannot be recorded as passed;
remain in PROVE, disarm any armed or recorded acceptance, and follow FIX ->
BUILD -> PROVE before ACCEPT. Keep a still-valid Review and Reviewed feature tip
on proof-only retries; a HEAD change still needs a refreshed review. Automated
venues keep `runtime: not applicable`. Proof files retained by the compact
archive must be cited as one exact inline-code path; prose, directories, padded
code spans, and partial paths are not proof citations.
