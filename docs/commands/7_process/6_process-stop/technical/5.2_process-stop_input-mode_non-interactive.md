# `process:stop` Non-Interactive Input Mode

[Back to `process:stop` technical contract.](1_process-stop.md)

This page describes how non-interactive input mode resolves `process:stop` input
without prompts.

**Input mode:** Non-interactive.

## Selection

Use this input mode when either condition is true:

- the command is running without a TTY;
- `--json` is present.

`--json` selects the JSON renderer and forces non-interactive input mode even
when the process has a TTY.

## Responsibilities

- Read supplied arguments, options, and local context.
- Apply documented defaults.
- Never render prompts.
- Fail before side effects when required app or workspace context is absent.
- Fail before side effects when supplied input is forbidden or invalid.
- Treat omitted `[name]` as all-process selection for the resolved app or
  workspace context.

## Required Input Failures

| Field | Missing when | Behavior |
| --- | --- | --- |
| `app` | Neither `--app` nor local context resolves exactly one app, and `--workspace` does not resolve an app. | Fail before side effects with `error.code=validation_failed` and `error.meta.field=app`. |

## Test Mapping

Primary test owners:

| Path | Coverage |
| --- | --- |
| `tests/Feature/Commands/Processes/ProcessStopNonInteractiveInputModeTest.php` | No-prompt selection without a TTY, `--json` forcing non-interactive mode, missing app failures, workspace context resolution, all-process selection when `[name]` is omitted, invalid option failures, and no side effects on validation failure. |
