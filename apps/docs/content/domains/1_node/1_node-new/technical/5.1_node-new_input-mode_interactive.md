# Interactive Input Mode: `orbit node:new [name]`

[Back to `node:new` technical contract.](1_node-new.md)

This page describes how interactive input mode resolves missing `node:new`
input before command behavior starts.

**Input mode:** Interactive.

## Selection

Use this input mode only when both conditions are true:

- the command is running in a TTY;
- `--json` is not present.

When `--json` is present, use
[`5.2_node-new_input-mode_non-interactive.md`](5.2_node-new_input-mode_non-interactive.md) even if the process has a TTY.

Authorization is owned by the gateway. Callers that are configured but are not
gateways resolve input like other clients and may receive a gateway
authorization failure after the request is forwarded. Unconfigured clients can
only use the first-gateway bootstrap path.

## Responsibilities

- Read any supplied arguments and options.
- Prompt for missing required fields before side effects.
- Validate field-local values immediately when a supplied value is read or a
  prompt is submitted.
- Re-prompt after invalid field-local values.
- Stop before side effects when a resolved value violates a domain rule that
  cannot be corrected by re-prompting.
- Return a fully resolved input object to the canonical command flow.

## Prompt Order

| Step | Prompt ID | Rendered when |
| --- | --- | --- |
| 1 | `node_new.name` | `[name]` is missing. |
| 2 | `node_new.template` | Neither `--template`, `--operator`, nor `--roles` is supplied. |
| 3 | `node_new.tld` | `--tld` is missing. Every node identity requires an explicit TLD. |
| 4 | `node_new.host` | A host-provisioned template or role set is selected and `--host` is missing. |
| 5 | `node_new.app_production_public_traffic` | `app-production` template or `app-prod` role is selected. |
| 6 | `node_new.ingress_node` | Private `app-prod` placement is selected and `--ingress` is missing. |
| 7 | `node_new.operator_name` | First-gateway bootstrap is selected and `--operator-name` is missing. |
| 8 | `node_new.operator_tld` | First-gateway bootstrap is selected and `--operator-tld` is missing. |
| 9 | `node_new.valkey_node` | `websocket` template or role is selected and `--valkey-node` is missing. |
| 10 | `node_new.user` | SSH provisioning is used and `--user` is missing; the prompt lets the operator override the `root` default. |

When neither `--template`, `--operator`, nor `--roles` is supplied after the
template prompt, interactive input mode may continue down the bare client
identity path with no assigned roles.

Render `node_new.operator_name` only for first-gateway bootstrap. The default is
the local short hostname normalized to the node name format used by the gateway registry:
lowercase it, replace each run of unsupported characters with one hyphen, and
trim leading or trailing hyphens. If that default is empty, invalid, or
conflicts with the gateway node name, keep the operator at the prompt until a
valid client name is entered or the prompt is aborted.

When no node role is selected for a client identity, still render the mandatory
`node_new.tld`, but do not render `node_new.host`, `node_new.operator_name`,
`node_new.operator_tld`, or `node_new.user`.

When canonical `app-prod` is selected through a template expansion or explicit
role set, render the public traffic prompt before later prompts that depend on
placement:

```text
Serve public traffic from this node? [yes]
```

`yes` resolves to colocated `app-prod` + `ingress`. `no` resolves
to a private backend `app-prod` node and requires `node_new.ingress_node`.

## Prompt Mapping

Prompt mappings use stable prompt IDs and Laravel Prompts primitive names.

### `node_new.name`

| Property | Value |
| --- | --- |
| Primitive | `text` |
| Label | `Node name` |
| Source | `[name]` |
| Required | yes |
| Choices | none |
| Default | none |
| Validation timing | When `[name]` is read or when the prompt is submitted. |
| Invalid terminal behavior | Show validation message and re-prompt. |

### `node_new.template`

| Property | Value |
| --- | --- |
| Primitive | `select` |
| Label | `Node template` |
| Source | `--template` |
| Required | no |
| Choices | `operator`, `app-development`, `app-production`, `gateway`, `ingress`, `database`, `s3`, `websocket`, `metrics`, `agent` |
| Default | none |
| Validation timing | When `--template` is read or when the prompt is submitted. |
| Invalid terminal behavior | Show validation message and re-prompt. |

### `node_new.host`

| Property | Value |
| --- | --- |
| Primitive | `text` |
| Label | `Host` |
| Source | `--host` |
| Required | yes for nodes and gateway nodes |
| Choices | none |
| Default | none |
| Validation | IP address or dotted DNS name. |
| Validation timing | When `--host` is read or when the prompt is submitted. |
| Invalid terminal behavior | Show validation message and re-prompt. |

### `node_new.app_production_public_traffic`

| Property | Value |
| --- | --- |
| Primitive | `confirm` |
| Label | `Serve public traffic from this node?` |
| Source | placement choice for canonical `app-prod` |
| Required | yes for canonical `app-prod` |
| Choices | `yes`, `no` |
| Default | `yes` |
| Validation timing | When the prompt is submitted. |
| Invalid terminal behavior | Re-render the prompt. |

### `node_new.ingress_node`

| Property | Value |
| --- | --- |
| Primitive | `select` |
| Label | `Ingress node` |
| Source | `--ingress` |
| Required | yes for private `app-prod` placement |
| Choices | Active `ingress` nodes |
| Default | none |
| Validation timing | When `--ingress` is read or when the prompt is submitted. |
| Invalid terminal behavior | Show validation message and re-prompt. |

### `node_new.operator_name`

| Property | Value |
| --- | --- |
| Primitive | `text` |
| Label | `Client name` |
| Source | `--operator-name` |
| Required | yes for first-gateway bootstrap |
| Choices | none |
| Default | Normalized local short hostname. |
| Validation timing | When `--operator-name` is read or when the prompt is submitted. |
| Invalid terminal behavior | Show validation message and re-prompt. |

### `node_new.tld`

| Property | Value |
| --- | --- |
| Primitive | `text` |
| Label | `Node TLD` |
| Source | `--tld` |
| Required | yes for every node identity |
| Choices | none |
| Default | none |
| Validation timing | When `--tld` is read or when the prompt is submitted; reserved `orbit` is invalid. |
| Invalid terminal behavior | Show validation message and re-prompt. |

### `node_new.operator_tld`

| Property | Value |
| --- | --- |
| Primitive | `text` |
| Label | `Client TLD` |
| Source | `--operator-tld` |
| Required | yes for first-gateway bootstrap |
| Choices | none |
| Default | none |
| Validation timing | When `--operator-tld` is read or when the prompt is submitted; it must be unique, differ from `node_new.tld`, and cannot be reserved `orbit`. |
| Invalid terminal behavior | Show validation message and re-prompt. |

### `node_new.valkey_node`

| Property | Value |
| --- | --- |
| Primitive | `select` |
| Label | `Valkey node` |
| Source | `--valkey-node` |
| Required | yes for `websocket` |
| Choices | Active `database` nodes with Valkey expected or installed |
| Default | none |
| Validation timing | When `--valkey-node` is read or when the prompt is submitted. |
| Invalid terminal behavior | Show validation message and re-prompt. |

### `node_new.user`

| Property | Value |
| --- | --- |
| Primitive | `text` |
| Label | `SSH user` |
| Source | `--user` |
| Required | no; default satisfies omission |
| Choices | none |
| Default | `root` |
| Validation timing | When `--user` is read or when the prompt is submitted. |
| Invalid terminal behavior | Show validation message and re-prompt. |

## Primitive References

- [`text`](../../../../ux/commands/inputs/text-prompt.md)
- [`select`](../../../../ux/commands/inputs/select-prompt.md)
- [`confirm`](../../../../ux/commands/inputs/confirm-prompt.md)

## Failure Semantics

- Missing required input is collected by prompt before side effects.
- Invalid field-local input shows a validation message and re-prompts until the
  value is valid, the answer changes the selected path so the prompt is no
  longer needed, or the user aborts. There is no generic retry cap.
- Path eligibility is evaluated as soon as the prompt result makes the selected
  path knowable. If a blocker is already knowable and the current prompt can
  correct it, show a validation message and re-prompt at that prompt instead of
  asking for later unrelated input. If the blocker cannot be corrected by the
  current prompt flow, stop before side effects.
- Forbidden input follows the canonical input contract. If a forbidden value was
  collected by prompt and the current prompt can correct it safely, show a
  validation message and re-prompt. If the value was supplied as an option or
  argument, or cannot be corrected safely in the current prompt flow, fail
  before side effects.
- Gateway-side `authorization_failed` rejections (missing `node:new` on the
  caller's grant) surface before prompts or side effects.
- If the user aborts a prompt, exit with the standard command failure status
  and do not start side effects.

## Test Mapping

| Path | Coverage |
| --- | --- |

There is no gateway-side coverage for this deferred mapping: no current routine test proves this documented behavior. Keep it as a coverage gap until a focused test lands.
