# Human Renderer: `orbit instance:analytics enable`

[Back to `instance:analytics enable` technical contract.](1_instance-analytics-enable.md)

**Renderer:** Human renderer.

## Primitive

- Progress: [Progress tree](../../../../ux/commands/progress/progress-tree.md)

## Selection

Use this renderer by default when `--json` is absent.

## Progress Tree

Render an in-progress tree after input resolution and before gateway writes or
route enactment begins.

```text
┌ Enabling App Analytics
○ Validate app and analytics prerequisites
○ Register public tracking routes
○ Apply router tracking routes
○ Apply ingress TLS and tracking routes
└ Working...
```

## Success Render

```text
binding:
  app: docs
  instance: production
  serving_node: app-prod-1
  enabled: true
  site_domain: docs.example.com
  internal_host: analytics.orbit
  dashboard_url: https://analytics.orbit
  public_hosts:
    - analytics.docs.example.com
  tracking_endpoints:
    - host: analytics.docs.example.com
      script_base_url: https://analytics.docs.example.com
      script_url: https://analytics.docs.example.com/js/script.js
      event_endpoint: https://analytics.docs.example.com/api/event
      data_domain: docs.example.com
      snippet: <script defer data-domain="docs.example.com" src="https://analytics.docs.example.com/js/script.js"></script>
route_enactment:
  status: completed
  placements:
    - router
    - ingress
dns_expectation:
  ingress_node: ingress-1
  targets:
    - type: A
      value: 203.0.113.10
  provider_managed: false
public_readiness:
  status: not_verified
remaining_actions:
  - configure_provider_dns
  - ensure_plausible_site
  - integrate_application_script
  - verify_public_readiness
```

## Error Render

Errors are rendered as concise prose messages.

## Test Mapping

| Path | Coverage |
| --- | --- |
| `apps/gateway/tests/Feature/Http/Api/AppAnalyticsControllerTest.php` | Gateway response fields that human output renders and validation failure payloads. |
| `apps/cli/tests/Feature/Commands/App/AppAnalyticsEnableCommandTest.php` | Human renderer success output and local validation prose. |
