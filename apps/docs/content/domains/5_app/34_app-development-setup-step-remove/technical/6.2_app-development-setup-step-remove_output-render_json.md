# JSON output: `app-development-setup-step:remove`

## Envelope

This renderer uses the shared [JSON Envelope](../../../README.md#json-envelope).

## Primitive

None. JSON output is emitted as one machine-readable envelope.

## Test Mapping

| Path | Coverage |
| --- | --- |
| `apps/cli/tests/Feature/Commands/App/AppDevelopmentSetupStepCommandTest.php` | JSON removal envelope with destructive consent. |
| `apps/gateway/tests/Feature/Http/Api/AppDevelopmentSetupStepControllerTest.php` | Gateway removed-step envelope and errors. |

Success returns `success.data.step` for the removed row; failure returns the shared `error` envelope.
