# `orbit database:schema <target>`

[Back to Database commands.](../README.md)

Show schema metadata for a stored database connection resolved from a dotted instance or workspace target.

## Usage

```bash
orbit database:schema {target} [--connection=<slug>] [--json]
```

## Technical Contract

See [`database-schema` technical contract](technical/1_database-schema.md).
