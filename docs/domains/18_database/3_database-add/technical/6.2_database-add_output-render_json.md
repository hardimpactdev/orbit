# JSON Renderer: `orbit database:add`

[Back to `database:add` technical contract.](1_database-add.md)

## Primitive

None. JSON output is emitted as a machine-readable envelope via `$this->line(...)`.

## Envelope

This renderer uses the shared [JSON Envelope](../../../README.md#json-envelope).

## Success Shape

`success.data.connection` uses the database JSON entity defined in
[`../../README.md`](../../README.md#database-json-entity).

## Error Shape

The top-level envelope is either `success` or `error`.

Exhaustive command-specific codes:

- `database_connection.slug_taken`
- `validation_failed`
- `authorization_failed`

## Test Mapping

| Path | Coverage |
| --- | --- |
| `tests/Feature/Commands/Database/DatabaseAddJsonRendererTest.php` | `success.data.connection` shape and exhaustive error-code coverage. |
