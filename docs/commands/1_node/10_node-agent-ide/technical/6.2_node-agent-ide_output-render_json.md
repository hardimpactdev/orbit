# JSON Renderer: `orbit node:agent-ide [name] [agent_ide]`

[Back to `node:agent-ide` technical contract.](1_node-agent-ide.md)

This page describes the JSON-rendered output for `node:agent-ide`.

**Renderer:** JSON renderer.

## Primitive

None. JSON renderer emits the standard envelope only.

## Selection

Use this renderer when `--json` is present.

`--json` also disables interactive prompts and forces the
[`non-interactive input mode`](5.2_node-agent-ide_input-mode_non-interactive.md), even
when the process has a TTY.

## Envelope

This renderer uses the shared [JSON Envelope](../../../README.md#json-envelope).

## Success Shape

### Adapter set to a new value

```json
{
  "success": {
    "data": {
      "name": "app-1",
      "agent_ide": {
        "adapter": "opencode",
        "source": "node"
      },
      "action": "set"
    }
  }
}
```

### Adapter cleared with `none`

```json
{
  "success": {
    "data": {
      "name": "app-1",
      "agent_ide": {
        "adapter": null,
        "source": "default"
      },
      "action": "set"
    }
  }
}
```

### No-op convergence (already matching)

```json
{
  "success": {
    "data": {
      "name": "app-1",
      "agent_ide": {
        "adapter": "opencode",
        "source": "node"
      },
      "action": "converged"
    }
  }
}
```

| Field | Type | Meaning |
| --- | --- | --- |
| `name` | string | The target node name. |
| `agent_ide.adapter` | string \| null | The configured adapter name, or `null` when cleared with `none`. |
| `agent_ide.source` | string | `"node"` when an explicit non-`none` adapter is set; `"default"` when cleared with `none`. |
| `action` | string | `"set"` when the value changed; `"converged"` when the value already matched. |

## Error Shapes

### Missing required input

```json
{
  "error": {
    "code": "validation_failed",
    "message": "Node name is required.",
    "meta": {
      "field": "name"
    }
  }
}
```

```json
{
  "error": {
    "code": "validation_failed",
    "message": "Agent IDE adapter is required.",
    "meta": {
      "field": "agent_ide"
    }
  }
}
```

### Node not found

```json
{
  "error": {
    "code": "node.not_found",
    "message": "Node 'app-1' not found.",
    "meta": {
      "name": "app-1"
    }
  }
}
```

### Unsupported adapter

```json
{
  "error": {
    "code": "node.unsupported_adapter",
    "message": "Adapter 'unknown-ide' is not supported.",
    "meta": {
      "adapter": "unknown-ide"
    }
  }
}
```

### Gateway unavailable

```json
{
  "error": {
    "code": "gateway_unavailable",
    "message": "Gateway connection is required to update node configuration.",
    "meta": {}
  }
}
```

### Authorization failed

```json
{
  "error": {
    "code": "authorization_failed",
    "message": "This control node is not authorized to update node configuration.",
    "meta": {
      "required_node": "gateway-1",
      "caller_role": "control"
    }
  }
}
```

### Caller role not allowed

```json
{
  "error": {
    "code": "caller_role_not_allowed",
    "message": "This command may only be run from a control or gateway node.",
    "meta": {
      "caller_role": "app"
    }
  }
}
```

## Enum And Identifier Values

| Field | Values |
| --- | --- |
| `action` | `set`, `converged` |
| `agent_ide.source` | `node`, `default` |
| `error.code` | `validation_failed`, `node.not_found`, `node.unsupported_adapter`, `gateway_unavailable`, `authorization_failed`, `caller_role_not_allowed` |

The `error.code` values listed here are exhaustive for `node:agent-ide`. Add a
new value to this renderer contract before implementing another JSON failure
code.

## Test Mapping

Primary test owners:

| Path | Coverage |
| --- | --- |
| `tests/Feature/Commands/Nodes/NodeAgentIdeCommandTest.php` | JSON envelope shape, success payload with `agent_ide` object, converged payload, clear payload, node-not-found error, unsupported-adapter error, app-role caller-denial error, gateway-unavailable error, and authorization error. |

Required missing-file coverage:

| Path | Coverage |
| --- | --- |
| `tests/Feature/Commands/Nodes/NodeAgentIdeJsonRendererTest.php` | JSON renderer selection, discriminated envelope shape, success payload fields including `agent_ide.adapter` and `agent_ide.source`, converged payload, clear payload, every `error.code` value, error metadata, and enum values. |
