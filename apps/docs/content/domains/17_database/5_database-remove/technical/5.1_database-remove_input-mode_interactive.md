# Technical Contract: `orbit database:remove` (Interactive Input)

This contract defines behavior for **interactive input mode**.

[Back to the canonical contract.](1_database-remove.md)

**Input mode:** Interactive.

Use this input mode only when both conditions are true:

- the command is running in a TTY;
- `--json` is not present.

When `--json` is present, use
[`5.2_database-remove_input-mode_non-interactive.md`](5.2_database-remove_input-mode_non-interactive.md)
even if the process has a TTY.

## Prompts

| ID | Primitive | Label | Required |
| --- | --- | --- | --- |
| `force` | `confirm` | Remove database connection and all target mappings? | Yes when `--force` is absent. |

## Primitive References

- [`confirm`](../../../../ux/commands/inputs/confirm-prompt.md)

## Behavior

- Orbit prompts once for explicit destructive confirmation after resolving the selected connection.
- A declined confirmation exits without side effects and returns
  `error.code=validation_failed`, `error.meta.field=force`, and
  `error.meta.reason=destructive_consent_required`.

## Test Mapping

| Path | Coverage |
| --- | --- |
| `apps/cli/tests/Feature/Commands/Database/DatabaseWriteCommandsTest.php` | Confirmation prompt and affirmative consent path that dispatches delete with `force=true`. |

There is no gateway-side coverage for this mapped surface; linked CLI tests cover only the mapped assertions above. Remaining behavior stays a coverage gap until focused tests land.

There is no current routine CLI test for a declined confirmation cancel path; keep that assertion as a coverage gap until a focused test lands.
