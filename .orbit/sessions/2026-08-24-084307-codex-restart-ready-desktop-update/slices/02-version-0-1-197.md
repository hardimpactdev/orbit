# Orbit Feature Slice

- Slice: 02-version-0-1-197
- Depends on: 01-restart-ready-handoff

## Outcome

The corrected release candidate has version `0.1.197`, which is newer than the
rejected private `0.1.196` candidate already installed on Mini and NMBP.

## Scope

- Included: Change only the root `VERSION` file from `0.1.196` to `0.1.197` and prove the canonical version helper reports it.
- Excluded: Release artifact builds, tags, GitHub releases, changelog prose, and any additional source change.

## Authority

- Decisions: The public release remains `v0.1.195`; live Macs already report the rejected private candidate `0.1.196`, so a strictly newer candidate is required for normal update selection.
- Product docs: `.agents/skills/release/SKILL.md` versioned release contract.

## Proof

- Focused: RED that `bin/orbit-version` reports `0.1.196`; GREEN that it reports exactly `0.1.197`, plus `git diff --check`.
