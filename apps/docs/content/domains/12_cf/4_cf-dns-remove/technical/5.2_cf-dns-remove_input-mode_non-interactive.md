# Non-Interactive Input Mode: `orbit cf-dns:remove`

[Back to `cf-dns:remove` technical contract.](1_cf-dns-remove.md)

**Input mode:** Non-interactive.

## Selection

Use this input mode without a TTY or when `--json` is present.
`--json` forces non-interactive input mode.

## Prompt Behavior

Never render prompts. `--force` is required for destructive consent.
Missing `--force` fails before side effects with `error.code=validation_failed`
and `error.meta.field=force`. `--json` never provides destructive consent.

## Test Mapping

| Path | Coverage |
| --- | --- |
| `apps/cli/tests/Feature/Commands/Cloudflare/CloudflareWriteCommandsTest.php` | JSON missing-`--force` validation before gateway contact and forced non-interactive removal. |
| `apps/cli/tests/Feature/Commands/Cloudflare/CloudflareRenderCommandsTest.php` | Human missing-`--force` failure before progress tree rendering. |

There is no gateway-side coverage for this command-local input mode: input handling lives in `apps/cli`. Linked tests cover the non-interactive paths above. Behavior without a TTY and without JSON mode remains a coverage gap until focused tests land.
