# Human Renderer: `orbit solo:scratchpad:clear`

[Back to `solo:scratchpad:clear` technical contract.](1_solo-scratchpad-clear.md)

**Renderer:** Human renderer.

## Primitive

None. Solo command renderers print compact resource or status output from the Solo response.

## Selection

Use this renderer for ordinary operator-facing output.

## Progress Tree

No progress tree is rendered. Solo commands are gateway proxy operations with a single result.

## Output

The renderer displays the primary Solo resource, list, or mutation result returned by the gateway. Failures render the Orbit error code and message.

## Destructive consent

Without `--force`, interactive mode confirms the resolved scratchpad before the
gateway request. Declining renders `validation_failed: Operation cancelled.`
and performs no request. `--force` skips the prompt.

## Test Mapping

| Path | Coverage |
| --- | --- |
| `apps/cli/tests/Feature/Commands/Solo/SoloReadOnlyCommandTest.php` | Human rendering for read-only Solo responses. |
| `apps/cli/tests/Feature/Commands/Solo/SoloMutatingCommandTest.php` | Destructive consent prompt and human failure rendering for mutating Solo responses. |
| `apps/gateway/tests/Feature/Http/Api/SoloProxyControllerTest.php` | Gateway response shape consumed by the CLI renderer. |
