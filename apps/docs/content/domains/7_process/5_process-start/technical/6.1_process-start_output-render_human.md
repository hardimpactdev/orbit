# Human Renderer: `orbit process:start [name]`

[Back to `process:start` technical contract.](1_process-start.md)

This page describes the human-rendered terminal output for `process:start`.

**Renderer:** Human renderer.

## Primitive

- Progress: [Progress tree](../../../../ux/commands/progress/progress-tree.md)

## Selection

Use this renderer when `--json` is not present.

## Progress Tree

Render the complete progress tree after input resolution and before side effects begin. `process:start` performs remote runtime actions.

```text
┌ Starting Processes
○ Resolve runtime units
○ Start runtime units
○ Record process events
└ Working...
```

## Success Output

For a named process:

```text
Process 'vite' started for app instance 'docs.production'
```

For a workspace context:

```text
Process 'vite' started for workspace 'feature-docs' on app instance 'docs.production'
```

When `[name]` is omitted:

```text
2 processes started for workspace 'feature-docs' on app instance 'docs.production'
```

## Failure Output

Human-rendered failures must show the failing field or phase, clear prose, no JSON envelope, and next-step guidance only when the operator can act on it. Bulk failures must identify the failed process names and leave successful processes visible in the summary.

## Test Mapping

Primary test owners:

| Path | Coverage |
| --- | --- |
| `apps/cli/tests/Feature/Commands/Process/ProcessWriteCommandTest.php` | Human process:start progress tree output for named and bulk runtime actions, success footers, validation prose, and gateway failure prose. |

There is no gateway-side coverage for this command-local mapping: input handling and renderer behavior live in `apps/cli`. Gateway API behavior is mapped in the command contract file when a gateway-side surface exists.
