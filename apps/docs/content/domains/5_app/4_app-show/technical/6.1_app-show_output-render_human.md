# Human Renderer: `orbit app:show [app]`

[Back to `app:show` technical contract.](1_app-show.md)

This page describes the human-rendered terminal output for `app:show`.

**Renderer:** Human renderer.

## Primitive

- Detail: [Show detail](../../../../ux/commands/details/show-detail.md)

## Selection

Use this renderer when `--json` is not present.

The human renderer does not own input prompting. Prompt behavior belongs to
[`interactive input mode`](5.1_app-show_input-mode_interactive.md).
Non-interactive validation failures are rendered here as prose errors when
`--json` is absent.

## Progress Tree

`app:show` is a registry read. Execution is expected to complete below one
second and does not perform slow external work or live app-role probes. No
progress tree is rendered.

## Success Output

When the app is found:

```text

┌  App: docs
│
├  Domain        docs.test
│
├  Node          app-1 (1.2.3.4)
│
├  Repository    git@github.com:my/repo.git
│
├  PHP           8.5
│
├  Path          /home/docs/app
│
├  Root          /home/docs/app/public
│
├  Workspaces    main, feature-docs
│
├  Processes     vite
│
└  Routes        docs.test

```

Workspaces, processes, and routes render as comma-separated one-line summaries.
No live status is rendered. Missing values render as `—`.

## Failure Output

Human-rendered failures must show:

- the failing phase or field;
- a clear prose error;
- no JSON envelope;
- next-step guidance only when the operator can act on it.

### Missing input

```text
App name is required.
```

### App not found

```text
App 'docs' not found or not visible.
```

### Not authorized

```text
This caller is not authorized to inspect 'docs'.
```

### Gateway unavailable

```text
Gateway connection is required to show app details.
```

### Local context invalid

```text
Local node role setting is invalid.
```

## Test Mapping

| Path | Coverage |
| --- | --- |
| `apps/cli/tests/Feature/Commands/App/AppShowCommandTest.php` | Human header/domain/node output, no-progress-tree behavior, and failure prose. |

Agent IDE block and full workspaces/processes/routes list rendering remain coverage gaps until focused tests land.

There is no gateway-side coverage for this command-local mapping: input handling and renderer behavior live in `apps/cli`. Gateway API behavior is mapped in the command contract file when a gateway-side surface exists.
