# Human Renderer: `orbit database:add`

[Back to `database:add` technical contract.](1_database-add.md)

## Primitive

None. Renderer prints one concise success or failure line and does not render a
table, prompt, spinner, or progress tree.

## Progress Tree

No progress tree. The command validates input before side effects and then
performs one local registry mutation or typed gateway request that is expected
to complete below one second. If slow external work is added later, add a typed
progress stream before rendering a progress tree.

## Human Strings

- Success: "Database connection '{slug}' created."
- Failure: "Failed to create database connection '{slug}'."

## Test Mapping

| Path | Coverage |
| --- | --- |
| `apps/cli/tests/Feature/Commands/Database/DatabaseWriteCommandsTest.php` | Concise create success line without envelope dump or secret leakage. |

There is no gateway-side coverage for this mapped surface; linked CLI tests cover only the mapped assertions above. Remaining behavior stays a coverage gap until focused tests land.
