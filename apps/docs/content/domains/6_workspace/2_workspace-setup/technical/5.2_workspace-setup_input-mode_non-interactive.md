# Technical Contract: `orbit workspace:setup` (Non-Interactive Input)

This contract defines behavior for **non-interactive input mode** (e.g., CI,
scripts, or when `--json` is present).

[Back to the canonical contract.](1_workspace-setup.md)

**Input mode:** Non-interactive.

Use this input mode when either condition is true:

- the command is running without a TTY (non-tty);
- `--json` is present.

`--json` selects the JSON renderer and forces this input mode (forces
non-interactive) even when the process has a TTY. This input mode never
renders prompts and never waits for user input; there are no fallback
prompts.

## Behavior

- **Validation**: All required inputs must be supplied via flags or derived
  from unambiguous local context. Missing required input causes an immediate
  failure before side effects.
- **No prompts**: The command will never block on user input. Prompts are
  never rendered.
- **Adoption rules**: To adopt an unmanaged path, `[name]`, `--instance`,
  and `--path` must be explicitly supplied (or `[name]` derived from valid
  Codex Git-worktree metadata and `--instance` derivable from local context). The
  path must be absolute on the owning node.
- **Codex metadata applies before gateway lookup**: when `[name]` is missing
  and an explicit `--path` identifies a managed Codex worktree, the local CLI
  resolves the name from its structured Git metadata without shell execution.
  A synced branch supplies the normalized slug; thread metadata falls back to
  `codex-<key>`. Paths without usable Codex metadata may fall back to a valid
  path basename as the workspace slug.
- **CWD path-ownership lookup applies in non-interactive mode**: when
  `[name]` is missing and `--json` or a non-TTY caller is used, the gateway
  path-ownership lookup still runs. If it returns `workspace`, identity
  resolves from the lookup. If it returns `instance_root`, the command fails
  before side effects with `error.code=workspace.path_is_instance_root` and the
  `next_command` hint pointing at `workspace:new`. If it returns
  `inside_instance` or `unregistered`, local Codex Git-worktree metadata or a valid
  path-basename may resolve identity. When `[name]` is still missing after
  those lookups, the command fails with `error.code=validation_failed`
  because no prompt is available to recover the missing `[name]`.

## Failure Semantics

- Missing required input fails before side effects with
  `error.code=validation_failed`.
- `error.meta.field` names the missing input.
- `error.meta.reason` may describe the missing-input rule that failed.
- Non-absolute `--path` fails before side effects with
  `error.code=validation_failed`, `error.meta.field=path`.

## Destructive Consent

`workspace:setup` has no destructive consent prompt and no destructive-skip
flag. Managed runtime artifacts are gateway-owned and overwritten without
prompting; there is no scenario where non-interactive mode would silently
skip a confirmation that interactive mode would have asked for.

## Test Mapping

| Path | Coverage |
| --- | --- |

There is no gateway-side coverage for this input mode: non-interactive selection and local validation are owned by `apps/cli`. When this file documents destructive consent, routine coverage is CLI-owned; If coverage is missing, track it as later test work. Do not map it to gateway coverage.
