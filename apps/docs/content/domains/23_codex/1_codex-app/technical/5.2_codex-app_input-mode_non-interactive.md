# Non-Interactive Input Mode: `orbit codex:app`

[Back to `codex:app` technical contract.](1_codex-app.md)

**Input mode:** Non-interactive.

## Selection

Use this input mode when `--json` is present or when the command is running
without a TTY. `--json` always forces non-interactive input mode.

Non-interactive input mode must never render prompts.

## Responsibilities

Prompts are never rendered in non-interactive mode; prompts are never rendered.

- `action` is required.
- `--node` is required.
- A dotted `app.instance` selector is required for `add` and `remove`; a bare
  logical app fails with `error.meta.reason=app_instance_required`.
- An external-driver instance fails with `error.meta.reason=unsupported`
  before source placement, authorization, or target-node effects.
- `app` is rejected for `list`.
- Never render prompts.
- No values are prompted or inferred from `node:default`.

Validation failures happen before gateway side effects.

## Test Mapping

No gateway-side coverage exists for this file because non-interactive input
mode is resolved in the CLI before gateway requests are sent.

| Path | Coverage |
| --- | --- |
| `apps/cli/tests/Feature/Commands/Codex/CodexAppCommandTest.php` | Non-interactive required input failures, JSON input mode, and gateway forwarding payloads. |
