candidate=bd83f57319891a274c6da3d5b2892a7a725c2b0a
bin/orbit-feature-proof-receipt

Rebased `fix-worktree-dependency-bootstrap` onto exact current main `20b35a21a25aa40274102b3a84c8fa0f3cb91ba8` (clean, no conflict). Corrected both blocking review findings on that tip.

Changes:
- `bin/orbit-prepare-worktree-test`: executable fixture where every Composer install creates `vendor/autoload.php` except `apps/gateway`. Preparation exits nonzero, stderr names the exact gateway path, stdout lacks `WORKTREE_PREPARED`, and gateway app setup does not run. A scratch mutant that removes only the in-loop `ensure_composer_autoload` is killed by this case.
- `bin/orbit-prepare-worktree`: mismatch block remains both ways. When `origin/main` is an ancestor of local `main`, stderr says local main is ahead, prints both SHAs, and says it must be pushed. Otherwise it still says local main differs and must be updated. Fixture covers the ahead direction; the script does not fetch, pull, reset, or mutate main.
- Grep polish: negative path assertions use `grep -qF` / `grep -qxF`.
- `PRODUCT_DECISIONS.md`: restored a name-free supersession clause on the 2026-08-22 tmux-flow entry (`Supersedes the 2026-08-22 routing decision landed at 6603011b1.`).
- `HARNESS.md` FRAME step 3: one positive precondition that default `--base=main` requires local `main` to equal `origin/main`. Left off `AGENT_FAST_PATH.md` so the combined authority-byte cap still holds.

Proof:
- `bash -n bin/orbit-prepare-worktree bin/orbit-prepare-worktree-test`
- `bin/orbit-prepare-worktree-test`
- focused `WorkerToolsTest`, `McpConfigurationTest`, `QualityGateArtifactsTest` (116 passed)
- `composer quality-check` on `bd83f57319891a274c6da3d5b2892a7a725c2b0a` (exit 0, dirty=false)
- `bin/orbit-feature-proof-receipt` (`ok: true`, candidate `bd83f57319891a274c6da3d5b2892a7a725c2b0a`, venue automated, runtime not applicable, artifact `.orbit/quality-gates/quality-check-2026-08-23T091807Z-5a7e14b81469.json`)
