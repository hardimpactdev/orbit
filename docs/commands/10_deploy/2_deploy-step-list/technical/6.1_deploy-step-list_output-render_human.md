# Human Renderer: `orbit deploy:step-list`

[Back to `deploy:step-list` technical contract.](1_deploy-step-list.md)

**Renderer:** Human terminal renderer.

## Primitive

- Output table: [`Laravel\Prompts\table`](../../../ux/lists/table.md)

## Selection

Use this renderer when `--json` is not present.

## Progress Tree

`deploy:step-list` is a read-only gateway registry query. Execution is expected
to complete below one second and does not perform slow external work. No
progress tree is rendered.

## Success Output

Render a table with one row per deployment step, excluding the command body so
long or multiline scripts do not distort the table.

| Column | Meaning |
| --- | --- |
| ID | Gateway-assigned step id. |
| ORDER | Step order. |
| TITLE | Display label. |
| TIMEOUT | Step timeout in seconds. |

After the table, render a `Commands:` section. Each step command is rendered as
an indented block under `[order] title`, preserving line breaks exactly as
stored in the deployment policy.

Empty results render a concise empty state for the selected production app.

## Failure Output

Validation, gateway, authorization, app-not-found, and production-app
eligibility failures render one concise error line with the rejected field or
app selector when available.

## Test Mapping

| Path | Coverage |
| --- | --- |
| `tests/Feature/Commands/Deploy/DeployStepListHumanRendererTest.php` | Human renderer selection, ordered table output, empty state output, retention rendering, and human failure messages. |
