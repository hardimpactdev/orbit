# JSON Renderer: `orbit database:detach`

[Back to `database:detach` technical contract.](1_database-detach.md)

## Primitive

None. JSON output is emitted as a machine-readable envelope via `$this->line(...)`.

## Envelope

This renderer uses the shared [JSON Envelope](../../../README.md#json-envelope).

## Success Shape

```json
{
  "success": {
    "data": {
      "result": {
        "action": "detached",
        "connection": "acme",
        "target_type": "app_instance",
        "target": "acme.development",
        "env_prefix": "DB"
      }
    },
    "meta": []
  }
}
```

## Error Shape

The top-level envelope is either `success` or `error`.

## Test Mapping

| Path | Coverage |
| --- | --- |
| `apps/cli/tests/Feature/Commands/Database/DatabaseWriteCommandsTest.php` | Detach success envelope with `success.data.result` shape. |

There is no gateway-side coverage for this mapped surface; linked CLI tests cover only the mapped assertions above. Remaining behavior stays a coverage gap until focused tests land.

Mapping-not-found and exhaustive documented detach error codes remain coverage gaps until focused tests land.
