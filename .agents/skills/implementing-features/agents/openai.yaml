interface:
  display_name: "Implementing Features"
  short_description: "Own an Orbit feature from frame through land."
  default_prompt: "Own FRAME through LAND. For each dependency-ready or reopened slice, dispatch one fresh native Luna child using model gpt-5.6-luna and reasoning_effort=low; the same child handles corrections and amends its checkpoint. Sol audits all slices, runs FEATURE PROVE and records the feature proof receipt, then dispatches exactly one Claude general reviewer through existing tmux tooling. Complete the diff-derived proof venue, run deterministic checks, involve the user only for remaining human judgment, and preserve unrelated sessions and files."
