interface:
  display_name: "Loop Observer"
  short_description: "Observe one loop only on explicit request."
  default_prompt: "Use this read-only skill only because the human explicitly requested live observation. Sample the named loop, report concrete friction with evidence, and do not steer, edit, dispatch, or create routine ceremony."
