# JSON Renderer: `orbit tool:start`

[Back to `tool-start` technical contract.](1_tool-start.md)

**Renderer:** JSON renderer.

## Primitive

None. JSON output is emitted as a single machine-readable envelope.

## Selection

Use this renderer when `--json` is present.

Use `--stream-json` instead of `--json` only when callers need
newline-delimited progress frames. That renderer follows the shared
[Stream JSON Frames](../../../README.md#stream-json-frames) contract.

## Envelope

This renderer uses the shared [JSON Envelope](../../../README.md#json-envelope).

## Success Shape

```json
{
  "success": {
    "data": {
      "tool": {
        "name": "orbstack",
        "node": "mac-1",
        "action": "start"
      }
    },
    "meta": []
  }
}
```

## Error Shape

```json
{
  "error": {
    "code": "tool.unsupported_action",
    "message": "Tool 'composer' does not support start.",
    "meta": {
      "tool": "composer",
      "action": "start"
    }
  }
}
```

| Field | Type | Meaning |
| --- | --- | --- |
| `error.code` | `validation_failed`, `gateway_unavailable`, `authorization_failed`, `node.agent_unreachable`, `tool.not_found`, `tool.unsupported_action`, `tool.unsupported_on_node`, `tool.runtime_missing`, `tool.runtime_ambiguous`, `tool.remote_action_failed` | Stable command failure code. |
| `error.message` | string | Human-readable failure summary. |
| `error.meta` | object | Field, target, caller, runtime, or remote-action context for automation. |

`node.agent_unreachable` includes `error.meta.reason=agent_push_unavailable`.

## Test Mapping

| Path | Coverage |
| --- | --- |
| `apps/cli/tests/Feature/Commands/Tool/ToolWriteCommandTest.php` | Top-level `success` and `error` JSON envelope rendering for lifecycle commands, including `validation_failed`, `gateway_unavailable`, `authorization_failed`, and gateway error pass-through. |
| `apps/gateway/tests/Feature/Http/Api/ToolLifecycleControllerTest.php` | Gateway JSON success and handled `tool.not_found`, `tool.unsupported_action`, `tool.unsupported_on_node`, and `tool.remote_action_failed` error codes. |

Documented `error.code` values named for coverage tracking: `validation_failed`, `gateway_unavailable`, `authorization_failed`, `node.agent_unreachable`, `tool.not_found`, `tool.unsupported_action`, `tool.unsupported_on_node`, `tool.runtime_missing`, `tool.runtime_ambiguous`, `tool.remote_action_failed`. Linked tests cover only the mapped assertions above; remaining codes stay as coverage gaps until focused tests land.
