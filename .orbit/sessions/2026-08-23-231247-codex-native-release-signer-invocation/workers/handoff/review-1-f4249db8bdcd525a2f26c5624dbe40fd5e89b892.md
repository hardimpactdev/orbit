candidate=f4249db8bdcd525a2f26c5624dbe40fd5e89b892

CHECKOUT_PROOF: cwd=/Users/nckrtl/orbit/.worktrees/codex-native-release-signer-invocation; branch=codex/native-release-signer-invocation; head=f4249db8bdcd525a2f26c5624dbe40fd5e89b892; main=17be5aa46911f7d33037433d401c9abf106ca4fb; status=clean

## Review

Independent general review of candidate `f4249db8bdcd525a2f26c5624dbe40fd5e89b892` against base `17be5aa46911f7d33037433d401c9abf106ca4fb`. Checkout, branch, candidate, base, and cleanliness match the assignment.

Diff: `bin/orbit-build-desktop-bundle` signer invocation plus one deterministic guard in `NativeReleaseAssetsBuilderTest`.

Confirmed:
- Signed through the locked local `apps/macos` Tauri CLI: `( cd "$macos_dir"; npm run tauri -- signer sign "$archive" )`. This matches the already-working build idiom `npm run tauri -- build` in the same script (line 52) and resolves the `tauri` npm script backed by the `@tauri-apps/cli` devDependency.
- Resolves in a clean npm worktree: `npm ci` runs in `$macos_dir` (line 51) before signing, installing the local `@tauri-apps/cli` devDependency; the `tauri` script exists in `apps/macos/package.json`.
- `$archive` is absolute (`--output` normalized to `${repo_root}/...` at lines 19-22), so the `cd "$macos_dir"` subshell resolves the quoted path correctly.
- Fail-closed and ordering preserved: the signing subshell has `|| fail`, and the `[ -s "${archive}.sig" ]` non-empty signature check (line 66) still follows the signing step.
- Quoting correct: `"$archive"` and `"$macos_dir"` are double-quoted; subshell isolates the `cd`.
- Deterministic guard fails on the base broken npx invocation: the test asserts the source does not contain `npx --yes --prefix` or `@tauri-apps/cli signer sign`, and requires `npm run tauri -- signer sign "$archive"` before the single `[ -s "${archive}.sig" ]` occurrence — all of which fail against the base `npx --yes --prefix ... @tauri-apps/cli signer sign` source.
- No dependencies changed (`@tauri-apps/cli` devDependency already present); no updater keys, secrets, artifacts, or product behavior changed; no `test:e2e` lane run.

Bounded repo-wide search (`rg 'npx --yes --prefix|@tauri-apps/cli signer|signer sign'`) found the old pattern only in the new test's negative assertions; the caller chain `bin/orbit-build-native-release-assets` is intact.

Findings: none (no DEFECT, no POLISH).

Proof: SHA-bound `bin/orbit-feature-proof-receipt` ok=true; focused NativeReleaseAssetsBuilderTest passed; `composer quality-check` artifact `.orbit/quality-gates/quality-check-2026-08-23T210739Z-5c9bd895525c.json`; supporting Mini probe `LOCAL_TAURI_SIGNER_PROBE=PASS`. Runtime `not applicable` is honestly classified for the automated repository-tooling venue (full desktop signing belongs to the release/E2E boundary, out of scope here).

BLAST_RADIUS: not-required
HUMAN_JUDGMENT: not-required
VERDICT: PASS
