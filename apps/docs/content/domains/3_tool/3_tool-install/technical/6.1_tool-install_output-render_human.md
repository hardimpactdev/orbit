# Human Renderer: `orbit tool:install`

[Back to `tool-install` technical contract.](1_tool-install.md)

**Renderer:** Human renderer.

## Primitive

- Progress: [Progress tree](../../../../ux/commands/progress/progress-tree.md)

## Selection

Use this renderer when `--json` is not present.

## Progress Tree

Render an in-progress tree after input resolution and before gateway reads, node inspection, or node apply begins.

```text
┌ Installing Tool
○ Resolve target
○ Read gateway tool configuration
○ Run command action
└ Working...
```

## Success Output

Human output presents the selected tool state using Orbit product vocabulary: tool, node, expected capability state, observed state when available, managed flag, and version/config metadata when tracked by the tool definition.

## Failure Output

Human-rendered failures show the failing field or condition, a concise prose error, and actionable recovery guidance when the operator can resolve the issue.

Missing target source failures tell the operator to provide `--node`, provide
`--instance`, configure `node:default`, or run interactively to select a target.

## Test Mapping

| Path | Coverage |
| --- | --- |
| `apps/cli/tests/Feature/Commands/Tool/ToolWriteCommandTest.php` | Human renderer smoke for the interactive `tool:install` prompt path and success output. |
| `apps/cli/tests/Feature/Commands/StreamsGatewayProgressTest.php` | Shared gateway streaming progress and error rendering used by install stream output. |

There is no gateway-side coverage for this renderer: install human rendering is owned by `apps/cli`.
