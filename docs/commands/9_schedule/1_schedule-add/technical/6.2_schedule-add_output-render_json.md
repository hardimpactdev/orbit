# JSON Renderer: `orbit schedule:add`

[Back to `schedule:add` technical contract.](1_schedule-add.md)

**Renderer:** JSON renderer.

## Selection

Use this renderer when `--json` is present.

## Envelope

This renderer uses the shared [JSON Envelope](../../../README.md#json-envelope).

## Success Shape

```json
{
  "success": {
    "data": {
      "result": {
        "action": "created",
        "scheduler_pickup": "confirmed"
      },
      "schedule": {
        "name": "laravel-scheduler",
        "scope": "app",
        "target": {
          "type": "app",
          "name": "docs",
          "node": "app-1"
        },
        "interval": "every minute",
        "timezone": "Europe/Amsterdam",
        "execution": {
          "type": "command",
          "value": "php artisan schedule:run"
        },
        "enabled": true,
        "status": "expected",
        "last_run": null
      }
    }
  }
}
```

## Error Shape

```json
{
  "error": {
    "code": "schedule.scheduler_unreachable",
    "message": "Schedule 'laravel-scheduler' was recorded, but the Orbit Scheduler on node 'app-1' could not be confirmed reachable.",
    "data": {
      "schedule": {
        "name": "laravel-scheduler",
        "scope": "app",
        "target": {
          "type": "app",
          "name": "docs",
          "node": "app-1"
        },
        "interval": "every minute",
        "timezone": "Europe/Amsterdam",
        "execution": {
          "type": "command",
          "value": "php artisan schedule:run"
        },
        "enabled": true,
        "status": "scheduler_unreachable",
        "last_run": null
      }
    },
    "meta": {
      "node": "app-1",
      "next_command": "doctor --family=schedule"
    }
  }
}
```

| Field | Type | Meaning |
| --- | --- | --- |
| `success.data.result.action` | string | `created`. |
| `success.data.result.scheduler_pickup` | string | Scheduler pickup result, usually `confirmed`. |
| `success.data.schedule` | object | Schedule entity defined in [`9_schedule/README.md`](../../README.md#schedule-json-entity). |
| `error.code` | string | Stable command failure code. |
| `error.data.schedule` | object | Present when gateway intent exists after a scheduler-unreachable failure. |
| `error.meta.next_command` | string | Recovery command for scheduler-unreachable failures. |

## Test Mapping

| Path | Coverage |
| --- | --- |
| `tests/Feature/Commands/Schedule/ScheduleAddJsonRendererTest.php` | JSON renderer selection, success entity shape, scheduler-unreachable error data, recovery metadata, and every `error.code` value documented in this renderer. |
