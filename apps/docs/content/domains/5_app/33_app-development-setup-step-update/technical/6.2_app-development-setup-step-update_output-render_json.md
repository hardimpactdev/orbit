# JSON output: `app-development-setup-step:update`

## Envelope

This renderer uses the shared [JSON Envelope](../../../README.md#json-envelope).

## Primitive

None. JSON output is emitted as one machine-readable envelope.

## Test Mapping

| Path | Coverage |
| --- | --- |
| `apps/cli/tests/Feature/Commands/App/AppDevelopmentSetupStepCommandTest.php` | JSON update envelope and validation errors. |
| `apps/gateway/tests/Feature/Http/Api/AppDevelopmentSetupStepControllerTest.php` | Gateway update success and error shapes. |

Success returns `success.data.step`; failure returns the shared `error` envelope.
