# Non-Interactive Input Mode: `orbit node:update [name]`

[Back to `node:update` technical contract.](1_node-update.md)

This page describes how non-interactive input mode resolves `node:update` input
without prompts.

**Input mode:** Non-interactive.

## Selection

Use this input mode when either condition is true:

- the command is running without a TTY;
- `--json` is present.

`--json` selects the JSON renderer and forces this input mode even when the
process has a TTY.

Unconfigured operator callers are rejected before this input mode reads command
input beyond what is needed to identify the execution-context violation.
App-role callers are gateway clients; when locally configured, they resolve
input like other gateway clients and render the gateway's caller-role rejection.

## Responsibilities

- Read supplied arguments and options.
- Apply documented defaults.
- Never render prompts.
- Fail before side effects when required input is absent.
- Fail before side effects when no supported field flags are provided.
- Fail before side effects when supplied input is forbidden or invalid.
- Evaluate path eligibility as soon as the fields needed for the rule are known.

## Required Input Failures

| Field | Missing when | Behavior |
| --- | --- | --- |
| `name` | `[name]` is absent. | Fail before side effects. |
| any field | No supported field flags are provided. | Fail before side effects. |

## Validation Failures

Fail before side effects when:

- `name` does not match an existing active node record;
- `--environment` is supplied for a non-app node;
- `--tld` is supplied for a gateway target, control target, or app target whose
  effective environment is `production`;
- `--tld` does not match the single lowercase DNS label syntax;
- `--tld` already belongs to another active node;
- `--host`, `--public-ipv4`, or `--public-ipv6` is supplied for a control
  target node;
- the same field flag is supplied more than once in a single invocation.

The selected output renderer owns how these failures are displayed.

## Test Mapping

Primary test owners:

| Path | Coverage |
| --- | --- |
| `tests/Feature/Commands/Nodes/NodeUpdateCommandTest.php` | Non-interactive missing-field failure and field validation. |

Required coverage:

| Path | Coverage |
| --- | --- |
| `tests/Feature/Commands/Nodes/NodeUpdateNonInteractiveInputModeTest.php` | No-prompt selection without a TTY, `--json` forcing non-interactive mode, missing `name` failure, missing field flags failure, TLD role/effective-environment rejection, development-app TLD success, production-to-development plus TLD success, duplicate-TLD conflict, and invalid value failures. |
