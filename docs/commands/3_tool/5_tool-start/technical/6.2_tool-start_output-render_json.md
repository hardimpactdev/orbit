# JSON Renderer: `orbit tool-start`

[Back to `tool-start` technical contract.](1_tool-start.md)

**Renderer:** JSON renderer.

## Primitive

None. JSON output is emitted as a machine-readable envelope via `$this->line(...)`.

## Selection

Use this renderer when `--json` is present.

## Envelope

This renderer uses the shared [JSON Envelope](../../../README.md#json-envelope).

## Success Shape

```json
{
  "success": {
    "data": {
      "tool": []
    },
    "meta": {}
  }
}
```

Command-specific result data uses the canonical tool entity from [`README.md`](../../README.md#tool-json-entity) unless the command returns logs or credentials.

## Error Shape

```json
{
  "error": {
    "code": "validation_failed",
    "message": "Required input is missing or invalid.",
    "meta": {
      "field": "tool"
    }
  }
}
```

| Field | Type | Meaning |
| --- | --- | --- |
| `error.code` | `validation_failed`, `gateway_unavailable`, `authorization_failed`, `tool.not_found`, `tool.unsupported_action`, `tool.remote_action_failed` | Stable command failure code. |
| `error.message` | string | Human-readable failure summary. |
| `error.meta` | object | Field, target, caller, or remote-action context for automation. |

`tool.remote_action_failed` preserves node-side apply diagnostics:

```json
{
  "error": {
    "code": "tool.remote_action_failed",
    "message": "Tool 'caddy' start failed on node 'app-1'.",
    "meta": {
      "tool": "caddy",
      "node": "app-1",
      "action": "start",
      "exit_code": 7,
      "stderr": "systemctl failed"
    }
  }
}
```

### Missing target

In non-interactive mode, including `--json`, missing target resolution fails
without prompting:

```json
{
  "error": {
    "code": "validation_failed",
    "message": "A node or app target is required. Provide --node, --app, configure node:default, or select a target interactively.",
    "meta": {
      "fields": [
        "target"
      ]
    }
  }
}
```

### App/node mismatch

When `--app` resolves to a different node than `--node`, the command fails
before side effects:

```json
{
  "error": {
    "code": "validation_failed",
    "message": "Invalid value for --app: 'docs'. App is not owned by the selected node.",
    "meta": {
      "field": "app",
      "value": "docs",
      "node": "app-2",
      "resolved_node": "app-1",
      "reason": "target_mismatch"
    }
  }
}
```

## Test Mapping

| Path | Coverage |
| --- | --- |
| `tests/Feature/Commands/Tools/ToolStartJsonRendererTest.php` | JSON renderer selection, success envelope and canonical entity shape, metadata placement, validation failures, gateway error preservation, remote-action diagnostics, and `--json` non-interactive behavior. |
