# Technical Contract: `orbit app:register` (Non-Interactive Input)

This contract defines behavior for **non-interactive input mode** (e.g., CI, scripts, or when `--json` is present).

[Back to the canonical contract.](1_app-register.md)

**Input mode:** Non-interactive.

Use this input mode when either condition is true:

- the command is running without a TTY;
- `--json` is present.

`--json` selects the JSON renderer and forces this input mode even when the
process has a TTY.

## Behavior

- **Validation**: All required inputs must be supplied via arguments or options. Missing required input causes an immediate failure.
- **No Prompts**: The command will never wait for user input.
- **Selection Rules**: Existing placement requires a dotted instance selector,
  except that a bare logical slug may resolve exactly one eligible visible
  instance. Ambiguity fails with
  `error.meta.reason=app_instance_required`.
- **Adoption Rules**: To adopt an unmanaged path, `--path` and `--node` must be
  explicitly provided. A new logical slug creates `development` without
  `--domain` or `production` with it.

## Failure Semantics

- Missing required input fails before side effects with
  `error.code=validation_failed`.
- `error.meta.field` names the missing input.
- `error.meta.reason` may describe the missing-input rule that failed.

## Test Mapping

| Path | Coverage |
| --- | --- |
| `apps/cli/tests/Feature/Commands/App/AppWriteCommandTest.php` | Non-interactive register POST payload and adopted action assertion. |

There is no gateway-side coverage for this command-local mapping: input handling and renderer behavior live in `apps/cli`. Gateway API behavior is mapped in the command contract file when a gateway-side surface exists.
