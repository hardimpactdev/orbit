# JSON Renderer: `orbit database:schema`

[Back to `database:schema` technical contract.](1_database-schema.md)

## Primitive

None. JSON output is emitted as a machine-readable envelope via `$this->line(...)`.

## Envelope

This renderer uses the shared [JSON Envelope](../../../README.md#json-envelope).

## Success Shape

`success.data` contains `connection`, `tables[]`, and for each table nested
`columns[]`, `indexes[]`, and nullable metadata fields reported by the driver.

## Error Shape

The top-level envelope is either `success` or `error`.

## Test Mapping

| Path | Coverage |
| --- | --- |
| `tests/Feature/Commands/Database/DatabaseSchemaJsonRendererTest.php` | Schema success envelope and documented error-envelope coverage. |
