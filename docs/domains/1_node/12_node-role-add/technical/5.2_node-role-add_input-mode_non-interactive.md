# Non-Interactive Input: `orbit node role:add`

[Back to `node role:add` technical contract.](1_node-role-add.md)

**Input mode:** Non-interactive.

Applies without a TTY or when `--json` is present.

`--json` forces non-interactive input mode.

## Responsibilities

- Read supplied arguments and options.
- Apply documented defaults.
- Never render prompts.
- Fail before side effects when required input is absent or invalid.
- Fail before side effects when `--s3-data-path` is supplied for a role other
  than `s3`, or when the supplied S3 data path is not absolute.

## Test Mapping

| Path | Coverage |
| --- | --- |
| `apps/gateway/tests/Feature/Commands/Nodes/NodeRoleAddCommandTest.php` | Required-argument and role-option validation in JSON mode. |
