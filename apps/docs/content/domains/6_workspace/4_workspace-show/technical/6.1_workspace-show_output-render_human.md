# Human Renderer: `orbit workspace:show [name]`

[Back to `workspace:show` technical contract.](1_workspace-show.md)

This page describes the human-rendered terminal output for `workspace:show`.

**Renderer:** Human renderer.

## Primitive

- Detail: [Show detail](../../../../ux/commands/details/show-detail.md)

## Selection

Use this renderer when `--json` is not present.

The human renderer does not own input prompting. Prompt behavior belongs to
[`interactive input mode`](5.1_workspace-show_input-mode_interactive.md).
Non-interactive validation failures are rendered here as prose errors when
`--json` is absent.

## Progress Tree

`workspace:show` is a registry read. Execution is expected to complete below
one second and does not perform slow external work or live node probes.
No progress tree is rendered.

## Success Output

When the workspace is found:

```text

┌  Workspace: feature-docs.docs
│
├  URL         https://feature-docs.docs.test
│
├  Node        app-1 (1.2.3.4)
│
├  Path        /home/orbit/apps/docs/.worktrees/feature-docs
│
│
├  PHP         8.5
│
└  Processes   vite

```

The title combines the workspace slug and the parent project slug as
`{workspace}.{app}`. Labels align to a single column.

Inherited processes render as a comma-separated one-line summary. The PHP line
shows the bare effective value without inheritance annotations.

The human overview intentionally omits machine-oriented entity fields
(`adopted`, `lifecycle_status`) and renderer-internal facts such as runtime
container names. The JSON renderer carries the richer canonical entity for
machine callers.

## Failure Output

Human-rendered failures must show:

- the failing phase or field;
- a clear prose error;
- no JSON envelope;
- next-step guidance only when the operator can act on it.

### Missing input

```text
Workspace name is required.
```

### Workspace not found

```text
Error: Workspace 'feature-docs' not found or not visible.
Check 'orbit workspace:list' for available workspaces.
```

### Ambiguous workspace

```text
Error: Workspace name 'feature-docs' is ambiguous.
Found in app: docs, api.
Specify the parent project with --instance=<app.instance>.
```

### Not authorized

```text
This caller is not authorized to inspect 'feature-docs'.
```

### Gateway unavailable

```text
Gateway connection is required to show workspace details.
```

## Test Mapping

Primary test owners:

| Path | Coverage |
| --- | --- |
| `apps/cli/tests/Feature/Commands/Workspace/WorkspaceShowCommandTest.php` | CLI show request forwarding, human detail layout, JSON payload passthrough, and not-found error handling. |

There is no gateway-side coverage for this renderer: human output rendering is owned by `apps/cli`. When this file documents destructive consent or warning payload prose, current routine coverage is limited to the linked CLI rows above.
