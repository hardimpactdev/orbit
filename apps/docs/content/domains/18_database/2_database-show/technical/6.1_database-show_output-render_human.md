# Human Renderer: `orbit database:show`

[Back to `database:show` technical contract.](1_database-show.md)

## Primitive

- Detail: [Show detail](../../../../ux/commands/details/show-detail.md)

## Progress Tree

No progress tree. The command is expected to complete from gateway reads only.

## Success Output

```text

┌  Database connection: ditis-hr
│
├  Driver   pgsql
│
├  Host     127.0.0.1
│
├  Port     5434
│
├  Name     ditis_hr
│
├  User     orbit
│
├  Targets  ditis-hr.development
│
└  Node     beast

```

The detail view renders `Path` for SQLite/file-backed connections. Missing
values render as `—`. `Targets` lists dotted app-instance identities and
workspace names together as comma-separated values.

## Failure Output

- Failure: "Database connection '{slug}' was not found."

## Test Mapping

| Path | Coverage |
| --- | --- |
| `apps/cli/tests/Feature/Commands/Database/DatabaseReadCommandsTest.php` | Show-detail tree labels, sqlite Path rendering, em-dash missing values, and target display. |

There is no gateway-side coverage for this mapped surface; linked CLI tests cover only the mapped assertions above. Remaining behavior stays a coverage gap until focused tests land.

There is no current routine CLI test for the human not-found failure string; keep that assertion as a coverage gap until a focused test lands.
