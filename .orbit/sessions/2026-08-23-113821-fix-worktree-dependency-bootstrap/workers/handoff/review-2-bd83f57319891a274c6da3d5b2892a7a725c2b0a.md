# review-2 corrected-tip review: worktree dependency bootstrap

candidate=bd83f57319891a274c6da3d5b2892a7a725c2b0a

CHECKOUT_PROOF: cwd=/Users/nckrtl/orbit/.worktrees/fix-worktree-dependency-bootstrap; branch=fix-worktree-dependency-bootstrap; head=bd83f57319891a274c6da3d5b2892a7a725c2b0a; main=20b35a21a25aa40274102b3a84c8fa0f3cb91ba8; status=clean

Base `20b35a21a25aa40274102b3a84c8fa0f3cb91ba8` is an ancestor of HEAD. Resumed the review-1 context; persona `.agents/review-personas/general.md`. Treated `.orbit/quality-gates/quality-check-2026-08-23T091807Z-5a7e14b81469.json` and the exact-SHA receipt as authoritative; focused suites and `composer quality-check` were not rerun.

## Prior Finding Closure

**DEFECT 1 (per-app autoload guard unproven) — CLOSED.** `bin/orbit-prepare-worktree-test:252-291` adds `feature-missing-gateway-autoload`: the stub creates `vendor/autoload.php` for every install except `*/apps/gateway`, and the fixture commits real `apps/gateway/artisan` and `.env.example` markers first so `prepare_gateway_app` is genuinely reachable. It asserts nonzero exit, the exact gateway project path in stderr, absence of `WORKTREE_PREPARED`, absence of `Preparing gateway app state`, and absence of the gateway state directory. Mutation check: removing only `bin/orbit-prepare-worktree:332` (the in-loop `ensure_composer_autoload`) now fails with `FAIL: prepare-worktree must fail when a tracked app Composer install does not produce vendor/autoload.php`. The prior mutant no longer survives.

**DEFECT 2 (local-main-ahead misdirection) — CLOSED.** `bin/orbit-prepare-worktree:76-80` branches on `git merge-base --is-ancestor 'origin/main^{commit}' 'main^{commit}'`. Ahead: "local main (X) is ahead of origin/main (Y); local main must be pushed". Behind or diverged: unchanged "differs … must be updated". Both carry both SHAs; the branch runs inside an `if`, so a non-ancestor or error exit falls through to the fail-closed stale wording rather than tripping `set -e`. `bin/orbit-prepare-worktree-test:293-378` covers both directions and, for each, asserts the opposite wording is absent, the worktree-add log is empty, the worktree path is not created, the mutation log is empty, and local `main` is byte-identical before and after. No fetch, pull, or reset.

**POLISH 1-5 — all closed.** `WORKTREE_PREPARED` absence is now asserted for both autoload cases (lines 249, 285). Negative-path assertions use `grep -qF`/`grep -qxF` (line 80 included). `PRODUCT_DECISIONS.md:41` restores a name-free `Supersedes the 2026-08-22 routing decision landed at 6603011b1.` `HARNESS.md:29` adds the precondition, and it is contract-tested at `McpConfigurationTest:190` rather than left as loose prose. Omitting `AGENT_FAST_PATH.md` is justified: `McpConfigurationTest:730-737` caps the combined authority bytes at 35600, and HARNESS (19129) + AGENT_FAST_PATH (3996) + the skill (6670, cap 6720) already consume 29795.

## Rebase Integrity

The feature diff against the new base is the intended eight files only. `HARNESS.md` carries exactly two hunks (the workers sentence and the FRAME step 3 precondition) on top of main's restructured text; `McpConfigurationTest` retains main's newer assertions (`focused Mago`, `predicate, identity, vocabulary, or schema`) while removing only the retired-name lines. No main behavior was reverted by the rebase.

## DEFECT

### DEFECT 3 — the root-project arm of invariant 4 is still unproven

- Severity: blocking (missing proof for a named dangerous invariant; the goal names "the root plus every tracked app and package").
- Evidence: `bin/orbit-prepare-worktree-test:7` and `:245`; guard at `bin/orbit-prepare-worktree:326`.
- Mutation check: removing only `ensure_composer_autoload "$worktree_path"` (the root arm) leaves the suite green — `orbit-prepare-worktree tests passed`, exit 0. Production behavior is correct; the proof is not.
- Two compounding causes, both verified:
  1. `:7` runs `TMP="$(cd "$TMP" && pwd)"` without `-P`. On this machine `mktemp -d` yields `/var/folders/…` and the script prints the physical `/private/var/folders/…`, so `$REPO` is never the string the script emits. Probe: `pwd` → `/var/folders/1j/…/probe`, `pwd -P` → `/private/var/folders/1j/…/probe`. Every `grep -qF "$REPO…"` assertion in the harness passes only because `/private/var/…` happens to contain `/var/…` as a substring.
  2. `:245` is a substring match on the worktree prefix, so a failure in a *different* project under the same branch satisfies it. In the root-guard mutant run the recorded stderr was `orbit-prepare-worktree: Composer install did not produce vendor/autoload.php in …/feature-missing-autoload/packages/core` — the in-loop guard firing on `packages/core` — and the assertion meant to pin the root project accepted it.
- Impact: the root `vendor/autoload.php` postcondition can be deleted with the suite green, which reinstates exactly the class of bug this feature exists to prevent. The `pwd` issue additionally weakens every path assertion in the harness, including the new gateway one, which is sound today only because the stub can fail nowhere else.
- Smallest correction, validated end to end: change `:7` to `TMP="$(cd "$TMP" && pwd -P)"` and make `:245` a whole-line match, `grep -qxF "orbit-prepare-worktree: Composer install did not produce vendor/autoload.php in $REPO/.worktrees/${MISSING_AUTOLOAD_BRANCH}" …`. With both applied, the pristine script prints `orbit-prepare-worktree tests passed` and the root-guard mutant fails with `FAIL: missing autoload error must print the exact project path`.

## POLISH

1. After the `pwd -P` fix, tighten `:281` (`grep -qF "$GATEWAY_APP_PATH"`) to the same whole-line `grep -qxF` form so the gateway case pins its project by identity rather than by containment.
2. The negative assertions written as `grep -qF … \` + `&& fail …` (lines 249-250, 285-288, 327-328, 367-368) are correct today only through `set -e`'s exemption for a non-final command in an `&&` list. `if grep -qF …; then fail …; fi` states the intent without depending on that rule.
3. `HARNESS.md:29` is a 101-character line inside an otherwise ~76-column wrapped list item, leaving the orphaned 42-character line 30 (`Fill Goal, Scope, branch, worktree, and`). Re-wrap the item.

## Blast Radius Evidence

The diff still edits `PRODUCT_DECISIONS.md` and shared feature-flow authority, so `complete` applies. Bounded repository-wide check re-run on the corrected tip: `rg -n --hidden -l 'Solo'` excluding `.git`, `.orbit/**`, `docs/superpowers/**`, `**/archives/**`, and `harness-signals/**`.

Result: no active feature-flow authority file retains the retired name — `HARNESS.md`, `AGENT_FAST_PATH.md`, `AGENTS.md`/`CLAUDE.md`, `.agents/skills/implementing-features/**`, `.agents/skills/handling-feature-requests/**`, and `docs/orbit-feature-development-graph.html` produce no hit. Every remaining hit is the separate Solo product extension (`apps/docs/content/domains/23_solo/**`, `apps/docs/content/command-catalog.md` and the other generated docs indexes, `apps/docs/config/librarian.php`, `apps/cli` and `apps/gateway` Solo surfaces, `packages/core`) or older dated ledger lines about that product (`PRODUCT_DECISIONS.md:65,163,191`) — all named out of scope. `.agents/skills/solo-todo-handoff` remains absent after its guard test was removed. No affected surface is unresolved.

## Notes

- Acceptance routing unchanged: venue `automated`, runtime `not applicable`, matching the receipt.
- All eight real tracked projects in this worktree contain `vendor/autoload.php`, so the fail-closed postcondition still introduces no false rejection.

BLAST_RADIUS: complete
HUMAN_JUDGMENT: not-required
VERDICT: FIX
