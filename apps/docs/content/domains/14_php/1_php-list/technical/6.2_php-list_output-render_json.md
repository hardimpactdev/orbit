# JSON Renderer: `orbit php:list`

[Back to `php:list` technical contract.](1_php-list.md)

**Renderer:** JSON renderer.

## Primitive

None. JSON output is emitted as a machine-readable envelope via `$this->line(...)`.

## Selection

Use this renderer when `--json` is present.

## Envelope

This renderer uses the shared [JSON Envelope](../../../README.md#json-envelope).

## Success Shape

```json
{
  "success": {
    "data": {
      "php": {
        "node": "app-1",
        "supported": ["8.5", "8.4", "8.3"],
        "available_images": ["8.5"],
        "app": {
          "name": "docs",
          "php_version": "8.5"
        },
        "workspace": {
          "name": "feature-docs",
          "php_version": "8.4",
          "inherits": false
        }
      }
    },
    "meta": {}
  }
}
```

When `--live` is supplied, `success.meta.live=true`; otherwise `success.meta`
is empty.

## Error Shape

```json
{
  "error": {
    "code": "validation_failed",
    "message": "Required input is missing or invalid.",
    "meta": {
      "field": "node"
    }
  }
}
```

| Field | Type | Meaning |
| --- | --- | --- |
| `success.data.php` | object | PHP runtime view defined by the family README. |
| `success.meta.live` | boolean | Present and `true` only when `--live` supplied live image facts. |
| `error.code` | `validation_failed`, `gateway_unavailable`, `authorization_failed` | Stable command failure code. |
| `error.message` | string | Human-readable failure summary. |
| `error.meta` | object | Field, target, caller, or live-inspection context for automation. |

## Test Mapping

| Path | Coverage |
| --- | --- |
| `apps/cli/tests/Feature/Commands/Php/PhpListCommandTest.php` | JSON envelope, `success.data.php` shape, empty meta placement, and `success.meta.live` with `--live`. |

There is no gateway-side coverage for this renderer: JSON rendering is owned by `apps/cli`. Documented `error.code` values named for coverage tracking: `validation_failed`, `gateway_unavailable`, `authorization_failed`. Linked tests cover only the mapped assertions above; remaining assertions for renderer codes stay as coverage gaps until focused tests land.
