# Non-Interactive Input Mode: `orbit doctor`

[Back to `doctor` technical contract.](1_doctor.md)

**Input mode:** Non-interactive.

Use this mode when the process has no TTY, when `--json` or `--stream-json` is
present, or when `--restore` or `--adopt` is supplied. `--json` and
`--stream-json` always force non-interactive input mode. `--fix` selects
interactive mode and is rejected without a TTY when no other resolution mode
flag is present.

## Required Direction

Bulk resolution in non-interactive mode must select an explicit direction:

```bash
orbit doctor --restore
orbit doctor --adopt
```

`--fix`, `--restore`, and `--adopt` are mutually exclusive. When `--json` or
`--stream-json` is present and a resolution mode is requested, the resolution
mode must be `--restore` or `--adopt`; pairing either machine-readable renderer
with `--fix` is rejected with `error.code=validation_failed` because the
interactive driver cannot prompt in non-interactive mode.

`--json` and `--stream-json` are mutually exclusive. `--json` emits one final
terminal frame; `--stream-json` emits every gateway progress frame as
newline-delimited JSON.

`--dry-run` is valid only with `--restore` or `--adopt`. It returns the same scoped diagnostic plus `actions[]` entries with `status=planned`, but it does not invoke family fixers or adopters and keeps the command effect as `read`.

## Behavior

- Run probes for the resolved scope.
- In `restore` mode on a single target node, select every finding with
  disposition `genuine_drift` (catalog-declared restorable) and apply its
  restore action. Repeat probe+apply under the same
  node/families/key/instance/workspace fence until clean, no-progress, or
  max passes. Emit structured `convergence` metadata (`passes`,
  `stop_reason`, `max_passes`) and disposition counts on the summary.
- In `adopt` mode, select every finding declared adoptable by its family and apply the adopt action once, then re-probe.
- After any real (non-dry-run) restore or adopt mutation, re-probe the same
  selected node/families/key/target scope. Freshly observed issues decide
  terminal health; completed/created/updated action receipts never hide
  remaining findings.
- `--all` is verify-only and rejected with resolution modes.
- In verify mode (no resolution flag), report findings without acting on them.
- When `--key=<issue-key>` is present, filter findings to that exact issue key before selecting restore/adopt actions.
- When `--dry-run` is present, plan supported actions and unsupported skips without applying either and without a post-mutation re-probe or multi-pass loop.
- Skip unsupported findings (non-genuine dispositions for restore) and include them in the final diagnostic with their disposition.
- Fail when no selected finding supports the requested direction.
- Never invent repairs for `invalid_intent`.
- Never render prompts.

## Test Mapping

| Path | Coverage |
| --- | --- |
| `apps/gateway/tests/Feature/Http/Api/DoctorRunControllerTest.php` | Gateway verify/fix endpoint contracts, `--key` filtering, bulk restore/adopt action completion, dry-run action planning, and JSON envelope shapes for resolution-mode results. |
| `apps/cli/tests/Feature/Commands/Operation/DoctorFixCommandTest.php` | CLI `--json --fix` rejection and non-interactive fix-mode validation. |
| `apps/cli/tests/Feature/Commands/Operation/DoctorCommandTest.php` | `--json --stream-json` ambiguity, `--stream-json --fix` rejection, and no gateway I/O before those validation failures. |
