# Human output: `app-development-setup-step:list`

Success prints an ordered table with `ID`, `ORDER`, `COMMAND`, and `TIMEOUT`.
An empty authorized app prints an empty-state message and exits zero.

## Progress Tree

No streamed progress is emitted.

## Primitive

None. The renderer prints one bounded table and does not stream progress.

## Test Mapping

| Path | Coverage |
| --- | --- |
| `apps/cli/tests/Feature/Commands/App/AppDevelopmentSetupStepCommandTest.php` | Ordered human table headings and values. |
| `apps/gateway/tests/Feature/Http/Api/AppDevelopmentSetupStepControllerTest.php` | Ordered step fields supplied to the human table renderer. |
