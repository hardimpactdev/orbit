# JSON Renderer: `orbit database:attach`

[Back to `database:attach` technical contract.](1_database-attach.md)

## Primitive

None. JSON output is emitted as a machine-readable envelope via `$this->line(...)`.

## Envelope

This renderer uses the shared [JSON Envelope](../../../README.md#json-envelope).

## Success Shape

`success.data.connection` uses the database JSON entity defined in
[`../../README.md`](../../README.md#database-json-entity).

## Error Shape

The top-level envelope is either `success` or `error`.

## Test Mapping

| Path | Coverage |
| --- | --- |
| `tests/Feature/Commands/Database/DatabaseAttachJsonRendererTest.php` | `success.data.connection` shape and conflict or validation error envelopes. |
