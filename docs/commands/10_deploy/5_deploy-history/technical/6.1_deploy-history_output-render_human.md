# Human Renderer: `orbit deploy:history`

[Back to `deploy:history` technical contract.](1_deploy-history.md)

**Renderer:** Human terminal renderer.

## Selection

Use this renderer when `--json` is not present.

## Progress Tree

No progress tree is rendered because execution is a gateway-local database read
expected to stay below one second and performs no slow external work.

## Run List

The human renderer outputs a table sorted by `started_at` descending.

```text
Deployment History: docs

ID  Status      Started         Duration  Failed Step
44  failed      2 minutes ago   12s       Run migrations
43  completed   1 hour ago      1m 30s    -
```

Empty history renders a concise empty-state line naming the app.

## Failure Output

Validation and not-found failures render the rejected field or app selector.
Gateway and authorization failures render the selected app when available.
Production-app eligibility failures name the resolved non-production app.

## Test Mapping

| Path | Coverage |
| --- | --- |
| `tests/Feature/Commands/Deploy/DeployHistoryHumanRendererTest.php` | Human renderer selection, table column rendering, newest-first display, empty history output, limit validation prose, and failure prose for every error code in the canonical contract. |
