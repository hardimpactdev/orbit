# JSON Renderer: `orbit proxy:list`

[Back to `proxy:list` technical contract.](1_proxy-list.md)

**Renderer:** JSON renderer.

## Primitive

None. JSON output is emitted as a machine-readable envelope via `$this->line(...)`.

## Selection

Use this renderer when `--json` is present.

## Envelope

This renderer uses the shared [JSON Envelope](../../../README.md#json-envelope).

## Success Shape

```json
{
  "success": {
    "data": {
      "routes": [
        {
          "domain": "docs.test",
          "kind": "app",
          "owner": {
            "type": "app",
            "name": "docs"
          },
          "node": "app-1",
          "target": {
            "type": "instance",
            "value": "docs.development"
          },
          "redirect_code": null,
          "tls": {
            "managed_by": "orbit",
            "trusted_by_gateway_ca": true
          },
          "status": "converged"
        },
        {
          "domain": "happie.nmbp",
          "kind": "app",
          "owner": {
            "type": "app",
            "name": "happie"
          },
          "node": "nmbp",
          "target": {
            "type": "instance",
            "value": "happie.nmbp"
          },
          "redirect_code": null,
          "tls": {
            "managed_by": "orbit",
            "trusted_by_gateway_ca": true
          },
          "status": "unknown"
        },
        {
          "domain": "redirect.docs.test",
          "kind": "redirect",
          "owner": {
            "type": "custom",
            "name": null
          },
          "node": "app-1",
          "target": {
            "type": "redirect",
            "value": "https://docs.test"
          },
          "redirect_code": 302,
          "tls": {
            "managed_by": "orbit",
            "trusted_by_gateway_ca": true
          },
          "status": "intent_only"
        }
      ]
    },
    "meta": {
      "filter": "all",
      "node": null,
      "count": 3
    }
  }
}
```

## Error Shape

```json
{
  "error": {
    "code": "validation_failed",
    "message": "The selected proxy route filter is invalid.",
    "meta": {
      "field": "filter",
      "allowed": ["all", "app", "app-websocket", "app-analytics", "workspace", "gateway", "websocket", "s3", "analytics", "tool", "custom", "redirect"]
    }
  }
}
```

| Field | Type | Meaning |
| --- | --- | --- |
| `success.data.routes[]` | array | Proxy route entities visible under the selected scope. |
| `success.meta.filter` | string | Applied filter. |
| `success.meta.node` | string \| null | Applied serving-node filter. |
| `success.meta.count` | integer | Number of returned routes. |
| `error.code` | `validation_failed`, `gateway_unavailable`, `authorization_failed` | Stable command failure code. |
| `error.meta` | object | Field, node, caller, or authorization context for automation. |

`routes[].status` is persisted enactment evidence, not a live Caddy probe.
Rows without an enactment record return `unknown`; Orbit never upgrades
registry intent to `expected` merely because the database row exists.

## Test Mapping

| Path | Coverage |
| --- | --- |
| `apps/cli/tests/Feature/Commands/Proxy/ProxyListCommandTest.php` | JSON `proxy:list` success envelope, route list shape, filter metadata, empty list shape, and gateway/WireGuard failure passthrough. |


Documented `error.code` values named for coverage tracking: `validation_failed`, `gateway_unavailable`, and `authorization_failed`.

There is no gateway-side coverage for this command-local mapping: input handling and renderer behavior live in `apps/cli`. Gateway API behavior is mapped in the command contract file when a gateway-side surface exists.
