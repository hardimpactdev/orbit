# `process:restart` Interactive Input Mode

[Back to `process:restart` technical contract.](1_process-restart.md)

This page describes how interactive input mode resolves `process:restart` input.

**Input mode:** Interactive.

## Selection

Use this input mode only when the process is attached to a TTY and `--json` is not present.

## Prompt Behavior

Prompt for missing required input in this order:

| Field | Prompt primitive | Notes |
| --- | --- | --- |
| `app` | Datatable | Show concrete app instances the caller is authorized to operate when app-instance context is absent. A supplied ambiguous bare app slug fails with `validation_failed`, `field=app`, and `reason=app_instance_required` instead of prompting. |

`name` is optional. When `[name]` is omitted, interactive mode restarts all process definitions for the resolved node, app-instance, or workspace context; it does not prompt for a process by default.
`--workspace` is an explicit filter. If a supplied workspace name is ambiguous,
the command fails and asks the operator to pass `--app=<app.instance>`.

Prompt validation runs as soon as the field needed for a decision is known.

## Primitive References

- [`datatable`](../../../../ux/commands/lists/data-table-prompt.md)

## Test Mapping

Primary test owners:

| Path | Coverage |
| --- | --- |

There is no gateway-side coverage for this deferred mapping: no current routine gateway test proves this documented behavior. Keep it as a coverage gap until a focused test lands.
