# JSON Renderer: `orbit app-setup-step:remove`

[Back to `app-setup-step:remove` technical contract.](1_app-setup-step-remove.md)

## Primitive

None. JSON output is emitted as a machine-readable envelope.

## Envelope

This renderer uses the shared [JSON Envelope](../../../README.md#json-envelope).

## Success Shape

`success.data.step` contains the removed setup step. `success.meta.action`
contains `removed`.

## Error Shape

The top-level envelope is either `success` or `error`.

Exhaustive command-specific codes:

- `app.not_found`
- `app.setup_step_not_found`
- `validation_failed`
- `authorization_failed`

## Test Mapping

| Path | Coverage |
| --- | --- |
| `apps/cli/tests/Feature/Commands/App/AppSetupCommandTest.php` | JSON-mode forced setup-step removal forwards DELETE destructive consent and succeeds without renderer drift. |
| `apps/gateway/tests/Feature/Http/Api/AppSetupStepControllerTest.php` | Gateway JSON removal entity and action metadata for deleted setup steps. |
