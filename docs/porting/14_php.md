# 14_php — PHP Workstream

Detail file for the PHP command family. Top-level command status lives in
[`PORTING.md`](PORTING.md). Doc authority: `docs/commands/14_php/`.

## Status

Not started. The abstraction seed exists, so this family is ready for the first
bounded implementation slice. Activity logging should be added command-by-command
as each PHP runtime surface is ported.

## Commands

- [ ] `php:list`
- [ ] `php:use`
