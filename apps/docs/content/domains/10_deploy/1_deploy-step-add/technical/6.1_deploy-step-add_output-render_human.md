# Human Renderer: `orbit deploy:step-add`

[Back to `deploy:step-add` technical contract.](1_deploy-step-add.md)

**Renderer:** Human terminal renderer.

## Primitive

None. Renderer prints a concise success or failure line and performs no slow external work.

## Selection

Use this renderer when `--json` is not present.

## Progress Tree

`deploy:step-add` writes one gateway policy row and performs no slow external
work. Execution is expected to complete below one second. No progress tree is
rendered.

## Success Output

Render the app, step id, title, command, order, and retention metadata.

## Failure Output

Validation, gateway, authorization, app-not-found, and
production-app eligibility failures render one concise error line with the
rejected field or instance selector when available.

## Test Mapping

| Path | Coverage |
| --- | --- |
| `apps/cli/tests/Feature/Commands/Deploy/DeployWriteCommandsTest.php` | Human added-step summary output including order, command body, finite retention, and unlimited retention rendering without dumping the JSON envelope. |

There is no gateway-side coverage for this mapped surface; linked CLI tests cover only the mapped assertions above. Remaining behavior stays a coverage gap until focused tests land.

Coverage gaps until focused tests land: human failure messages for validation, authorization, and production-app eligibility errors.
