# `process:stop` Interactive Input Mode

[Back to `process:stop` technical contract.](1_process-stop.md)

This page describes how interactive input mode resolves `process:stop` input.

**Input mode:** Interactive.

## Selection

Use this input mode only when the process is attached to a TTY and `--json` is
not present.

## Prompt Behavior

Prompt for missing required input in this order:

| Field | Prompt primitive | Notes |
| --- | --- | --- |
| `app` | Select | Show apps the caller is authorized to operate when app context is absent or ambiguous. |
| `workspace` | Select | Prompt only when workspace context is ambiguous and the operator chooses a workspace-scoped runtime action. |
| `name` | Select or text | Prefer selecting from processes defined for the resolved app. |

Prompt validation runs as soon as the field needed for a decision is known.

## Test Mapping

Primary test owners:

| Path | Coverage |
| --- | --- |
| `tests/Feature/Commands/Processes/ProcessStopInteractiveInputModeTest.php` | TTY selection without `--json`, app selection, workspace selection, process selection, immediate validation, and cancellation before side effects. |
