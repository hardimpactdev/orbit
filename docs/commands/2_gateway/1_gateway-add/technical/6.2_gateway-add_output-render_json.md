# JSON Renderer: `orbit gateway:add [gateway_ip]`

[Back to `gateway:add` technical contract.](1_gateway-add.md)

This page describes the JSON-rendered output for `gateway:add`.

**Renderer:** JSON renderer.

## Primitive

None. JSON output is emitted as a machine-readable envelope via `$this->line(...)`.

## Selection

Use this renderer when `--json` is present.

`--json` also disables interactive prompts and forces the
[`non-interactive input mode`](5.2_gateway-add_input-mode_non-interactive.md), even
when the process has a TTY.

## Envelope

This renderer uses the shared [JSON Envelope](../../../README.md#json-envelope).

Input mode failures, domain validation failures, authorization failures,
network failures, and local write failures must all use this envelope.

## Enum And Identifier Values

JSON enum values and structured identifiers are part of the renderer contract:

| Field | Values |
| --- | --- |
| `result.action` | `added`, `converged` |
| `gateway.role` | `gateway` |
| `local_node.role` | `control` |
| `gateway.status`, `local_node.status` | `active` |
| `gateway.platform`, `local_node.platform` | Normalized platform-version identifier such as `ubuntu_24-04` or `macos_15-4`; use `unknown` only when the value is structurally required but unavailable from the verified source. |
| `error.meta.caller_role` | `gateway`, `app`, `control` |
| `error.meta.reason` | `missing`, `invalid_ip` |
| `error.code` | `validation_failed`, `caller_role_not_allowed`, `gateway_unavailable`, `node.identity_unknown`, `node.gateway_api_error`, `node.local_config_write_failed` |

The `error.code` values listed here are exhaustive for `gateway:add`. Add a new
value to this renderer contract before implementing another JSON failure code.

Gateway and local-node objects are verified references. They do not imply that
`gateway:add` created or updated node records.

## Success: Added

```json
{
  "success": {
    "data": {
      "result": {
        "action": "added"
      },
      "gateway": {
        "name": "gateway-1",
        "role": "gateway",
        "status": "active",
        "platform": "ubuntu_24-04",
        "addresses": {
          "wireguard": "10.6.0.2"
        }
      },
      "local_node": {
        "name": "control-1",
        "role": "control",
        "status": "active",
        "platform": "macos_15-4",
        "addresses": {
          "wireguard": "10.6.0.8"
        }
      },
      "local_onboarding": {
        "gateway_trust": "trusted",
        "gateway_config": "stored",
        "gateway_api": "verified"
      }
    }
  }
}
```

## Success: Converged

When local gateway trust, settings, and identity are already configured and
verified:

```json
{
  "success": {
    "data": {
      "result": {
        "action": "converged"
      },
      "gateway": {
        "name": "gateway-1",
        "role": "gateway",
        "status": "active",
        "platform": "ubuntu_24-04",
        "addresses": {
          "wireguard": "10.6.0.2"
        }
      },
      "local_node": {
        "name": "control-1",
        "role": "control",
        "status": "active",
        "platform": "macos_15-4",
        "addresses": {
          "wireguard": "10.6.0.8"
        }
      },
      "local_onboarding": {
        "gateway_trust": "already_trusted",
        "gateway_config": "already_stored",
        "gateway_api": "verified"
      }
    }
  }
}
```

## Validation Error: Missing Gateway IP

```json
{
  "error": {
    "code": "validation_failed",
    "message": "Gateway IP is required when it cannot be derived from an active WireGuard network.",
    "meta": {
      "field": "gateway_ip",
      "reason": "missing"
    }
  }
}
```

## Validation Error: Invalid Gateway IP

```json
{
  "error": {
    "code": "validation_failed",
    "message": "Gateway IP must be a valid Orbit WireGuard address.",
    "meta": {
      "field": "gateway_ip",
      "reason": "invalid_ip"
    }
  }
}
```

## Caller Role Error

```json
{
  "error": {
    "code": "caller_role_not_allowed",
    "message": "This command may only be run from a control node.",
    "meta": {
      "caller_role": "gateway"
    }
  }
}
```

## Identity Unknown Error

```json
{
  "error": {
    "code": "node.identity_unknown",
    "message": "This peer is not registered on the gateway at 10.6.0.2. Ask your admin to run `orbit node:new --role=control <name>` on the gateway first, then retry.",
    "meta": {
      "gateway_ip": "10.6.0.2"
    }
  }
}
```

## Gateway Unavailable Error

```json
{
  "error": {
    "code": "gateway_unavailable",
    "message": "Gateway at 10.6.0.2 returned HTTP 500 for /api/me.",
    "meta": {
      "gateway_ip": "10.6.0.2",
      "status": 500
    }
  }
}
```

## Gateway API Error

```json
{
  "error": {
    "code": "node.gateway_api_error",
    "message": "Gateway at 10.6.0.2 returned an invalid identity response.",
    "meta": {
      "gateway_ip": "10.6.0.2",
      "endpoint": "/api/me"
    }
  }
}
```

## Local Config Write Failed Error

```json
{
  "error": {
    "code": "node.local_config_write_failed",
    "message": "Failed to store local gateway configuration.",
    "meta": {
      "gateway_ip": "10.6.0.2"
    }
  }
}
```

## Test Mapping

Primary test owners:

| Path | Coverage |
| --- | --- |
| `tests/Feature/Commands/Gateway/GatewayAddJsonRendererTest.php` | Primary owner for JSON output only: JSON renderer selection, discriminated success/error envelope shape, enum values, `result.action` values, added success payload shape, converged success payload shape, node-shaped verified references, validation error JSON shape, caller role error JSON shape, identity unknown error JSON shape, gateway unavailable error JSON shape, local config write failed error JSON shape, and every `error.code` value. Non-interactive input tests own `--json` forcing non-interactive input mode; input, role, and domain tests own the underlying behavior being serialized. |
