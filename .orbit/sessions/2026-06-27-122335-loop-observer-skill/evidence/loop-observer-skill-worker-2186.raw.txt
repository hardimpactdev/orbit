Verifying the worktree, then reading harness docs and skill-creator references before scaffolding `loop-observer`.
Writing the operational `SKILL.md` and running validation.
## Report

### Files changed

| Path | Action |
| --- | --- |
| `.agents/skills/loop-observer/SKILL.md` | Created (replaced scaffold) |
| `.agents/skills/loop-observer/agents/openai.yaml` | Created via `init_skill.py` |

No product docs, app code, tests, or other skills were modified.

### Verification

```text
python3 .../quick_validate.py .agents/skills/loop-observer  →  Skill is valid!
git diff --check  →  exit 0 (untracked skill dir only)
```

`composer docs-lint` was not run; only harness skill files were added.

### Skill contents (summary)

- **Role**: Live, concurrent, read-only observer alongside `implementing-features`.
- **vs post-feature analyzer**: Table contrasting timing, inputs, outputs, and goals.
- **Metrics**: Wrong files/tests, redundant reads, extra turns/tools, bad verification, worktree/branch mistakes, broad search, transcript/cleanup risk, time-to-correction.
- **Taxonomy**: Wrong turn, friction event, intervention-worthy, neutral observation.
- **Behavior**: Tail output, sample events, count objectively, no coaching; interrupt only for listed safety/validity cases.
- **Persistence**: Scratchpad/roadmap append by default; optional `.orbit/evidence/loop-observer-*.md`; not product authority.
- **Report template**: Markdown block at end of `SKILL.md`.

### Deviations

- `init_skill.py` needs **repeatable** `--interface key=value` flags; a single combined `--interface` string failed (documented in handoff as “when practical”).
- Removed scaffold “Structuring” and “Resources” sections so the skill stays operational and self-contained (`SKILL.md` + `agents/openai.yaml` only).

### Risks / follow-ups

- **Forward test**: `.orbit/loop.md` already defers running this observer on a real implementation loop until a future feature.
- **Discoverability**: Skill is not wired into `AGENT_FAST_PATH.md` or `HARNESS.md` role tables; add in a separate slice if orchestrators should discover it by default.
- **Untracked**: Files are new and unstaged; no commit per handoff.
