# JSON Renderer: `orbit deploy:history`

[Back to `deploy:history` technical contract.](1_deploy-history.md)

**Renderer:** JSON renderer.

## Primitive

None. JSON output is emitted as a machine-readable envelope via `$this->line(...)`.

## Selection

Use this renderer when `--json` is present.

## Envelope

This renderer uses the shared [JSON Envelope](../../../README.md#json-envelope).

## Success Shape

```json
{
  "success": {
    "data": {
      "runs": [
        {
          "id": 42,
          "project": "docs",
          "instance": "production",
          "status": "completed",
          "exit_code": 0,
          "started_at": "2026-05-02T09:00:00Z",
          "finished_at": "2026-05-02T09:01:30Z",
          "context": {
            "release": "20260502_090000_42",
            "app_path": "/home/docs/app",
            "release_path": "/home/docs/app/releases/20260502_090000_42",
            "app_user": "docs"
          },
          "steps": [
            {
              "id": 12,
              "title": "Pull latest",
              "status": "completed",
              "exit_code": 0
            }
          ]
        }
      ]
    },
    "meta": {
      "pagination": {
        "total": 1,
        "limit": 50,
        "limit_capped": false
      },
      "project": "docs",
      "instance": "production"
    }
  }
}
```

## Error Shape

```json
{
  "error": {
    "code": "validation_failed",
    "message": "Invalid value for --limit: must be a positive integer.",
    "meta": {
      "field": "limit",
      "value": "-1"
    }
  }
}
```

| Field | Type | Meaning |
| --- | --- | --- |
| `success.data.runs` | array | Deployment run entities sorted by `started_at` descending. |
| `success.meta.pagination.total` | integer | Total number of matching runs before the limit is applied. |
| `success.meta.pagination.limit` | integer | Effective limit used for this response. |
| `success.meta.pagination.limit_capped` | boolean | True when a caller-supplied limit exceeded `500` and was clamped. |
| `success.meta.project` | string | Project name. |
| `success.meta.instance` | string | Concrete instance that owns the returned history. |
| `error.code` | `validation_failed`, `gateway_unavailable`, `authorization_failed`, `instance.not_found`, `deploy.production_project_required` | Stable command failure code. `validation_failed` with `error.meta.reason=instance_required` reports an ambiguous bare project selector. |
| `error.meta` | object | Field, missing permission, instance selector, or authorization context for automation. |

## Test Mapping

| Path | Coverage |
| --- | --- |
| `apps/cli/tests/Feature/Commands/Deploy/DeployReadCommandsTest.php` | JSON renderer selection via `--json`, `success.data.runs` shape, `success.meta` app/count metadata, and gateway `validation_failed` passthrough for invalid limits. |

There is no gateway-side coverage for this mapped surface; linked CLI tests cover only the mapped assertions above. Remaining behavior stays a coverage gap until focused tests land.

Coverage gaps until focused tests land: pagination metadata including `limit_capped`, empty-history JSON shape, production-app failure shape, and all `error.code` values in this renderer.
