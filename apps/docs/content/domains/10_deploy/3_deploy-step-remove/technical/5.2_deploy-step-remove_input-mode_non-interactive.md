# Non-Interactive Input Mode: `orbit deploy:step-remove`

[Back to `deploy:step-remove` technical contract.](1_deploy-step-remove.md)

**Input mode:** Non-interactive.

## Mode Selection

This contract applies without a TTY or when `--json` is present. `--json`
forces non-interactive input mode.

## Behavior

No prompts are rendered. `app` and `step` must resolve from arguments, and
`--force` is required before side effects. Missing app or step fails before
side effects with `error.code=validation_failed`. Missing `--force` fails
before side effects with `error.code=validation_failed`,
`error.meta.field=force`, and
`error.meta.reason=destructive_consent_required`.

`--json` selects the JSON renderer only. It does not change validation, does not
grant authorization, and does not imply destructive consent.

## Test Mapping

| Path | Coverage |
| --- | --- |
| `apps/cli/tests/Feature/Commands/Deploy/DeployWriteCommandsTest.php` | Non-interactive missing `--force` validation with `meta.reason=destructive_consent_required` before gateway contact, and `--json` DELETE success envelope when force is supplied. |

There is no gateway-side coverage for this mapped surface; linked CLI tests cover only the mapped assertions above. Remaining behavior stays a coverage gap until focused tests land.

Coverage gaps until focused tests land: missing app/step validation, explicit non-TTY mode selection, and every documented non-interactive failure code.
