# JSON Renderer: `orbit workspace-teardown-step:list`

[Back to `workspace-teardown-step:list` technical contract.](1_workspace-teardown-step-list.md)

This page describes the JSON-rendered output for
`workspace-teardown-step:list`.

**Renderer:** JSON renderer.

## Primitive

None. JSON output is emitted as a machine-readable envelope via `$this->line(...)`.

## Selection

Use this renderer when `--json` is present.

## Envelope

This renderer uses the shared [JSON Envelope](../../../README.md#json-envelope).

The step array is rendered as a flat array under `success.data.steps`.
Steps are sorted by `order` ascending. This matches the order shown in the
human renderer, so both renderers represent the same result. Pagination,
count, or other execution context attaches to `success.meta.*`; it is not
nested inside `success.data`. Array length serves as the count.

## Success Shape

```json
{
  "success": {
    "data": {
      "steps": [
        {
          "id": 18,
          "app": "my-app",
          "phase": "teardown",
          "order": 1,
          "command": "dropdb my-app_db",
          "timeout_seconds": 600
        },
        {
          "id": 19,
          "app": "my-app",
          "phase": "teardown",
          "order": 2,
          "command": "rm -rf storage/logs",
          "timeout_seconds": 30
        }
      ]
    }
  }
}
```

| Field | Type | Meaning |
| --- | --- | --- |
| `steps` | array | Ordered list of teardown steps for the resolved `(app, phase=teardown)`. |
| `steps[].id` | integer | Gateway-assigned step identifier. Stable across re-runs and accepted by [`workspace-teardown-step:remove`](../../13_workspace-teardown-step-remove/workspace-teardown-step-remove.md). |
| `steps[].app` | string | Resolved parent app slug. |
| `steps[].phase` | string | Always `"teardown"` for this command. |
| `steps[].order` | integer | Absolute execution position within `(app, phase=teardown)`, ascending. |
| `steps[].command` | string | Shell command string as supplied to [`workspace-teardown-step:add`](../../11_workspace-teardown-step-add/workspace-teardown-step-add.md). Not parsed or expanded by the gateway. |
| `steps[].timeout_seconds` | integer | Strict positive integer step timeout in seconds. |

The `steps[]` record shape is the shared step shape published by every
`workspace-teardown-step:*` command. Fields such as `name`,
`working_directory`, `env_overrides`, and per-step `on_failure` are
explicitly absent and must not be added to a single command in the family
without updating the rest.

### Empty result

When the resolved app has no configured teardown steps:

```json
{
  "success": {
    "data": {
      "steps": []
    }
  }
}
```

## Error Shapes

### Validation failure (app)

Returned when the parent app cannot be resolved in non-interactive mode.

```json
{
  "error": {
    "code": "validation_failed",
    "message": "Could not resolve parent app.",
    "meta": {
      "field": "app",
      "reason": "missing_required_input"
    }
  }
}
```

### App not found

```json
{
  "error": {
    "code": "workspace.app_not_found",
    "message": "App 'my-app' not found.",
    "meta": {
      "app": "my-app"
    }
  }
}
```

### Unauthorized app

```json
{
  "error": {
    "code": "authorization_failed",
    "message": "You are not authorized to read teardown-step policy for 'my-app'.",
    "meta": {
      "app": "my-app"
    }
  }
}
```

### Gateway unavailable

```json
{
  "error": {
    "code": "gateway_unavailable",
    "message": "Gateway connection is required to list teardown steps.",
    "meta": {}
  }
}
```

## Common Error Codes

| Code | Meaning |
| --- | --- |
| `validation_failed` | A supplied input failed gateway-side static validation (parent app could not be resolved). `error.meta.field` names the offending input. |
| `workspace.app_not_found` | Resolved app slug does not exist in gateway configuration. `error.meta.app`. |
| `authorization_failed` | The caller is not authorized to read teardown-step policy for the target app. `error.meta.app`. |
| `gateway_unavailable` | The CLI cannot reach the gateway API. |

The `error.code` values listed here are exhaustive for
`workspace-teardown-step:list`. Add a new value to this renderer contract
before implementing another JSON failure code.

## Test Mapping

| Path | Coverage |
| --- | --- |
| `apps/cli/tests/Feature/Commands/Workspace/WorkspaceStepListCommandTest.php` | JSON teardown-step list payload and phase=teardown assertions. |

There is no gateway-side coverage for this renderer: JSON envelope rendering is owned by `apps/cli`. Documented error.code values and warning payload shape are not exhaustively asserted by the linked CLI tests unless the rows above name them explicitly. When this file documents destructive consent, routine coverage is CLI-owned.
