# JSON Renderer: `orbit deploy:step-add`

[Back to `deploy:step-add` technical contract.](1_deploy-step-add.md)

**Renderer:** JSON renderer.

## Primitive

None. JSON output is emitted as a machine-readable envelope via `$this->line(...)`.

## Selection

Use this renderer when `--json` is present.

## Envelope

This renderer uses the shared [JSON Envelope](../../../README.md#json-envelope).

## Success Shape

```json
{
  "success": {
    "data": {
      "step": {
        "id": 12,
        "app": "docs",
        "title": "Pull latest",
        "command": "git pull origin main",
        "order": 1,
        "timeout_seconds": 600,
        "retention": null
      }
    },
    "meta": {
      "action": "created"
    }
  }
}
```

## Error Shape

```json
{
  "error": {
    "code": "deploy.production_app_required",
    "message": "App 'docs' is not a production app.",
    "meta": {
      "app": "docs",
      "environment": "development"
    }
  }
}
```

| Field | Type | Meaning |
| --- | --- | --- |
| `success.data.step` | object | Deploy step entity defined in [`10_deploy/README.md`](../../README.md#deploy-step-json-entity). |
| `success.meta.action` | string | `created`. |
| `error.code` | `validation_failed`, `gateway_unavailable`, `authorization_failed`, `app.not_found`, `deploy.production_app_required` | Stable command failure code. |
| `error.meta` | object | Field, missing permission, app selector, app environment, or authorization context for automation. |

## Test Mapping

| Path | Coverage |
| --- | --- |
| `apps/gateway/tests/Feature/Commands/Deploy/DeployStepAddJsonRendererTest.php` | JSON renderer selection, step entity shape including timeout metadata, created metadata, production-app failure shape, and every `error.code` value documented in this renderer. |
