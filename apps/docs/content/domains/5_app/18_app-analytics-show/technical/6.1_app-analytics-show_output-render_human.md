# Human Renderer: `orbit app:analytics show`

[Back to `app:analytics show` technical contract.](1_app-analytics-show.md)

**Renderer:** Human renderer.

## Primitive

None. Output is emitted as prose lines.

## Selection

Use this renderer by default when `--json` is absent.

## Progress Tree

No progress tree is rendered. This command is a fast gateway database read.

## Success Render

```text
binding:
  app: docs
  app_instance: production
  serving_node: app-prod-1
  enabled: true
  internal_host: analytics.orbit
  dashboard_url: https://analytics.orbit
  public_hosts:
    - analytics.docs.example.com
  tracking_endpoints:
    - host: analytics.docs.example.com
      script_base_url: https://analytics.docs.example.com
      event_endpoint: https://analytics.docs.example.com/api/event
```

## Error Render

Errors are rendered as concise prose messages.

## Test Mapping

| Path | Coverage |
| --- | --- |
| `apps/gateway/tests/Feature/Http/Api/AppAnalyticsControllerTest.php` | Gateway response fields that human output renders and binding-missing payloads. |
| `apps/cli/tests/Feature/Commands/App/AppAnalyticsShowCommandTest.php` | Human renderer success output and binding-missing prose. |
