# Human Renderer: `orbit node:grant [consuming_node] [serving_node]`

[Back to `node:grant` technical contract.](1_node-grant.md)

This page describes the human-rendered terminal output for `node:grant`.

**Renderer:** Human renderer.

## Primitive

- Progress: [Progress tree](../../../../ux/commands/progress/progress-tree.md)

## Selection

Use this renderer when `--json` is not present.

The human renderer does not own input prompting. Interactive input may prompt
for missing nodes and the initial permission set before the renderer runs.
Non-interactive validation failures are rendered here as prose errors when
`--json` is absent.

## Progress Tree

`node:grant` performs only gateway-owned registry writes with no slow external
work. Execution is expected to complete well below one second on the happy path,
so no progress tree is rendered. If future implementation adds slow external
work, add a progress tree here.

## Success Output

When a new grant is created:

```text
Granted 'operator-1' access to 'app-1'
Permissions: node:read, tool:read
```

When the grant already exists (idempotent success):

```text
'operator-1' already has access to 'app-1'
Permissions: node:read, tool:read
Run `orbit node:permissions operator-1 app-1` to edit this grant.
```

When redundant permissions were normalized away, append:

```text
Warnings:
- Removed redundant permissions: tool:list
```

## Failure Output

Human-rendered failures must show:

- the failing field or condition;
- a clear prose error;
- no JSON envelope;
- next-step guidance only when the operator can act on it.

### Missing consuming node

```text
Consuming node is required.
```

### Missing serving node

```text
Serving node is required.
```

### Consuming node not found

```text
Consuming node 'operator-1' not found.
```

### Serving node not found

```text
Serving node 'app-1' not found.
```

### Grant policy violation

```text
Grant from 'operator-1' to 'app-1' violates node access policy.
```

### Gateway unavailable

```text
Gateway connection is required to grant node access.
```

### Authorization failed

```text
This action requires the node:grant permission on a grant to the gateway.
```

## Test Mapping

| Path | Coverage |
| --- | --- |
| `apps/cli/tests/Feature/Commands/Node/NodeWriteCommandTest.php` | Human grant, idempotent, and warning prose. |

There is no gateway-side coverage for this command-local mapping: input handling and renderer behavior live in `apps/cli`. Gateway API behavior is mapped in the command contract file when a gateway-side surface exists.