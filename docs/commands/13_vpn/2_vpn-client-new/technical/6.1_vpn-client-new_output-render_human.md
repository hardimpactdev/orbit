# Human Renderer: `orbit vpn-client:new <name>`

[Back to `vpn-client:new` technical contract.](1_vpn-client-new.md)

**Renderer:** Human terminal renderer.

## Primitive

- Progress: [Progress tree](../../../ux/progress/progress-tree.md)

## Selection

Use this renderer when `--json` is not present.

## Progress Tree

`vpn-client:new` performs gateway-local backend authentication and writes a
backend peer, so the human renderer must render a progress tree after input
resolution and before side effects begin:

```text
┌ Creating VPN client
○ Validate gateway access
○ Authenticate VPN backend
○ Create VPN client
○ Render client config
└ Working...
```

When `--config` is absent, the `Render client config` step is omitted.

## Success Output

```text
VPN client `laptop` created
Address: 10.6.0.7
```

When `--config` is present, print the generated config after the summary:

```text
# --- wg-orbit.conf ---
[Interface]
...
```

## Failure Output

Human-rendered failures use one concise error line and no JSON envelope.
Validation failures identify the conflicting field or client name when known.

## Test Mapping

| Path | Coverage |
| --- | --- |
| `tests/Feature/Commands/Vpn/VpnClientNewRendererTest.php` | Progress tree, success prose, WireGuard address summary, optional config block, validation failure prose, backend failure prose, and absence of JSON envelopes in human mode. |
