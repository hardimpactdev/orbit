{"tool":"pest","result":"failed","tests":24,"passed":19,"assertions":257,"duration_ms":3718,"failed":5,"failures":[{"test":"P\\Tests\\Feature\\Release\\OrbitReleaseWorkflowTest::__pest_evaluable_it_promotes_prebuilt_cli_artifacts_gateway_image_and_release_manifest_on_GitHub_releases","file":"/Users/nckrtl/orbit/.worktrees/release-ghcr-tag-after-verify/apps/gateway/tests/Feature/Release/OrbitReleaseWorkflowTest.php","line":154,"message":"Failed asserting that 'name: Orbit Release\\n\n\\n\n# Drafts do not fire release created/edited/deleted. prereleased is public.\\n\n# Drafts also do not create a git tag. The operator must push v<VERSION>\\n\n# before creating the draft so checkout of inputs.tag can resolve.\\n\n# This workflow has no tag-push trigger; the push alone starts nothing.\\n\n# workflow_dispatch verifies a draft and publishes only on success.\\n\n# release published is a safety net; failure converts it back to a draft.\\n\n# Publishing with GITHUB_TOKEN does not re-enter this workflow.\\n\non:\\n\n  workflow_dispatch:\\n\n    inputs:\\n\n      tag:\\n\n        description: Draft release tag to verify and publish\\n\n        required: true\\n\n        type: string\\n\n  release:\\n\n    types: [published]\\n\n\\n\nconcurrency:\\n\n  group: orbit-release-${{ inputs.tag || github.event.release.tag_name }}\\n\n  cancel-in-progress: false\\n\n\\n\npermissions:\\n\n  contents: write\\n\n  packages: write\\n\n\\n\nenv:\\n\n  REGISTRY: ghcr.io\\n\n  IMAGE_NAME: hardimpactdev/orbit-gateway\\n\n\\n\njobs:\\n\n  release:\\n\n    name: Publish Release Artifacts\\n\n    runs-on: ubuntu-latest\\n\n\\n\n    steps:\\n\n      - name: Checkout\\n\n        uses: actions/checkout@v4\\n\n        with:\\n\n          fetch-depth: 0\\n\n          ref: ${{ inputs.tag || github.event.release.tag_name }}\\n\n\\n\n      - name: Set up PHP\\n\n        uses: shivammathur/setup-php@v2\\n\n        with:\\n\n          php-version: \"8.5\"\\n\n          extensions: pdo_sqlite, sqlite3, curl, openssl, mbstring, tokenizer, ctype, filter, fileinfo, phar, zlib\\n\n          tools: composer\\n\n\\n\n      - name: Resolve release version\\n\n        id: version\\n\n        env:\\n\n          RELEASE_TAG: ${{ inputs.tag || github.event.release.tag_name }}\\n\n        run: |\\n\n          set -euo pipefail\\n\n          tag=\"${RELEASE_TAG:-${GITHUB_REF_NAME:-}}\"\\n\n          version=\"${tag#v}\"\\n\n          canonical_version=\"$(bin/orbit-version)\"\\n\n\\n\n          if [ \"$tag\" = \"$version\" ]; then\\n\n            echo \"Release tag must start with v, got [$tag].\" >&2\\n\n            exit 1\\n\n          fi\\n\n\\n\n          if [ \"$version\" != \"$canonical_version\" ]; then\\n\n            echo \"Release tag [$tag] does not match VERSION [$canonical_version].\" >&2\\n\n            exit 1\\n\n          fi\\n\n\\n\n          echo \"tag=${tag}\" >> \"$GITHUB_OUTPUT\"\\n\n          echo \"value=${version}\" >> \"$GITHUB_OUTPUT\"\\n\n\\n\n      - name: Download prebuilt release assets\\n\n        id: assets\\n\n        env:\\n\n          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}\\n\n          TAG: ${{ steps.version.outputs.tag }}\\n\n        run: |\\n\n          set -euo pipefail\\n\n          asset_dir=\"$RUNNER_TEMP/orbit-release-assets\"\\n\n          mkdir -p \"$asset_dir\"\\n\n\\n\n          gh release download \"$TAG\" \\\\n\n            --repo \"$GITHUB_REPOSITORY\" \\\\n\n            --pattern orbit-linux-x64 \\\\n\n            --pattern orbit-macos-arm64 \\\\n\n            --pattern orbit-reverb-linux-amd64.tar \\\\n\n            --pattern orbit-release-manifest.json \\\\n\n            --dir \"$asset_dir\"\\n\n\\n\n          test -s \"$asset_dir/orbit-linux-x64\"\\n\n          test -s \"$asset_dir/orbit-macos-arm64\"\\n\n          test -s \"$asset_dir/orbit-reverb-linux-amd64.tar\"\\n\n          test -s \"$asset_dir/orbit-release-manifest.json\"\\n\n\\n\n          echo \"dir=${asset_dir}\" >> \"$GITHUB_OUTPUT\"\\n\n\\n\n      - name: Verify promoted release manifest\\n\n        id: manifest\\n\n        env:\\n\n          ASSET_DIR: ${{ steps.assets.outputs.dir }}\\n\n          VERSION: ${{ steps.version.outputs.value }}\\n\n          GITHUB_REPOSITORY: ${{ github.repository }}\\n\n        run: |\\n\n          set -euo pipefail\\n\n\\n\n          php <<'PHP'\\n\n          <?php\\n\n\\n\n          declare(strict_types=1);\\n\n\\n\n          $assetDir = getenv('ASSET_DIR') ?: '';\\n\n          $version = getenv('VERSION') ?: '';\\n\n          $repository = getenv('GITHUB_REPOSITORY') ?: '';\\n\n          $manifestPath = \"{$assetDir}/orbit-release-manifest.json\";\\n\n\\n\n          $manifest = json_decode((string) file_get_contents($manifestPath), true, flags: JSON_THROW_ON_ERROR);\\n\n\\n\n          if (! is_array($manifest)) {\\n\n              fail('Release manifest must be a JSON object.');\\n\n          }\\n\n\\n\n          if (($manifest['version'] ?? null) !== $version) {\\n\n              fail(\"Release manifest version does not match {$version}.\");\\n\n          }\\n\n\\n\n          if (($manifest['source'] ?? null) !== 'github-release') {\\n\n              fail('Release manifest source must be github-release after promotion.');\\n\n          }\\n\n\\n\n          $gateway = $manifest['images']['gateway'] ?? null;\\n\n          $gatewayPrefix = \"ghcr.io/hardimpactdev/orbit-gateway:{$version}@sha256:\";\\n\n          $gatewayDigest = is_string($gateway) ? substr($gateway, strlen($gatewayPrefix)) : '';\\n\n\\n\n          if (\\n\n              ! is_string($gateway)\\n\n              || ! str_starts_with($gateway, $gatewayPrefix)\\n\n              || preg_match('/^[0-9a-f]{64}$/D', $gatewayDigest) !== 1\\n\n          ) {\\n\n              fail('Release manifest gateway image must be the promoted digest-pinned ghcr.io/hardimpactdev/orbit-gateway image.');\\n\n          }\\n\n\\n\n          $expectedArtifacts = [\\n\n              'linux-amd64' => 'orbit-linux-x64',\\n\n              'darwin-arm64' => 'orbit-macos-arm64',\\n\n          ];\\n\n\\n\n          foreach ($expectedArtifacts as $platform => $asset) {\\n\n              $artifact = $manifest['cli_artifacts'][$platform] ?? null;\\n\n\\n\n              if (! is_array($artifact)) {\\n\n                  fail(\"Release manifest missing CLI artifact {$platform}.\");\\n\n              }\\n\n\\n\n              $expectedUrl = \"https://github.com/{$repository}/releases/download/v{$version}/{$asset}\";\\n\n\\n\n              if (($artifact['url'] ?? null) !== $expectedUrl) {\\n\n                  fail(\"Release manifest {$platform} URL must point at {$expectedUrl}.\");\\n\n              }\\n\n\\n\n              $actualHash = hash_file('sha256', \"{$assetDir}/{$asset}\");\\n\n\\n\n              if (! is_string($actualHash) || ($artifact['sha256'] ?? null) !== $actualHash) {\\n\n                  fail(\"Release manifest {$platform} sha256 does not match the promoted asset.\");\\n\n              }\\n\n          }\\n\n\\n\n          $websocketArtifact = $manifest['role_image_artifacts']['orbit-websocket'] ?? null;\\n\n\\n\n          if (! is_array($websocketArtifact)) {\\n\n              fail('Release manifest must publish role_image_artifacts.orbit-websocket for credential-free websocket role acquisition.');\\n\n          }\\n\n\\n\n          $reverbAsset = 'orbit-reverb-linux-amd64.tar';\\n\n          $expectedReverbUrl = \"https://github.com/{$repository}/releases/download/v{$version}/{$reverbAsset}\";\\n\n\\n\n          if (($websocketArtifact['url'] ?? null) !== $expectedReverbUrl) {\\n\n              fail(\"Release manifest role_image_artifacts.orbit-websocket URL must point at {$expectedReverbUrl}.\");\\n\n          }\\n\n\\n\n          $reverbPath = \"{$assetDir}/{$reverbAsset}\";\\n\n\\n\n          if (! is_file($reverbPath)) {\\n\n              fail(\"Release manifest websocket role image archive {$reverbAsset} was not published with the release.\");\\n\n          }\\n\n\\n\n          $reverbHash = hash_file('sha256', $reverbPath);\\n\n\\n\n          if (! is_string($reverbHash) || ($websocketArtifact['sha256'] ?? null) !== $reverbHash) {\\n\n              fail('Release manifest role_image_artifacts.orbit-websocket sha256 does not match the promoted archive.');\\n\n          }\\n\n\\n\n          file_put_contents(getenv('GITHUB_OUTPUT'), \"gateway_ref={$gateway}\\n\", FILE_APPEND);\\n\n\\n\n          function fail(string $message): never\\n\n          {\\n\n              fwrite(STDERR, \"{$message}\\n\");\\n\n\\n\n              exit(1);\\n\n          }\\n\n          PHP\\n\n\\n\n      - name: Set up Docker Buildx\\n\n        uses: docker/setup-buildx-action@v3\\n\n\\n\n      - name: Log in to GHCR\\n\n        run: |\\n\n          echo \"${{ secrets.GITHUB_TOKEN }}\" | docker login \"$REGISTRY\" -u \"${{ github.actor }}\" --password-stdin\\n\n\\n\n      - name: Promote canonical gateway image version tag\\n\n        env:\\n\n          GATEWAY_REF: ${{ steps.manifest.outputs.gateway_ref }}\\n\n          VERSION: ${{ steps.version.outputs.value }}\\n\n        run: |\\n\n          set -euo pipefail\\n\n\\n\n          expected_digest=\"${GATEWAY_REF##*@}\"\\n\n          promoted_ref=\"${REGISTRY}/${IMAGE_NAME}:${VERSION}\"\\n\n\\n\n          docker buildx imagetools create \\\\n\n            --prefer-index=false \\\\n\n            --tag \"$promoted_ref\" \\\\n\n            \"$GATEWAY_REF\"\\n\n\\n\n          inspect_output=\"$(docker buildx imagetools inspect \"$promoted_ref\")\"\\n\n          actual_digest=\"$(awk '/^Digest:/ { print $2; exit }' <<< \"$inspect_output\")\"\\n\n\\n\n          if [ \"$actual_digest\" != \"$expected_digest\" ]; then\\n\n            echo \"Promoted gateway digest is [${actual_digest:-missing}], expected [$expected_digest].\" >&2\\n\n            exit 1\\n\n          fi\\n\n\\n\n          echo \"Promoted gateway digest ${expected_digest} to ${promoted_ref}.\"\\n\n\\n\n      - name: Verify promoted gateway image is pullable\\n\n        env:\\n\n          GATEWAY_REF: ${{ steps.manifest.outputs.gateway_ref }}\\n\n        run: |\\n\n          set -euo pipefail\\n\n          docker_config=\"$(mktemp -d)\"\\n\n          trap 'rm -rf \"$docker_config\"' EXIT\\n\n\\n\n          DOCKER_CONFIG=\"$docker_config\" docker pull \"$GATEWAY_REF\"\\n\n\\n\n      - name: Verify promoted gateway image bakes the release version\\n\n        env:\\n\n          GATEWAY_REF: ${{ steps.manifest.outputs.gateway_ref }}\\n\n          VERSION: ${{ steps.version.outputs.value }}\\n\n        run: |\\n\n          set -euo pipefail\\n\n\\n\n          baked=\"$(docker run --rm --entrypoint cat \"$GATEWAY_REF\" /srv/orbit/VERSION || true)\"\\n\n          if [ \"$baked\" != \"$VERSION\" ]; then\\n\n            echo \"Gateway image /srv/orbit/VERSION is [$baked], expected [$VERSION] (gateway-version regression).\" >&2\\n\n            exit 1\\n\n          fi\\n\n\\n\n          echo \"Gateway image bakes version ${VERSION} at /srv/orbit/VERSION.\"\\n\n\\n\n      - name: Publish split package repositories\\n\n        env:\\n\n          TAG: ${{ steps.version.outputs.tag }}\\n\n          VERSION: ${{ steps.version.outputs.value }}\\n\n          SPLIT_PUSH_TOKEN: ${{ secrets.ORBIT_RELEASE_TOKEN || github.token }}\\n\n          PACKAGIST_USERNAME: ${{ secrets.PACKAGIST_USERNAME }}\\n\n          PACKAGIST_TOKEN: ${{ secrets.PACKAGIST_TOKEN }}\\n\n        run: |\\n\n          set -euo pipefail\\n\n\\n\n          publish_split() {\\n\n            package=\"$1\"\\n\n            repository=\"$2\"\\n\n            output=\"$RUNNER_TEMP/orbit-splits/$package\"\\n\n\\n\n            rm -rf \"$output\"\\n\n            bin/orbit-prepare-release-package --package=\"$package\" --version=\"$VERSION\" --output=\"$output\"\\n\n\\n\n            # Never ship install trees into split repositories.\\n\n            rm -rf \"$output/node_modules\" \"$output/coverage\"\\n\n\\n\n            git -C \"$output\" init\\n\n            git -C \"$output\" config user.name \"github-actions[bot]\"\\n\n            git -C \"$output\" config user.email \"41898282+github-actions[bot]@users.noreply.github.com\"\\n\n            git -C \"$output\" add -A\\n\n            if git -C \"$output\" ls-files --error-unmatch node_modules >/dev/null 2>&1; then\\n\n              echo \"Refusing to publish split package with node_modules tracked.\" >&2\\n\n              exit 1\\n\n            fi\\n\n            git -C \"$output\" commit -m \"Release $TAG\"\\n\n            git -C \"$output\" branch -M main\\n\n            git -C \"$output\" tag -f \"$TAG\"\\n\n            git -C \"$output\" remote add origin \"https://x-access-token:[REDACTED:secret-assignment]\"\\n\n            git -C \"$output\" push --force origin HEAD:main\\n\n            git -C \"$output\" push --force origin \"refs/tags/${TAG}\"\\n\n          }\\n\n\\n\n          # TypeScript SDK (@hardimpactdev/orbit-sdk-typescript) is independently\\n\n          # versioned and published from hardimpactdev/orbit-sdk-typescript via\\n\n          # its own OIDC release workflow. Root orbit-release.yml must not\\n\n          # prepare, split-tag, or npm-publish that package.\\n\n\\n\n          publish_split core hardimpactdev/orbit-core\\n\n          publish_split sdk hardimpactdev/orbit-sdk-laravel\\n\n          publish_split cli hardimpactdev/orbit-cli\\n\n          publish_split gateway hardimpactdev/orbit-gateway\\n\n\\n\n          if [ -n \"${PACKAGIST_USERNAME:-}\" ] && [ -n \"${PACKAGIST_TOKEN:-}\" ]; then\\n\n            curl -fsS \\\\n\n              -X POST \\\\n\n              -H \"Content-Type: application/json\" \\\\n\n              -d '{\"repository\":{\"url\":\"https://github.com/hardimpactdev/orbit-core\"}}' \\\\n\n              \"https://packagist.org/api/update-package?username=${PACKAGIST_USERNAME}&apiToken=${PACKAGIST_TOKEN}\"\\n\n            curl -fsS \\\\n\n              -X POST \\\\n\n              -H \"Content-Type: application/json\" \\\\n\n              -d '{\"repository\":{\"url\":\"https://github.com/hardimpactdev/orbit-sdk-laravel\"}}' \\\\n\n              \"https://packagist.org/api/update-package?username=${PACKAGIST_USERNAME}&apiToken=${PACKAGIST_TOKEN}\"\\n\n          else\\n\n            echo \"Packagist credentials not configured; skipping hardimpactdev/orbit-core and hardimpactdev/orbit-sdk-laravel API updates.\"\\n\n          fi\\n\n\\n\n      - name: Publish verified GitHub release\\n\n        env:\\n\n          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}\\n\n          TAG: ${{ steps.version.outputs.tag }}\\n\n        run: |\\n\n          set -euo pipefail\\n\n\\n\n          draft=\"$(gh release view \"$TAG\" --repo \"$GITHUB_REPOSITORY\" --json isDraft --jq .isDraft)\"\\n\n          if [ \"$draft\" = \"true\" ]; then\\n\n            gh release edit \"$TAG\" --draft=false --repo \"$GITHUB_REPOSITORY\"\\n\n            echo \"Published verified GitHub release ${TAG}.\"\\n\n          else\\n\n            echo \"GitHub release ${TAG} is already published.\"\\n\n          fi\\n\n\\n\n      - name: Keep failed release unpublished\\n\n        if: failure()\\n\n        env:\\n\n          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}\\n\n          TAG: ${{ steps.version.outputs.tag || inputs.tag || github.event.release.tag_name }}\\n\n        run: |\\n\n          set -euo pipefail\\n\n\\n\n          if [ -z \"${TAG:-}\" ]; then\\n\n            echo \"Release tag was not resolved; cannot convert the release to a draft.\" >&2\\n\n            exit 0\\n\n          fi\\n\n\\n\n          draft=\"$(gh release view \"$TAG\" --repo \"$GITHUB_REPOSITORY\" --json isDraft --jq .isDraft)\"\\n\n          if [ \"$draft\" = \"false\" ]; then\\n\n            gh release edit \"$TAG\" --draft --repo \"$GITHUB_REPOSITORY\"\\n\n            echo \"Converted ${TAG} back to a draft after a failed verification or promotion step.\"\\n\n          else\\n\n            echo \"GitHub release ${TAG} remains unpublished.\"\\n\n          fi\\n\n' [ASCII](length: 13821) contains \"GATEWAY_DIGEST: ${{ steps.manifest.outputs.gateway_digest }}\" [ASCII](length: 60)."},{"test":"P\\Tests\\Feature\\Release\\OrbitReleaseWorkflowTest::__pest_evaluable_it_keeps_the_GitHub_release_unpublished_until_draft_asset_verification_succeeds","file":"/Users/nckrtl/orbit/.worktrees/release-ghcr-tag-after-verify/apps/gateway/tests/Feature/Release/OrbitReleaseWorkflowTest.php","line":286,"message":"Failed asserting that 'name: Orbit Release\\n\n\\n\n# Drafts do not fire release created/edited/deleted. prereleased is public.\\n\n# Drafts also do not create a git tag. The operator must push v<VERSION>\\n\n# before creating the draft so checkout of inputs.tag can resolve.\\n\n# This workflow has no tag-push trigger; the push alone starts nothing.\\n\n# workflow_dispatch verifies a draft and publishes only on success.\\n\n# release published is a safety net; failure converts it back to a draft.\\n\n# Publishing with GITHUB_TOKEN does not re-enter this workflow.\\n\non:\\n\n  workflow_dispatch:\\n\n    inputs:\\n\n      tag:\\n\n        description: Draft release tag to verify and publish\\n\n        required: true\\n\n        type: string\\n\n  release:\\n\n    types: [published]\\n\n\\n\nconcurrency:\\n\n  group: orbit-release-${{ inputs.tag || github.event.release.tag_name }}\\n\n  cancel-in-progress: false\\n\n\\n\npermissions:\\n\n  contents: write\\n\n  packages: write\\n\n\\n\nenv:\\n\n  REGISTRY: ghcr.io\\n\n  IMAGE_NAME: hardimpactdev/orbit-gateway\\n\n\\n\njobs:\\n\n  release:\\n\n    name: Publish Release Artifacts\\n\n    runs-on: ubuntu-latest\\n\n\\n\n    steps:\\n\n      - name: Checkout\\n\n        uses: actions/checkout@v4\\n\n        with:\\n\n          fetch-depth: 0\\n\n          ref: ${{ inputs.tag || github.event.release.tag_name }}\\n\n\\n\n      - name: Set up PHP\\n\n        uses: shivammathur/setup-php@v2\\n\n        with:\\n\n          php-version: \"8.5\"\\n\n          extensions: pdo_sqlite, sqlite3, curl, openssl, mbstring, tokenizer, ctype, filter, fileinfo, phar, zlib\\n\n          tools: composer\\n\n\\n\n      - name: Resolve release version\\n\n        id: version\\n\n        env:\\n\n          RELEASE_TAG: ${{ inputs.tag || github.event.release.tag_name }}\\n\n        run: |\\n\n          set -euo pipefail\\n\n          tag=\"${RELEASE_TAG:-${GITHUB_REF_NAME:-}}\"\\n\n          version=\"${tag#v}\"\\n\n          canonical_version=\"$(bin/orbit-version)\"\\n\n\\n\n          if [ \"$tag\" = \"$version\" ]; then\\n\n            echo \"Release tag must start with v, got [$tag].\" >&2\\n\n            exit 1\\n\n          fi\\n\n\\n\n          if [ \"$version\" != \"$canonical_version\" ]; then\\n\n            echo \"Release tag [$tag] does not match VERSION [$canonical_version].\" >&2\\n\n            exit 1\\n\n          fi\\n\n\\n\n          echo \"tag=${tag}\" >> \"$GITHUB_OUTPUT\"\\n\n          echo \"value=${version}\" >> \"$GITHUB_OUTPUT\"\\n\n\\n\n      - name: Download prebuilt release assets\\n\n        id: assets\\n\n        env:\\n\n          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}\\n\n          TAG: ${{ steps.version.outputs.tag }}\\n\n        run: |\\n\n          set -euo pipefail\\n\n          asset_dir=\"$RUNNER_TEMP/orbit-release-assets\"\\n\n          mkdir -p \"$asset_dir\"\\n\n\\n\n          gh release download \"$TAG\" \\\\n\n            --repo \"$GITHUB_REPOSITORY\" \\\\n\n            --pattern orbit-linux-x64 \\\\n\n            --pattern orbit-macos-arm64 \\\\n\n            --pattern orbit-reverb-linux-amd64.tar \\\\n\n            --pattern orbit-release-manifest.json \\\\n\n            --dir \"$asset_dir\"\\n\n\\n\n          test -s \"$asset_dir/orbit-linux-x64\"\\n\n          test -s \"$asset_dir/orbit-macos-arm64\"\\n\n          test -s \"$asset_dir/orbit-reverb-linux-amd64.tar\"\\n\n          test -s \"$asset_dir/orbit-release-manifest.json\"\\n\n\\n\n          echo \"dir=${asset_dir}\" >> \"$GITHUB_OUTPUT\"\\n\n\\n\n      - name: Verify promoted release manifest\\n\n        id: manifest\\n\n        env:\\n\n          ASSET_DIR: ${{ steps.assets.outputs.dir }}\\n\n          VERSION: ${{ steps.version.outputs.value }}\\n\n          GITHUB_REPOSITORY: ${{ github.repository }}\\n\n        run: |\\n\n          set -euo pipefail\\n\n\\n\n          php <<'PHP'\\n\n          <?php\\n\n\\n\n          declare(strict_types=1);\\n\n\\n\n          $assetDir = getenv('ASSET_DIR') ?: '';\\n\n          $version = getenv('VERSION') ?: '';\\n\n          $repository = getenv('GITHUB_REPOSITORY') ?: '';\\n\n          $manifestPath = \"{$assetDir}/orbit-release-manifest.json\";\\n\n\\n\n          $manifest = json_decode((string) file_get_contents($manifestPath), true, flags: JSON_THROW_ON_ERROR);\\n\n\\n\n          if (! is_array($manifest)) {\\n\n              fail('Release manifest must be a JSON object.');\\n\n          }\\n\n\\n\n          if (($manifest['version'] ?? null) !== $version) {\\n\n              fail(\"Release manifest version does not match {$version}.\");\\n\n          }\\n\n\\n\n          if (($manifest['source'] ?? null) !== 'github-release') {\\n\n              fail('Release manifest source must be github-release after promotion.');\\n\n          }\\n\n\\n\n          $gateway = $manifest['images']['gateway'] ?? null;\\n\n          $gatewayPrefix = \"ghcr.io/hardimpactdev/orbit-gateway:{$version}@sha256:\";\\n\n          $gatewayDigest = is_string($gateway) ? substr($gateway, strlen($gatewayPrefix)) : '';\\n\n\\n\n          if (\\n\n              ! is_string($gateway)\\n\n              || ! str_starts_with($gateway, $gatewayPrefix)\\n\n              || preg_match('/^[0-9a-f]{64}$/D', $gatewayDigest) !== 1\\n\n          ) {\\n\n              fail('Release manifest gateway image must be the promoted digest-pinned ghcr.io/hardimpactdev/orbit-gateway image.');\\n\n          }\\n\n\\n\n          $expectedArtifacts = [\\n\n              'linux-amd64' => 'orbit-linux-x64',\\n\n              'darwin-arm64' => 'orbit-macos-arm64',\\n\n          ];\\n\n\\n\n          foreach ($expectedArtifacts as $platform => $asset) {\\n\n              $artifact = $manifest['cli_artifacts'][$platform] ?? null;\\n\n\\n\n              if (! is_array($artifact)) {\\n\n                  fail(\"Release manifest missing CLI artifact {$platform}.\");\\n\n              }\\n\n\\n\n              $expectedUrl = \"https://github.com/{$repository}/releases/download/v{$version}/{$asset}\";\\n\n\\n\n              if (($artifact['url'] ?? null) !== $expectedUrl) {\\n\n                  fail(\"Release manifest {$platform} URL must point at {$expectedUrl}.\");\\n\n              }\\n\n\\n\n              $actualHash = hash_file('sha256', \"{$assetDir}/{$asset}\");\\n\n\\n\n              if (! is_string($actualHash) || ($artifact['sha256'] ?? null) !== $actualHash) {\\n\n                  fail(\"Release manifest {$platform} sha256 does not match the promoted asset.\");\\n\n              }\\n\n          }\\n\n\\n\n          $websocketArtifact = $manifest['role_image_artifacts']['orbit-websocket'] ?? null;\\n\n\\n\n          if (! is_array($websocketArtifact)) {\\n\n              fail('Release manifest must publish role_image_artifacts.orbit-websocket for credential-free websocket role acquisition.');\\n\n          }\\n\n\\n\n          $reverbAsset = 'orbit-reverb-linux-amd64.tar';\\n\n          $expectedReverbUrl = \"https://github.com/{$repository}/releases/download/v{$version}/{$reverbAsset}\";\\n\n\\n\n          if (($websocketArtifact['url'] ?? null) !== $expectedReverbUrl) {\\n\n              fail(\"Release manifest role_image_artifacts.orbit-websocket URL must point at {$expectedReverbUrl}.\");\\n\n          }\\n\n\\n\n          $reverbPath = \"{$assetDir}/{$reverbAsset}\";\\n\n\\n\n          if (! is_file($reverbPath)) {\\n\n              fail(\"Release manifest websocket role image archive {$reverbAsset} was not published with the release.\");\\n\n          }\\n\n\\n\n          $reverbHash = hash_file('sha256', $reverbPath);\\n\n\\n\n          if (! is_string($reverbHash) || ($websocketArtifact['sha256'] ?? null) !== $reverbHash) {\\n\n              fail('Release manifest role_image_artifacts.orbit-websocket sha256 does not match the promoted archive.');\\n\n          }\\n\n\\n\n          file_put_contents(getenv('GITHUB_OUTPUT'), \"gateway_ref={$gateway}\\n\", FILE_APPEND);\\n\n\\n\n          function fail(string $message): never\\n\n          {\\n\n              fwrite(STDERR, \"{$message}\\n\");\\n\n\\n\n              exit(1);\\n\n          }\\n\n          PHP\\n\n\\n\n      - name: Set up Docker Buildx\\n\n        uses: docker/setup-buildx-action@v3\\n\n\\n\n      - name: Log in to GHCR\\n\n        run: |\\n\n          echo \"${{ secrets.GITHUB_TOKEN }}\" | docker login \"$REGISTRY\" -u \"${{ github.actor }}\" --password-stdin\\n\n\\n\n      - name: Promote canonical gateway image version tag\\n\n        env:\\n\n          GATEWAY_REF: ${{ steps.manifest.outputs.gateway_ref }}\\n\n          VERSION: ${{ steps.version.outputs.value }}\\n\n        run: |\\n\n          set -euo pipefail\\n\n\\n\n          expected_digest=\"${GATEWAY_REF##*@}\"\\n\n          promoted_ref=\"${REGISTRY}/${IMAGE_NAME}:${VERSION}\"\\n\n\\n\n          docker buildx imagetools create \\\\n\n            --prefer-index=false \\\\n\n            --tag \"$promoted_ref\" \\\\n\n            \"$GATEWAY_REF\"\\n\n\\n\n          inspect_output=\"$(docker buildx imagetools inspect \"$promoted_ref\")\"\\n\n          actual_digest=\"$(awk '/^Digest:/ { print $2; exit }' <<< \"$inspect_output\")\"\\n\n\\n\n          if [ \"$actual_digest\" != \"$expected_digest\" ]; then\\n\n            echo \"Promoted gateway digest is [${actual_digest:-missing}], expected [$expected_digest].\" >&2\\n\n            exit 1\\n\n          fi\\n\n\\n\n          echo \"Promoted gateway digest ${expected_digest} to ${promoted_ref}.\"\\n\n\\n\n      - name: Verify promoted gateway image is pullable\\n\n        env:\\n\n          GATEWAY_REF: ${{ steps.manifest.outputs.gateway_ref }}\\n\n        run: |\\n\n          set -euo pipefail\\n\n          docker_config=\"$(mktemp -d)\"\\n\n          trap 'rm -rf \"$docker_config\"' EXIT\\n\n\\n\n          DOCKER_CONFIG=\"$docker_config\" docker pull \"$GATEWAY_REF\"\\n\n\\n\n      - name: Verify promoted gateway image bakes the release version\\n\n        env:\\n\n          GATEWAY_REF: ${{ steps.manifest.outputs.gateway_ref }}\\n\n          VERSION: ${{ steps.version.outputs.value }}\\n\n        run: |\\n\n          set -euo pipefail\\n\n\\n\n          baked=\"$(docker run --rm --entrypoint cat \"$GATEWAY_REF\" /srv/orbit/VERSION || true)\"\\n\n          if [ \"$baked\" != \"$VERSION\" ]; then\\n\n            echo \"Gateway image /srv/orbit/VERSION is [$baked], expected [$VERSION] (gateway-version regression).\" >&2\\n\n            exit 1\\n\n          fi\\n\n\\n\n          echo \"Gateway image bakes version ${VERSION} at /srv/orbit/VERSION.\"\\n\n\\n\n      - name: Publish split package repositories\\n\n        env:\\n\n          TAG: ${{ steps.version.outputs.tag }}\\n\n          VERSION: ${{ steps.version.outputs.value }}\\n\n          SPLIT_PUSH_TOKEN: ${{ secrets.ORBIT_RELEASE_TOKEN || github.token }}\\n\n          PACKAGIST_USERNAME: ${{ secrets.PACKAGIST_USERNAME }}\\n\n          PACKAGIST_TOKEN: ${{ secrets.PACKAGIST_TOKEN }}\\n\n        run: |\\n\n          set -euo pipefail\\n\n\\n\n          publish_split() {\\n\n            package=\"$1\"\\n\n            repository=\"$2\"\\n\n            output=\"$RUNNER_TEMP/orbit-splits/$package\"\\n\n\\n\n            rm -rf \"$output\"\\n\n            bin/orbit-prepare-release-package --package=\"$package\" --version=\"$VERSION\" --output=\"$output\"\\n\n\\n\n            # Never ship install trees into split repositories.\\n\n            rm -rf \"$output/node_modules\" \"$output/coverage\"\\n\n\\n\n            git -C \"$output\" init\\n\n            git -C \"$output\" config user.name \"github-actions[bot]\"\\n\n            git -C \"$output\" config user.email \"41898282+github-actions[bot]@users.noreply.github.com\"\\n\n            git -C \"$output\" add -A\\n\n            if git -C \"$output\" ls-files --error-unmatch node_modules >/dev/null 2>&1; then\\n\n              echo \"Refusing to publish split package with node_modules tracked.\" >&2\\n\n              exit 1\\n\n            fi\\n\n            git -C \"$output\" commit -m \"Release $TAG\"\\n\n            git -C \"$output\" branch -M main\\n\n            git -C \"$output\" tag -f \"$TAG\"\\n\n            git -C \"$output\" remote add origin \"https://x-access-token:[REDACTED:secret-assignment]\"\\n\n            git -C \"$output\" push --force origin HEAD:main\\n\n            git -C \"$output\" push --force origin \"refs/tags/${TAG}\"\\n\n          }\\n\n\\n\n          # TypeScript SDK (@hardimpactdev/orbit-sdk-typescript) is independently\\n\n          # versioned and published from hardimpactdev/orbit-sdk-typescript via\\n\n          # its own OIDC release workflow. Root orbit-release.yml must not\\n\n          # prepare, split-tag, or npm-publish that package.\\n\n\\n\n          publish_split core hardimpactdev/orbit-core\\n\n          publish_split sdk hardimpactdev/orbit-sdk-laravel\\n\n          publish_split cli hardimpactdev/orbit-cli\\n\n          publish_split gateway hardimpactdev/orbit-gateway\\n\n\\n\n          if [ -n \"${PACKAGIST_USERNAME:-}\" ] && [ -n \"${PACKAGIST_TOKEN:-}\" ]; then\\n\n            curl -fsS \\\\n\n              -X POST \\\\n\n              -H \"Content-Type: application/json\" \\\\n\n              -d '{\"repository\":{\"url\":\"https://github.com/hardimpactdev/orbit-core\"}}' \\\\n\n              \"https://packagist.org/api/update-package?username=${PACKAGIST_USERNAME}&apiToken=${PACKAGIST_TOKEN}\"\\n\n            curl -fsS \\\\n\n              -X POST \\\\n\n              -H \"Content-Type: application/json\" \\\\n\n              -d '{\"repository\":{\"url\":\"https://github.com/hardimpactdev/orbit-sdk-laravel\"}}' \\\\n\n              \"https://packagist.org/api/update-package?username=${PACKAGIST_USERNAME}&apiToken=${PACKAGIST_TOKEN}\"\\n\n          else\\n\n            echo \"Packagist credentials not configured; skipping hardimpactdev/orbit-core and hardimpactdev/orbit-sdk-laravel API updates.\"\\n\n          fi\\n\n\\n\n      - name: Publish verified GitHub release\\n\n        env:\\n\n          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}\\n\n          TAG: ${{ steps.version.outputs.tag }}\\n\n        run: |\\n\n          set -euo pipefail\\n\n\\n\n          draft=\"$(gh release view \"$TAG\" --repo \"$GITHUB_REPOSITORY\" --json isDraft --jq .isDraft)\"\\n\n          if [ \"$draft\" = \"true\" ]; then\\n\n            gh release edit \"$TAG\" --draft=false --repo \"$GITHUB_REPOSITORY\"\\n\n            echo \"Published verified GitHub release ${TAG}.\"\\n\n          else\\n\n            echo \"GitHub release ${TAG} is already published.\"\\n\n          fi\\n\n\\n\n      - name: Keep failed release unpublished\\n\n        if: failure()\\n\n        env:\\n\n          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}\\n\n          TAG: ${{ steps.version.outputs.tag || inputs.tag || github.event.release.tag_name }}\\n\n        run: |\\n\n          set -euo pipefail\\n\n\\n\n          if [ -z \"${TAG:-}\" ]; then\\n\n            echo \"Release tag was not resolved; cannot convert the release to a draft.\" >&2\\n\n            exit 0\\n\n          fi\\n\n\\n\n          draft=\"$(gh release view \"$TAG\" --repo \"$GITHUB_REPOSITORY\" --json isDraft --jq .isDraft)\"\\n\n          if [ \"$draft\" = \"false\" ]; then\\n\n            gh release edit \"$TAG\" --draft --repo \"$GITHUB_REPOSITORY\"\\n\n            echo \"Converted ${TAG} back to a draft after a failed verification or promotion step.\"\\n\n          else\\n\n            echo \"GitHub release ${TAG} remains unpublished.\"\\n\n          fi\\n\n' matches PCRE pattern \"/Verify promoted release manifest[\\s\\S]*Publish split package repositories[\\s\\S]*Promote canonical gateway image version tag[\\s\\S]*Publish verified GitHub release[\\s\\S]*Keep failed release unpublished/\"."},{"test":"P\\Tests\\Feature\\Release\\OrbitReleaseWorkflowTest::__pest_evaluable_it_promotes_the_GHCR_version_tag_only_after_the_accepted_image_verifies_and_split_packages_publish","file":"/Users/nckrtl/orbit/.worktrees/release-ghcr-tag-after-verify/apps/gateway/tests/Feature/Release/OrbitReleaseWorkflowTest.php","line":295,"message":"Failed asserting that 'name: Orbit Release\\n\n\\n\n# Drafts do not fire release created/edited/deleted. prereleased is public.\\n\n# Drafts also do not create a git tag. The operator must push v<VERSION>\\n\n# before creating the draft so checkout of inputs.tag can resolve.\\n\n# This workflow has no tag-push trigger; the push alone starts nothing.\\n\n# workflow_dispatch verifies a draft and publishes only on success.\\n\n# release published is a safety net; failure converts it back to a draft.\\n\n# Publishing with GITHUB_TOKEN does not re-enter this workflow.\\n\non:\\n\n  workflow_dispatch:\\n\n    inputs:\\n\n      tag:\\n\n        description: Draft release tag to verify and publish\\n\n        required: true\\n\n        type: string\\n\n  release:\\n\n    types: [published]\\n\n\\n\nconcurrency:\\n\n  group: orbit-release-${{ inputs.tag || github.event.release.tag_name }}\\n\n  cancel-in-progress: false\\n\n\\n\npermissions:\\n\n  contents: write\\n\n  packages: write\\n\n\\n\nenv:\\n\n  REGISTRY: ghcr.io\\n\n  IMAGE_NAME: hardimpactdev/orbit-gateway\\n\n\\n\njobs:\\n\n  release:\\n\n    name: Publish Release Artifacts\\n\n    runs-on: ubuntu-latest\\n\n\\n\n    steps:\\n\n      - name: Checkout\\n\n        uses: actions/checkout@v4\\n\n        with:\\n\n          fetch-depth: 0\\n\n          ref: ${{ inputs.tag || github.event.release.tag_name }}\\n\n\\n\n      - name: Set up PHP\\n\n        uses: shivammathur/setup-php@v2\\n\n        with:\\n\n          php-version: \"8.5\"\\n\n          extensions: pdo_sqlite, sqlite3, curl, openssl, mbstring, tokenizer, ctype, filter, fileinfo, phar, zlib\\n\n          tools: composer\\n\n\\n\n      - name: Resolve release version\\n\n        id: version\\n\n        env:\\n\n          RELEASE_TAG: ${{ inputs.tag || github.event.release.tag_name }}\\n\n        run: |\\n\n          set -euo pipefail\\n\n          tag=\"${RELEASE_TAG:-${GITHUB_REF_NAME:-}}\"\\n\n          version=\"${tag#v}\"\\n\n          canonical_version=\"$(bin/orbit-version)\"\\n\n\\n\n          if [ \"$tag\" = \"$version\" ]; then\\n\n            echo \"Release tag must start with v, got [$tag].\" >&2\\n\n            exit 1\\n\n          fi\\n\n\\n\n          if [ \"$version\" != \"$canonical_version\" ]; then\\n\n            echo \"Release tag [$tag] does not match VERSION [$canonical_version].\" >&2\\n\n            exit 1\\n\n          fi\\n\n\\n\n          echo \"tag=${tag}\" >> \"$GITHUB_OUTPUT\"\\n\n          echo \"value=${version}\" >> \"$GITHUB_OUTPUT\"\\n\n\\n\n      - name: Download prebuilt release assets\\n\n        id: assets\\n\n        env:\\n\n          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}\\n\n          TAG: ${{ steps.version.outputs.tag }}\\n\n        run: |\\n\n          set -euo pipefail\\n\n          asset_dir=\"$RUNNER_TEMP/orbit-release-assets\"\\n\n          mkdir -p \"$asset_dir\"\\n\n\\n\n          gh release download \"$TAG\" \\\\n\n            --repo \"$GITHUB_REPOSITORY\" \\\\n\n            --pattern orbit-linux-x64 \\\\n\n            --pattern orbit-macos-arm64 \\\\n\n            --pattern orbit-reverb-linux-amd64.tar \\\\n\n            --pattern orbit-release-manifest.json \\\\n\n            --dir \"$asset_dir\"\\n\n\\n\n          test -s \"$asset_dir/orbit-linux-x64\"\\n\n          test -s \"$asset_dir/orbit-macos-arm64\"\\n\n          test -s \"$asset_dir/orbit-reverb-linux-amd64.tar\"\\n\n          test -s \"$asset_dir/orbit-release-manifest.json\"\\n\n\\n\n          echo \"dir=${asset_dir}\" >> \"$GITHUB_OUTPUT\"\\n\n\\n\n      - name: Verify promoted release manifest\\n\n        id: manifest\\n\n        env:\\n\n          ASSET_DIR: ${{ steps.assets.outputs.dir }}\\n\n          VERSION: ${{ steps.version.outputs.value }}\\n\n          GITHUB_REPOSITORY: ${{ github.repository }}\\n\n        run: |\\n\n          set -euo pipefail\\n\n\\n\n          php <<'PHP'\\n\n          <?php\\n\n\\n\n          declare(strict_types=1);\\n\n\\n\n          $assetDir = getenv('ASSET_DIR') ?: '';\\n\n          $version = getenv('VERSION') ?: '';\\n\n          $repository = getenv('GITHUB_REPOSITORY') ?: '';\\n\n          $manifestPath = \"{$assetDir}/orbit-release-manifest.json\";\\n\n\\n\n          $manifest = json_decode((string) file_get_contents($manifestPath), true, flags: JSON_THROW_ON_ERROR);\\n\n\\n\n          if (! is_array($manifest)) {\\n\n              fail('Release manifest must be a JSON object.');\\n\n          }\\n\n\\n\n          if (($manifest['version'] ?? null) !== $version) {\\n\n              fail(\"Release manifest version does not match {$version}.\");\\n\n          }\\n\n\\n\n          if (($manifest['source'] ?? null) !== 'github-release') {\\n\n              fail('Release manifest source must be github-release after promotion.');\\n\n          }\\n\n\\n\n          $gateway = $manifest['images']['gateway'] ?? null;\\n\n          $gatewayPrefix = \"ghcr.io/hardimpactdev/orbit-gateway:{$version}@sha256:\";\\n\n          $gatewayDigest = is_string($gateway) ? substr($gateway, strlen($gatewayPrefix)) : '';\\n\n\\n\n          if (\\n\n              ! is_string($gateway)\\n\n              || ! str_starts_with($gateway, $gatewayPrefix)\\n\n              || preg_match('/^[0-9a-f]{64}$/D', $gatewayDigest) !== 1\\n\n          ) {\\n\n              fail('Release manifest gateway image must be the promoted digest-pinned ghcr.io/hardimpactdev/orbit-gateway image.');\\n\n          }\\n\n\\n\n          $expectedArtifacts = [\\n\n              'linux-amd64' => 'orbit-linux-x64',\\n\n              'darwin-arm64' => 'orbit-macos-arm64',\\n\n          ];\\n\n\\n\n          foreach ($expectedArtifacts as $platform => $asset) {\\n\n              $artifact = $manifest['cli_artifacts'][$platform] ?? null;\\n\n\\n\n              if (! is_array($artifact)) {\\n\n                  fail(\"Release manifest missing CLI artifact {$platform}.\");\\n\n              }\\n\n\\n\n              $expectedUrl = \"https://github.com/{$repository}/releases/download/v{$version}/{$asset}\";\\n\n\\n\n              if (($artifact['url'] ?? null) !== $expectedUrl) {\\n\n                  fail(\"Release manifest {$platform} URL must point at {$expectedUrl}.\");\\n\n              }\\n\n\\n\n              $actualHash = hash_file('sha256', \"{$assetDir}/{$asset}\");\\n\n\\n\n              if (! is_string($actualHash) || ($artifact['sha256'] ?? null) !== $actualHash) {\\n\n                  fail(\"Release manifest {$platform} sha256 does not match the promoted asset.\");\\n\n              }\\n\n          }\\n\n\\n\n          $websocketArtifact = $manifest['role_image_artifacts']['orbit-websocket'] ?? null;\\n\n\\n\n          if (! is_array($websocketArtifact)) {\\n\n              fail('Release manifest must publish role_image_artifacts.orbit-websocket for credential-free websocket role acquisition.');\\n\n          }\\n\n\\n\n          $reverbAsset = 'orbit-reverb-linux-amd64.tar';\\n\n          $expectedReverbUrl = \"https://github.com/{$repository}/releases/download/v{$version}/{$reverbAsset}\";\\n\n\\n\n          if (($websocketArtifact['url'] ?? null) !== $expectedReverbUrl) {\\n\n              fail(\"Release manifest role_image_artifacts.orbit-websocket URL must point at {$expectedReverbUrl}.\");\\n\n          }\\n\n\\n\n          $reverbPath = \"{$assetDir}/{$reverbAsset}\";\\n\n\\n\n          if (! is_file($reverbPath)) {\\n\n              fail(\"Release manifest websocket role image archive {$reverbAsset} was not published with the release.\");\\n\n          }\\n\n\\n\n          $reverbHash = hash_file('sha256', $reverbPath);\\n\n\\n\n          if (! is_string($reverbHash) || ($websocketArtifact['sha256'] ?? null) !== $reverbHash) {\\n\n              fail('Release manifest role_image_artifacts.orbit-websocket sha256 does not match the promoted archive.');\\n\n          }\\n\n\\n\n          file_put_contents(getenv('GITHUB_OUTPUT'), \"gateway_ref={$gateway}\\n\", FILE_APPEND);\\n\n\\n\n          function fail(string $message): never\\n\n          {\\n\n              fwrite(STDERR, \"{$message}\\n\");\\n\n\\n\n              exit(1);\\n\n          }\\n\n          PHP\\n\n\\n\n      - name: Set up Docker Buildx\\n\n        uses: docker/setup-buildx-action@v3\\n\n\\n\n      - name: Log in to GHCR\\n\n        run: |\\n\n          echo \"${{ secrets.GITHUB_TOKEN }}\" | docker login \"$REGISTRY\" -u \"${{ github.actor }}\" --password-stdin\\n\n\\n\n      - name: Promote canonical gateway image version tag\\n\n        env:\\n\n          GATEWAY_REF: ${{ steps.manifest.outputs.gateway_ref }}\\n\n          VERSION: ${{ steps.version.outputs.value }}\\n\n        run: |\\n\n          set -euo pipefail\\n\n\\n\n          expected_digest=\"${GATEWAY_REF##*@}\"\\n\n          promoted_ref=\"${REGISTRY}/${IMAGE_NAME}:${VERSION}\"\\n\n\\n\n          docker buildx imagetools create \\\\n\n            --prefer-index=false \\\\n\n            --tag \"$promoted_ref\" \\\\n\n            \"$GATEWAY_REF\"\\n\n\\n\n          inspect_output=\"$(docker buildx imagetools inspect \"$promoted_ref\")\"\\n\n          actual_digest=\"$(awk '/^Digest:/ { print $2; exit }' <<< \"$inspect_output\")\"\\n\n\\n\n          if [ \"$actual_digest\" != \"$expected_digest\" ]; then\\n\n            echo \"Promoted gateway digest is [${actual_digest:-missing}], expected [$expected_digest].\" >&2\\n\n            exit 1\\n\n          fi\\n\n\\n\n          echo \"Promoted gateway digest ${expected_digest} to ${promoted_ref}.\"\\n\n\\n\n      - name: Verify promoted gateway image is pullable\\n\n        env:\\n\n          GATEWAY_REF: ${{ steps.manifest.outputs.gateway_ref }}\\n\n        run: |\\n\n          set -euo pipefail\\n\n          docker_config=\"$(mktemp -d)\"\\n\n          trap 'rm -rf \"$docker_config\"' EXIT\\n\n\\n\n          DOCKER_CONFIG=\"$docker_config\" docker pull \"$GATEWAY_REF\"\\n\n\\n\n      - name: Verify promoted gateway image bakes the release version\\n\n        env:\\n\n          GATEWAY_REF: ${{ steps.manifest.outputs.gateway_ref }}\\n\n          VERSION: ${{ steps.version.outputs.value }}\\n\n        run: |\\n\n          set -euo pipefail\\n\n\\n\n          baked=\"$(docker run --rm --entrypoint cat \"$GATEWAY_REF\" /srv/orbit/VERSION || true)\"\\n\n          if [ \"$baked\" != \"$VERSION\" ]; then\\n\n            echo \"Gateway image /srv/orbit/VERSION is [$baked], expected [$VERSION] (gateway-version regression).\" >&2\\n\n            exit 1\\n\n          fi\\n\n\\n\n          echo \"Gateway image bakes version ${VERSION} at /srv/orbit/VERSION.\"\\n\n\\n\n      - name: Publish split package repositories\\n\n        env:\\n\n          TAG: ${{ steps.version.outputs.tag }}\\n\n          VERSION: ${{ steps.version.outputs.value }}\\n\n          SPLIT_PUSH_TOKEN: ${{ secrets.ORBIT_RELEASE_TOKEN || github.token }}\\n\n          PACKAGIST_USERNAME: ${{ secrets.PACKAGIST_USERNAME }}\\n\n          PACKAGIST_TOKEN: ${{ secrets.PACKAGIST_TOKEN }}\\n\n        run: |\\n\n          set -euo pipefail\\n\n\\n\n          publish_split() {\\n\n            package=\"$1\"\\n\n            repository=\"$2\"\\n\n            output=\"$RUNNER_TEMP/orbit-splits/$package\"\\n\n\\n\n            rm -rf \"$output\"\\n\n            bin/orbit-prepare-release-package --package=\"$package\" --version=\"$VERSION\" --output=\"$output\"\\n\n\\n\n            # Never ship install trees into split repositories.\\n\n            rm -rf \"$output/node_modules\" \"$output/coverage\"\\n\n\\n\n            git -C \"$output\" init\\n\n            git -C \"$output\" config user.name \"github-actions[bot]\"\\n\n            git -C \"$output\" config user.email \"41898282+github-actions[bot]@users.noreply.github.com\"\\n\n            git -C \"$output\" add -A\\n\n            if git -C \"$output\" ls-files --error-unmatch node_modules >/dev/null 2>&1; then\\n\n              echo \"Refusing to publish split package with node_modules tracked.\" >&2\\n\n              exit 1\\n\n            fi\\n\n            git -C \"$output\" commit -m \"Release $TAG\"\\n\n            git -C \"$output\" branch -M main\\n\n            git -C \"$output\" tag -f \"$TAG\"\\n\n            git -C \"$output\" remote add origin \"https://x-access-token:[REDACTED:secret-assignment]\"\\n\n            git -C \"$output\" push --force origin HEAD:main\\n\n            git -C \"$output\" push --force origin \"refs/tags/${TAG}\"\\n\n          }\\n\n\\n\n          # TypeScript SDK (@hardimpactdev/orbit-sdk-typescript) is independently\\n\n          # versioned and published from hardimpactdev/orbit-sdk-typescript via\\n\n          # its own OIDC release workflow. Root orbit-release.yml must not\\n\n          # prepare, split-tag, or npm-publish that package.\\n\n\\n\n          publish_split core hardimpactdev/orbit-core\\n\n          publish_split sdk hardimpactdev/orbit-sdk-laravel\\n\n          publish_split cli hardimpactdev/orbit-cli\\n\n          publish_split gateway hardimpactdev/orbit-gateway\\n\n\\n\n          if [ -n \"${PACKAGIST_USERNAME:-}\" ] && [ -n \"${PACKAGIST_TOKEN:-}\" ]; then\\n\n            curl -fsS \\\\n\n              -X POST \\\\n\n              -H \"Content-Type: application/json\" \\\\n\n              -d '{\"repository\":{\"url\":\"https://github.com/hardimpactdev/orbit-core\"}}' \\\\n\n              \"https://packagist.org/api/update-package?username=${PACKAGIST_USERNAME}&apiToken=${PACKAGIST_TOKEN}\"\\n\n            curl -fsS \\\\n\n              -X POST \\\\n\n              -H \"Content-Type: application/json\" \\\\n\n              -d '{\"repository\":{\"url\":\"https://github.com/hardimpactdev/orbit-sdk-laravel\"}}' \\\\n\n              \"https://packagist.org/api/update-package?username=${PACKAGIST_USERNAME}&apiToken=${PACKAGIST_TOKEN}\"\\n\n          else\\n\n            echo \"Packagist credentials not configured; skipping hardimpactdev/orbit-core and hardimpactdev/orbit-sdk-laravel API updates.\"\\n\n          fi\\n\n\\n\n      - name: Publish verified GitHub release\\n\n        env:\\n\n          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}\\n\n          TAG: ${{ steps.version.outputs.tag }}\\n\n        run: |\\n\n          set -euo pipefail\\n\n\\n\n          draft=\"$(gh release view \"$TAG\" --repo \"$GITHUB_REPOSITORY\" --json isDraft --jq .isDraft)\"\\n\n          if [ \"$draft\" = \"true\" ]; then\\n\n            gh release edit \"$TAG\" --draft=false --repo \"$GITHUB_REPOSITORY\"\\n\n            echo \"Published verified GitHub release ${TAG}.\"\\n\n          else\\n\n            echo \"GitHub release ${TAG} is already published.\"\\n\n          fi\\n\n\\n\n      - name: Keep failed release unpublished\\n\n        if: failure()\\n\n        env:\\n\n          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}\\n\n          TAG: ${{ steps.version.outputs.tag || inputs.tag || github.event.release.tag_name }}\\n\n        run: |\\n\n          set -euo pipefail\\n\n\\n\n          if [ -z \"${TAG:-}\" ]; then\\n\n            echo \"Release tag was not resolved; cannot convert the release to a draft.\" >&2\\n\n            exit 0\\n\n          fi\\n\n\\n\n          draft=\"$(gh release view \"$TAG\" --repo \"$GITHUB_REPOSITORY\" --json isDraft --jq .isDraft)\"\\n\n          if [ \"$draft\" = \"false\" ]; then\\n\n            gh release edit \"$TAG\" --draft --repo \"$GITHUB_REPOSITORY\"\\n\n            echo \"Converted ${TAG} back to a draft after a failed verification or promotion step.\"\\n\n          else\\n\n            echo \"GitHub release ${TAG} remains unpublished.\"\\n\n          fi\\n\n' [ASCII](length: 13821) contains \"Verify accepted gateway image is pullable\" [ASCII](length: 41)."},{"test":"P\\Tests\\Feature\\Release\\OrbitReleaseWorkflowTest::__pest_evaluable_it_keeps_the_operator_release_recipe_from_moving_the_GHCR_version_tag_before_the_workflow_verifies","file":"/Users/nckrtl/orbit/.worktrees/release-ghcr-tag-after-verify/apps/gateway/tests/Feature/Release/OrbitReleaseWorkflowTest.php","line":327,"message":"Expecting '---name: releasedescripti\u2026dly.' not to contain 'docker buildx imagetools create'."},{"test":"P\\Tests\\Feature\\Release\\OrbitReleaseWorkflowTest::__pest_evaluable_it_accepts_a_promotion_manifest_whose_websocket_archive_url_and_checksum_match","file":"/Users/nckrtl/orbit/.worktrees/release-ghcr-tag-after-verify/apps/gateway/tests/Feature/Release/OrbitReleaseWorkflowTest.php","line":399,"message":"Failed asserting that 'gateway_ref=ghcr.io/hardimpactdev/orbit-gateway:1.2.3@sha256:aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa\\n\n' [ASCII](length: 126) contains \"gateway_digest=sha256:aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa\" [ASCII](length: 86)."}]}
