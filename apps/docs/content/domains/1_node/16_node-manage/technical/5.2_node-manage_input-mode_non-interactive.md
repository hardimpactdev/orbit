# Non-Interactive Input Mode: `orbit node:manage`

[Back to `node:manage` technical contract.](1_node-manage.md)

**Input mode:** Non-interactive.

## Selection

Use this input mode when `--json` is present or when the command is running
without a TTY. `--json` always forces non-interactive input mode.

## Responsibilities

Prompts are never rendered in non-interactive mode.

- If `--user` is omitted, use the current local user.
- Require `--node-transport=transitional-ssh-fallback`; do not infer or prompt
  for transitional SSH authorization.
- Fail with `validation_failed` when the local SSH user cannot be resolved.
- Emit only the canonical JSON envelope in `--json` mode.

The command verifies the authenticated gateway identity and completes the
gateway transport preflight before mutating local `authorized_keys`.

## Test Mapping

No gateway-side coverage exists for this file because non-interactive input
mode is resolved in the CLI before gateway requests are sent.

| Path | Coverage |
| --- | --- |
| `apps/cli/tests/Feature/Commands/Node/NodeManageCommandTest.php` | JSON mode input resolution, no prompts, local validation failures, and gateway response pass-through. |
