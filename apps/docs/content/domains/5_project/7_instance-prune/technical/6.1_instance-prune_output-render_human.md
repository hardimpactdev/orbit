# Human Renderer: `orbit instance:prune`

[Back to technical contract](1_instance-prune.md)

## Primitive

- Progress: [Progress tree](../../../../ux/commands/progress/progress-tree.md)

This file owns the human output contract for `instance:prune`.

## Progress Tree

The human renderer uses a step tree grouped by workspace.

```text
┌ Pruning Workspaces For docs.development
○ Query agent IDE adapters
○ Remove workspace `feature-docs`
○ Remove workspace `stale-experiment`
└ Working...
```

If `--dry-run` is present:

```text
┌ Previewing Workspace Prune For docs.development
○ Query agent IDE adapters
○ Preview workspace `feature-docs`
○ Preview workspace `stale-experiment`
└ Dry run complete. No side effects performed.
```

## Failure Reporting

- **Validation Errors:** Rendered as standard Laravel Prompts error messages.
- **Partial Success:**
  ```text
  ! [Workspace] feature-docs: Partial removal.
    - [Filesystem] Error: Permission denied.
  ```

## Test Mapping

| Path | Coverage |
| --- | --- |
| `apps/cli/tests/Feature/Commands/App/AppWriteCommandTest.php` | Human renderer output including progress tree and dry-run labels. |

There is no gateway-side coverage for this command-local mapping: input handling and renderer behavior live in `apps/cli`. Gateway API behavior is mapped in the command contract file when a gateway-side surface exists.
