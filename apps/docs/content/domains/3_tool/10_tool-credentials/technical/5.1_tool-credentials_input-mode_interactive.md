# Interactive Input Mode: `orbit tool-credentials`

[Back to `tool-credentials` technical contract.](1_tool-credentials.md)

**Input mode:** Interactive.

This contract applies only when the process is attached to a TTY and `--json` is not present.

## Prompt Behavior

When the tool argument is omitted, prompt from the matching visible tool set for the resolved node.

Target node prompts may render only after gateway visibility is known. Invalid prompt answers re-prompt until the value is valid or the operator aborts.

## Test Mapping

| Path | Coverage |
| --- | --- |
| `apps/cli/tests/Feature/Commands/Tool/ToolCredentialsCommandTest.php` | Interactive tool prompt, default-node resolution before prompting, and side-effect prevention before confirmed input resolution. |
There is no gateway-side coverage for this command-local mapping: input handling and renderer behavior live in `apps/cli`. Gateway API behavior is mapped in the command contract file when a gateway-side surface exists.
