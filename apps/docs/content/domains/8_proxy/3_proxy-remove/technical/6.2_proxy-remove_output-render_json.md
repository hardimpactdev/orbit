# JSON Renderer: `orbit proxy:remove`

[Back to `proxy:remove` technical contract.](1_proxy-remove.md)

**Renderer:** JSON renderer.

## Primitive

None. JSON output is emitted as a machine-readable envelope via `$this->line(...)`.

## Selection

Use this renderer when `--json` is present.

## Envelope

This renderer uses the shared [JSON Envelope](../../../README.md#json-envelope).

## Success Shape

```json
{
  "success": {
    "data": {
      "route": {
        "domain": "redirect.docs.test",
        "kind": "redirect",
        "owner": {
          "type": "custom",
          "name": null
        },
        "node": "app-1",
        "target": {
          "type": "redirect",
          "value": "https://docs.test"
        },
        "redirect_code": 302,
        "tls": {
          "managed_by": "orbit",
          "trusted_by_gateway_ca": true
        },
        "status": "removed"
      }
    },
    "meta": {
      "backend_removed": true,
      "tls_removed": true
    }
  }
}
```

## Error Shape

```json
{
  "error": {
    "code": "proxy.cleanup_failed",
    "message": "The proxy route was removed from gateway configuration, but backend cleanup failed.",
    "data": {
      "route": {
        "domain": "redirect.docs.test",
        "kind": "redirect",
        "owner": {
          "type": "custom",
          "name": null
        },
        "node": "app-1",
        "target": {
          "type": "redirect",
          "value": "https://docs.test"
        },
        "redirect_code": 302,
        "tls": {
          "managed_by": "orbit",
          "trusted_by_gateway_ca": true
        },
        "status": "removed_with_drift"
      }
    },
    "meta": {
      "next_command": "doctor --family=proxy --restore --node [--node-transport=<transport>]=app-1"
    }
  }
}
```

| Field | Type | Meaning |
| --- | --- | --- |
| `success.data.route` | object | Removed custom proxy route. |
| `success.meta.backend_removed` | boolean | Whether backend route cleanup completed. |
| `success.meta.tls_removed` | boolean | Whether route-scoped TLS cleanup completed. |
| `error.code` | `validation_failed`, `gateway_unavailable`, `authorization_failed`, `proxy.not_found`, `proxy.owned_route_denied`, `destructive_consent_required`, `proxy.cleanup_failed` | Stable command failure code. |
| `error.data.route` | object | Removed route when cleanup failed after gateway configuration removal. |
| `error.meta` | object | Field, node, owner, caller, or recovery context for automation. |

## Test Mapping

| Path | Coverage |
| --- | --- |
| `apps/cli/tests/Feature/Commands/Proxy/ProxyWriteCommandTest.php` | JSON `proxy:remove` destructive consent failure, forced DELETE success envelope, cleanup metadata, and gateway error passthrough. |


Documented `error.code` values named for coverage tracking: `validation_failed`, `gateway_unavailable`, `authorization_failed`, `proxy.not_found`, `proxy.owned_route_denied`, `destructive_consent_required`, and `proxy.cleanup_failed`. Destructive consent coverage is asserted through the forced and unforced `proxy:remove` CLI paths.

There is no gateway-side coverage for this command-local mapping: input handling and renderer behavior live in `apps/cli`. Gateway API behavior is mapped in the command contract file when a gateway-side surface exists.
