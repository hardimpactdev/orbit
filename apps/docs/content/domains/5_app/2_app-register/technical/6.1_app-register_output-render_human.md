# Technical Contract: `orbit app:register` (Human Renderer)

This contract defines the **human renderer** for `app:register`.

[Back to the canonical contract.](1_app-register.md)

## Primitive

- Progress: [Progress tree](../../../../ux/commands/progress/progress-tree.md)

## Progress Tree

The human renderer uses a step tree to visualize the registration and
apply process.

```text
┌ Registering App
○ Resolve app configuration
○ Register or adopt selected app instance
○ Apply and verify instance runtime
○ Apply and verify instance routing
○ Verify application
└ Working...
```

## Human Strings

The success line varies by `result.action` so the operator can see what this
run did:

- **Success (`registered`)**: "App instance '{selector}' successfully registered on node '{node}'."
- **Success (`adopted`)**: "App instance '{selector}' successfully adopted from path '{path}' on node '{node}'."
- **Success (`moved`)**: "App instance '{selector}' successfully moved to path '{path}' on node '{node}'."
- **Success (`converged`)**: "App instance '{selector}' is already converged on node '{node}'. No changes were needed."
- **Success (`partial`)**: "App instance '{selector}' is registered on node '{node}', but proxy enactment is incomplete." The following warning names the failed layer, node, and operation.
- **Success with warning**: The success line above is rendered, followed by a
  `Warning:` block summarizing each entry from `success.meta.warnings[]` with
  its message and the suggested `next_command`. For example, when production
  activation is pending: "Warning: Production domain '{domain}' is not yet
  active. Retry with `orbit app:register {selector} --domain={domain}` once DNS
  has propagated."
- **Failure (path collision)**: "Path '{path}' on node '{node}' is already owned by app instance '{existing_instance}'. Pick a different path or remove that instance first."
- **Failure (instance required)**: "App '{app}' requires a concrete app instance selector."
- **Failure (other)**: "Failed to register app instance '{selector}'."

## Test Mapping

| Path | Coverage |
| --- | --- |
| `apps/cli/tests/Feature/Commands/App/AppWriteCommandTest.php` | Human registered/adopted/converged/partial output and warning payload prose. |

There is no gateway-side coverage for this command-local mapping: input handling and renderer behavior live in `apps/cli`. Gateway API behavior is mapped in the command contract file when a gateway-side surface exists.
