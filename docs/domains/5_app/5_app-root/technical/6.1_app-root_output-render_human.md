# Human Renderer: `orbit app:root`

[Back to technical contract](1_app-root.md)

**Renderer:** Human renderer.

## Primitive

- Progress: [Progress tree](../../../../ux/commands/progress/progress-tree.md)

## Selection

Use this renderer when `--json` is not present.

## Progress Tree

`app:root` performs remote application over SSH that may take longer than one
second, so the renderer emits a progress tree. The tree is rendered for both
the configuration-changing path and the converged no-op path so the operator can see
that artifacts were re-applied either way.

```text
┌ Updating App Root
○ Apply and verify root change
○ Apply PHP-FPM configuration
○ Apply proxy routes
└ Working...
```

The PHP-FPM step covers both writing the pool config and the reload required
to pick up the new document root; both are part of the FPM artifact step and
do not have separate progress lines.

## Human Strings

### Success — configuration changed

```text
SUCCESS: Document root for app 'docs' updated to 'public'.
Artifacts successfully re-applied on node 'app-01'.
```

### Success — converged no-op (configuration unchanged, artifacts re-applied)

```text
SUCCESS: Document root for app 'docs' is already 'public'.
Artifacts successfully re-applied on node 'app-01'.
```

If re-application found no observable change on the node, the second line
remains the same: a clean idempotent re-apply is still success.

### Success with Drift Warning

If gateway configuration was written but artifact re-application failed, the command
still succeeds (configuration was persisted) and reports the drift as a warning.

```text
SUCCESS: Document root for app 'docs' updated to 'public'.
WARNING: PHP-FPM configuration could not be re-applied on node 'app-01'.
  Code:  app.fpm_config_mismatch
  Next:  orbit doctor --fix --family=app --app=docs --restore
```

The warning code is the same singular `app.*` doctor code reported by
`doctor --family=app`; `app:root` does not invent its own drift vocabulary.

### Error: Invalid Root

```text
ERROR: The root path 'path/to/outside' resolves outside the application path.
  Resolved: /home/docs/app/../outside
  App path: /home/docs/app
```

### Error: App Not Found

```text
ERROR: Application 'missing-app' not found.
```

### Error: App-node caller denied

```text
This command may only be run from an operator or gateway node.
```

## Test Mapping

Primary test owners:

| Path | Coverage |
| --- | --- |
| `tests/Feature/Commands/Apps/AppRootCommandTest.php` | Human renderer selection, progress-tree rendering for both configuration-changed and converged paths, success prose for changed/converged variants, drift-warning prose with `app.fpm_config_mismatch`, and prose errors for `app.invalid_root`, `app.not_found`, and caller-role denial. |

Required missing-file coverage:

| Path | Coverage |
| --- | --- |
| `tests/Feature/Commands/Apps/AppRootHumanRendererTest.php` | Human renderer selection, exact progress-tree shape, success prose (changed and converged variants), drift-warning prose, and exact failure prose. |

Drift-warning prose includes the singular `app.*` doctor code and the
`next_command` line. Failure-prose coverage spans every error code, including
caller-role denial.
