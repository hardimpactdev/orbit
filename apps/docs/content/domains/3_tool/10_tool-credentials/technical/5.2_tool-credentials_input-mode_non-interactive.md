# Non-Interactive Input Mode: `orbit tool:credentials`

[Back to `tool-credentials` technical contract.](1_tool-credentials.md)

**Input mode:** Non-interactive.

This contract applies without a TTY or when `--json` is present. `--json` forces non-interactive input mode.

## Resolution Rules

No prompts are rendered. All required fields must be supplied by arguments, options, local defaults, or gateway-resolved context. Missing optional interactive selections fail as validation errors instead of rendering fallback prompts.

## Test Mapping

| Path | Coverage |
| --- | --- |
| `apps/cli/tests/Feature/Commands/Tool/ToolCredentialsCommandTest.php` | Non-interactive missing-tool failure, `--json` input-mode selection, no-prompt behavior, and validation error shape. |
There is no gateway-side coverage for this command-local mapping: input handling and renderer behavior live in `apps/cli`. Gateway API behavior is mapped in the command contract file when a gateway-side surface exists.
