# Non-Interactive Input Mode: `orbit deploy:step-add`

[Back to `deploy:step-add` technical contract.](1_deploy-step-add.md)

**Input mode:** Non-interactive.

Applies when the command is running without a TTY or when `--json` is present.
`--json` forces non-interactive input mode.

## Resolution Rules

Non-interactive input mode never render prompts and never waits for user input.
The app and command must be supplied before side effects begin. Missing app,
missing command, invalid order, invalid timeout, or invalid retention fails with
`validation_failed`.

## JSON Interaction

When `--json` is present, validation failures use the JSON renderer. No ANSI
progress output is emitted.

## Test Mapping

| Path | Coverage |
| --- | --- |
| `apps/cli/tests/Feature/Commands/Deploy/DeployWriteCommandsTest.php` | Non-interactive missing-command and invalid-timeout validation with `Http::assertNothingSent()`, and `--json` success envelope forwarding. |

There is no gateway-side coverage for this mapped surface; linked CLI tests cover only the mapped assertions above. Remaining behavior stays a coverage gap until focused tests land.

Coverage gaps until focused tests land: invalid `order` and `retention` validation, explicit non-TTY mode selection, and every documented non-interactive failure code.
