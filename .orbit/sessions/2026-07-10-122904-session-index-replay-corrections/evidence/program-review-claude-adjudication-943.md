# Whole-Program Review Adjudication — Claude 943 (Second Opinion)

Date: 2026-07-10
Inputs read: /tmp/program-review-index-semantics.md, /tmp/program-review-cli-core.md, /tmp/program-review-capture-archive.md, /tmp/session-index-replay-high-review-998.md, roadmap scratchpad 276 rev100 "2026-07-10 — User-requested high-model whole-program review", PRODUCT_DECISIONS.md (unchanged — no new direction touches any finding). Reviewer 998's named-set recomputation (22 deltas: 11 promotions + 9 clears + 2 raw-precedence, SHA-reconciled) independently closes the replay slice.

## Classifications

- **A — ACCEPT (P2).** Threshold met: independent PTY reproduction on main (`decorated=false tty=true supports=true`, 52 CSI sequences), named consumers at `StreamedStepTree.php:70` and `UpdateAllHumanProgressRenderer.php:957`, and reviewer 996 confirmed recorded output guidance already requires non-decorated output to be ANSI-free — this is applying recorded intent, the same contract family as the merged BufferedOutput fix, other quadrant.
- **B — ACCEPT (P1, highest repair priority).** Reproduced destructive escape: a validly-named destination symlink was traversed, `sentinel=deleted`, external victim replaced. Evidence-tool deletes external user data — worst class.
- **C — ACCEPT (P2).** Reproduced: refresh cleared the only coherent archive, then failed on an unreadable source file, leaving a partial. The capture-staging transaction contract was adjudicated precisely to prevent this shape; archive refresh must get the same pattern.
- **D — ACCEPT (P2).** Reproduced foreign `status: ok` for both providers with explicit wrong-cwd evidence in the transcript itself; `$cwd` received and unused. This is the Codex ownership contract's provider-parity gap, retained inside a slice that claimed exact-capture ownership.
- **E — ACCEPT (P2), fail-closed variant.** Reproduced: literal `not-json` manifest suppressed fallback and printed "validated staging." Where reviewer 997 left warn+fallback vs fail open, I adjudicate **fail** — see contract below.
- **F — ACCEPT (P2).** Reproduced: file symlink materialized external secret content into an archive that may be committed — confidentiality boundary, directly adjacent to the directory-only guard just merged.
- **Wrapper-path — TIGHTEN.** Two independent recurrences, deterministic trigger, cheap failure; meets the tighten bar but not a signal-record bar once the tool fix makes recurrence unreachable.
- Nothing is REJECT or DEFER at the finding level; deferred residuals are named under Risks.

## Sequencing: replay slice proceeds first

Merge the replay slice now through its exact-commit gates: reviewer 994 (pass, byte-preserved report, healthy capture) and high-reviewer 998 (pass, no P0-P3, independently recomputed named-set delta) both clear it; it *is* the fix for main's two index P2s; every A-F defect lives in different files and pre-dates it. Holding clean, twice-reviewed work behind unrelated repairs inverts the program's own serialization principle, and the rebaseline everyone needs waits on this merge plus the owner's primary regeneration (the `--check` staleness 998 saw is the documented expected pre-regeneration state).

## Smallest correction contracts

- **A (undecorated TTY):** split capability from permission — live repaint requires an *owned* TTY stream **and** `isDecorated()`; styling permission is `isDecorated()` alone; undecorated-on-TTY renders plain append-only with **zero** CSI. Proof: the 2×2 regression matrix (stream/non-stream × decorated/undecorated) under TTY and non-TTY, a PTY probe asserting zero CSI with `setDecorated(false)`, and — because this is `packages/core` `.php`, gate-classified topology-relevant — a retained-topology rerun of the prior dev-topology doctor pattern with one added undecorated/NO_COLOR capture beside the default-decorated no-regression capture. Pre-freeze condition: audit the four consumers for any reliance on the old decorated-non-TTY live fallback before fixing the matrix expectations.
- **B/C (archive containment + transaction):** canonicalize the configured archive home **once** at start (a symlinked home itself stays legal — resolve it; rejecting it would break linked archive homes), then lstat-reject symlinks at the destination and within, require generated/default destinations to be direct children of the resolved root, re-assert containment at the deletion site, and rebuild refresh as sibling-temp → backup-swap → rollback with the accepted callable seam for deterministic failure tests, preserving the double-failure nothing-deleted state. Refresh never clears the current final before a complete replacement exists.
- **E (manifest validity):** staged precedence requires every candidate manifest to parse as JSON with the recognized capture schema, provider/slug agreeing with its path, and a recognized status; `status: failed` manifests remain legitimate staged evidence (copied, waiver flow unchanged); `status: ok` requires the coherent artifact set. Any invalid manifest → the archive run **fails loudly** naming the exact path with repair-or-remove guidance. Warn+fallback is rejected: staged precedence exists to stop archive-time overwriting of lane-close evidence, so auto-falling-back both masks the corruption and re-runs exactly what staging was designed to preempt.
- **F (symlinks):** lstat/no-follow before any type check, source-type re-asserted at copy time; **source-side** symlinks (files or dirs, root or nested) are skipped with a recorded stderr + summary note so archives stay available without ever materializing external content; **destructive-side** symlinks (destination/root, item B) remain hard-fail. Fold the unchecked `writeJsonFile()` `file_put_contents` into the same slice — it is the checked-write family with a one-line fix and fixture.
- **D (Claude/Grok ownership):** provider-specific ownership evidence before cardinality, mirroring Codex exactly — Claude: exact normalized cwd from the provider-owned source (project-dir encoding and per-row `cwd`, verified in agreement when both exist) plus the standalone first-user-message identity marker; Grok: exact `prompt_context.json`/session-root cwd plus primary prompt identity. Wrong-cwd always excluded; full owners outrank partial; missing/malformed ownership evidence is partial-at-best when sole (manifest-visible basis) and ambiguous/missing otherwise — never a healthy singleton. Red fixtures for wrong-cwd and inherited-parent per provider must be derived from real session file shapes verified in-slice before the contract freezes.
- **Wrapper:** `bin/orbit-gateway-pest` detects a leading `apps/gateway/` on path arguments and strips it with a printed normalization note (deterministic, visible, and Pest still fails loudly on genuinely missing files), plus complete the existing skill sentence with "arguments are app-relative" — finishing existing prose, not duplicating it.

## Slice grouping and dependencies

**R1** `archive-integrity-hardening` = B+C+E+F+writeJsonFile (`bin/orbit-session-archive` + `SessionArchiveTest`) — dispatch immediately after the replay merge; P1 inside. **R2** `capture-provider-ownership-parity` = D (`bin/orbit-agent-session-capture` + `AgentSessionArchiveTest`). **R3** `live-repaint-decoration-permission` = A (`packages/core` + consumer audit + topology proof). **W** = wrapper (tiny). R1/R2/R3/W have disjoint ownership → parallel dispatch per the gate, serialized merges with R1 landing first; R1/R2/W are extensionless-bin + tests (quality-check evidence only per the classifier), R3 alone carries the retained-topology requirement. Rebaseline depends only on replay merge + primary regeneration, not on R1-R3.

## Tighten vs untouched

Tighten: the existing capture/archive containment-and-staging signal record — mark recurring and extend to both tools citing B-F as recurrence, one curated record, no new records; the two test suites gain the named fixture matrices; `implementing-features` gains only the wrapper clause. Untouched: HARNESS.md, review personas, LOOP.md.example, the accepted-adjudication guardrail routing (995 assessed the split as intentional policy routing), the cli-pest stdin surfaces, Codex ownership/floor semantics (floors are intentionally Codex-only with fresh-id/waiver recorded for other providers — 997 confirms), the Antigravity boundary, and PRODUCT_DECISIONS (zero product-direction impact anywhere in A-F).

## Risks

R3's capability/permission split may alter decorated-non-TTY static styling — the consumer audit gates that; D's contract depends on real provider file formats, so freeze it against real fixtures, not assumptions; E's fail-closed can block a cleanup boundary until staging is repaired — intended, but the failure message must name the operator path; deferred residuals recorded explicitly rather than silently: `spawned_processes` heuristic fallback without FK, parse-vs-copy TOCTOU, and descriptor-based no-follow hardening.

ADVICE: Merge the twice-passed replay slice now and regenerate the primary index, then dispatch four disjoint parallel repair slices — archive integrity (P1 B+C+E+F, fail-closed manifests, atomic refresh, no-follow copies), Claude/Grok ownership parity, core repaint capability-vs-permission with topology proof, and the wrapper-path normalization — merging serially with archive integrity first, extending one existing containment signal record instead of adding any, and rebaseline immediately after the replay merge.
