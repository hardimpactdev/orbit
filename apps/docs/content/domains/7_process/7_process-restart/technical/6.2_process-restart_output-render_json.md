# JSON Renderer: `orbit process:restart [name]`

[Back to `process:restart` technical contract.](1_process-restart.md)

This page describes the JSON-rendered output for `process:restart`.

**Renderer:** JSON renderer.

## Primitive

None. JSON output is emitted as a machine-readable envelope via `$this->line(...)`.

## Selection

Use this renderer when `--json` is present. `--json` also forces non-interactive input mode.

## Envelope

This renderer uses the shared [JSON Envelope](../../../README.md#json-envelope).

Success data contains the selected runtime contexts, runtime units, resulting runtime states, and durable events. The `runtimes` array contains exactly one item when `[name]` is supplied and every selected process in process order when `[name]` is omitted.

## Success Example

```json
{
  "success": {
    "data": {
      "runtimes": [
        {
          "process": "vite",
          "app": "docs",
          "workspace": "feature-docs",
          "runtime_unit": "orbit_docs_feature-docs_vite",
          "state": "running",
          "events": [
            {
              "type": "stopped"
            },
            {
              "type": "started"
            }
          ]
        }
      ]
    },
    "meta": []
  }
}
```

## Error Example

```json
{
  "error": {
    "code": "process.runtime_action_failed",
    "message": "The runtime unit could not be restarted.",
    "data": {
      "runtimes": [
        {
          "process": "vite",
          "app": "docs",
          "workspace": "feature-docs",
          "runtime_unit": "orbit_docs_feature-docs_vite",
          "state": "failed",
          "events": [],
          "message": "The process manager reported a restart failure."
        }
      ]
    },
    "meta": {
      "process": "vite",
      "partial_state": "none_restarted"
    }
  }
}
```

## Authorization Error

```json
{
  "error": {
    "code": "authorization_failed",
    "message": "This node is not authorized for 'process:restart' on 'app-1'.",
    "meta": {
      "reason": "missing_permission",
      "missing_permission": "process:restart",
      "serving_node": "app-1"
    }
  }
}
```

## Error Code Vocabulary

- `validation_failed`: node/app/workspace context is missing, invalid, or ambiguous in non-interactive input mode.
- `authorization_failed`: caller lacks `process:restart` on the resolved owning node.
- `process.not_found`: `[name]` is supplied and the named process does not exist for the resolved context.
- `process.none_configured`: `[name]` is omitted and the resolved context has no process definitions.
- `process.runtime_action_failed`: the gateway cannot restart the runtime unit on the owning node.

## Test Mapping

Primary test owners:

| Path | Coverage |
| --- | --- |
| `apps/cli/tests/Feature/Commands/Process/ProcessWriteCommandTest.php` | JSON process:restart success envelope, runtime action request forwarding, context validation failures, and gateway error passthrough. |

`ProcessWriteCommandTest.php` covers JSON success envelopes and
validation/gateway error passthrough for runtime action commands. Gateway API
controller tests own event shape, partial runtime failure data, and
authorization envelopes in the command contract file.

There is no gateway-side coverage for this command-local mapping: input handling and renderer behavior live in `apps/cli`. Gateway API behavior is mapped in the command contract file when a gateway-side surface exists.
