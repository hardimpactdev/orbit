# JSON Renderer: `orbit vpn-client:list`

[Back to `vpn-client:list` technical contract.](1_vpn-client-list.md)

**Renderer:** JSON renderer.

## Primitive

None. JSON output is emitted as a machine-readable envelope via `$this->line(...)`.

## Selection

Use this renderer when `--json` is present.

## Envelope

This renderer uses the shared [JSON Envelope](../../../README.md#json-envelope).

## Success Shape

```json
{
  "success": {
    "data": {
      "clients": [
        {
          "id": "client-1",
          "name": "laptop",
          "address": "10.6.0.7",
          "enabled": true,
          "latest_handshake_at": "2026-04-26T10:00:00Z",
          "kind": "admin"
        }
      ]
    },
    "meta": {
      "count": 1
    }
  }
}
```

## Error Shape

```json
{
  "error": {
    "code": "vpn_backend_auth_failed",
    "message": "VPN backend authentication failed.",
    "meta": {
      "reason": "totp_required"
    }
  }
}
```

| Field | Type | Meaning |
| --- | --- | --- |
| `success.data.clients[]` | array | Clients visible to Orbit on the active `vpn` role runtime backend. |
| `success.data.clients[].kind` | `admin`, `node`, `unknown` | Orbit classification of the backend peer. |
| `success.meta.count` | integer | Number of clients returned. |
| `error.code` | `vpn_runtime_unavailable`, `vpn_runtime_ssh_unavailable`, `authorization_failed`, `vpn_backend_unavailable`, `vpn_backend_auth_failed` | Stable command failure code. |
| `error.meta` | object | VPN runtime availability, VPN runtime SSH, authorization, backend, or authentication failure context. |

## Test Mapping

| Path | Coverage |
| --- | --- |
| `apps/cli/tests/Feature/Commands/Vpn/VpnCommandsTest.php` | JSON list success shape, empty list shape, metadata, and classified kind values for the covered error-code subset. |

There is no gateway-side coverage for this command-local mapping: input handling and renderer behavior live in `apps/cli`. Gateway API behavior is mapped in the command contract file when a gateway-side surface exists.

Documented `error.code` values named for coverage tracking are not exhaustively asserted by the linked CLI tests unless the rows above name them explicitly. Remaining renderer codes stay as coverage gaps until focused tests land.
