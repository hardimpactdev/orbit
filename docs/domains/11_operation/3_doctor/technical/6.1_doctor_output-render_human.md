# Human Renderer: `orbit doctor`

[Back to `doctor` technical contract.](1_doctor.md)

This page describes the human-rendered terminal output for `doctor`.

**Renderer:** Human renderer.

## Primitive

- Progress/result frame: doctor check-up frame.
- Inline category tables: [Table](../../../../ux/commands/lists/table.md)

## Selection

Use this renderer when `--json` is not present.

The human renderer does not own input prompting. `doctor` has optional filters
and fails invalid combinations before probes or side effects.

## Output Contract

Human output is an operator report, not a terse status line. It must make these
facts visible without requiring JSON:

- selected verify scope;
- resolved scope: target node, active-role category set, app, workspace, and
  whether `--self` resolved the calling peer's identified node;
- whether the final result is healthy;
- one visible row per checked category;
- live per-category state while checks run;
- issue and action details inline under the category that owns them;
- final summary prose for issue count and next action.

Healthy output must not imply that unselected categories, apps, or workspaces
were inspected. `doctor` always targets one node at a time, and the rendered
category set is derived from that target node's active roles.

## Category Catalog

The renderer maps canonical state families to operator-facing category labels.
The canonical family keys stay visible in JSON; human output uses the labels
below.

| Family key | Human category |
| --- | --- |
| `node` | `Node` |
| `app` | `Apps` |
| `workspace` | `Workspaces` |
| `process` | `Processes` |
| `proxy` | `Proxy routes` |
| `firewall_rule` | `Firewall` |
| `tool` | `Tools` |
| `schedule` | `Scheduling` |

`Node` is rendered singular because each run targets one node.

A future `DNS/TLD` row (operator/app targets) and `DNS` row (gateway target)
will be a renderer-only slice of `node` family probe results, pulling
resolver-specific findings into their own row. Until a DNS diagnostic source
lands, those facts stay inside the `Node` row and no separate DNS row is
emitted.

### Role-Aware Category Set

The visible rows are derived from the target node's active role assignments.
Family keys outside the active-role set are not probed and not rendered.

| Target role assignment state | Rendered categories |
| --- | --- |
| joined client with no active hosted role | `Node` |
| active `gateway` role | `Node` |
| active `database` role only | `Node`, `Tools` |
| active `app-development` or `app-production` role | `Node`, `Apps`, `Workspaces`, `Processes`, `Proxy routes`, `Firewall`, `Tools`, `Scheduling` |

When the target is the calling peer's identified node (default or `--self`),
that node's active roles drive the category set. When `--node=<other>` selects
a different node, the *target* node's active roles drive the category set; the
calling peer's role only governs who is allowed to ask. Narrow `--family`
scopes intersect with the active-role set; families outside that set are
rejected before probes.

## Framed Panel Contract

Human output is one framed panel. This single panel serves as both the
in-progress and final result display. The panel is re-rendered during progress
as each category completes, then replaced by the final result panel. The frame
uses a fixed-width terminal layout when possible:

- top border contains the spaced panel title;
- second divider contains the target line;
- category rows appear in active-role-derived order (see Category Catalog);
- issue/action tables appear inline immediately after the category row that owns
  them;
- final divider is `S U M M A R Y`;
- summary prose is centered when it fits.

The target line is:

```text
Performing check-up on <target>
```

for in-progress output, and:

```text
Successfully performed check-up on <target>
```

for final output when all selected probes completed. If a probe cannot complete,
use:

```text
Check-up on <target> could not complete
```

`<target>` is the resolved target node name. `doctor` always targets one
node per run.

## Progress Tree

The framed panel renders as a single unified display that updates or replaces during execution:

```
┌──────────────────────────────────────────┐
│  D O C T O R  [R E S U L T | I N G]     │
├──────────────────────────────────────────┤
│  [Performing | Successfully performed |  │
│   Check-up on <target>                   │
├──────────────────────────────────────────┤
│  ●  Family1         [Status]             │
│  ├─ [Optional issue table]               │
│  ├─ [Optional action table for resolution modes] │
│  ●  Family2         [Status]             │
│  ...                                     │
├──────────────────── S U M M A R Y ────────┤
│  [Summary prose centered when possible]  │
└──────────────────────────────────────────┘
```

The panel structure remains consistent across in-progress and final states; only the
status text and content rows update.

## In-Progress Panel

`doctor` may perform gateway, remote probe, restore, and adopt work, so the human renderer must show the in-progress panel after scope authorization and before probes or side effects begin. Each category row starts as `Queued`, then
updates in place:

| State | Status text |
| --- | --- |
| Queued | `Queued` |
| Probe running | `Checking` |
| Result aggregation | `Gathering results` |
| Healthy category | `OK` |
| Issue category | `<n> issue detected` or `<n> issues found` |
| Empty category | `Skipped, no <category data> configured` |
| Probe unavailable | `Unavailable, <reason>` |
| Action running | `Restoring` or `Adopting` |
| Action complete | `<n> restored`, `<n> adopted`, `<n> skipped`, or `<n> failed` |

The summary line updates as categories finish:

```text
<n> issues detected so far
```

When no completed category has issues yet, render:

```text
No issues detected so far
```

Example in-progress output for a operator-node target:

```text
┌─────────────────────────────  D O C T O R I N G  ────────────────────────────┐
│                                                                              │
├────────────────────────  Performing check-up on NMBP  ───────────────────────┤
│                                                                              │
●  Node           Checking                                                     │
│                                                                              │
├─────────────────────────────  S U M M A R Y  ────────────────────────────────┤
│                                                                              │
│                          Gathering doctor results                            │
│                                                                              │
└──────────────────────────────────────────────────────────────────────────────┘
```

Example in-progress output for an app-node target:

```text
┌─────────────────────────────  D O C T O R I N G  ────────────────────────────┐
│                                                                              │
├───────────────────────  Performing check-up on beast  ───────────────────────┤
│                                                                              │
●  Node           Checking                                                     │
│                                                                              │
●  Apps           Gathering results                                            │
│                                                                              │
●  Workspaces     2 issues found                                               │
│                                                                              │
●  Processes      Skipped, no processes configured                             │
│                                                                              │
●  Proxy routes   Checking                                                     │
│                                                                              │
●  Firewall       Checking                                                     │
│                                                                              │
●  Tools          Checking                                                     │
│                                                                              │
●  Scheduling     Checking                                                     │
│                                                                              │
├─────────────────────────────  S U M M A R Y  ────────────────────────────────┤
│                                                                              │
│                       3 issues detected so far                               │
│                                                                              │
└──────────────────────────────────────────────────────────────────────────────┘
```

The in-progress panel is not emitted for `--json`.

## Result Panel

The final human result is the same frame with title:

```text
D O C T O R  R E S U L T     (for verify mode)
D O C T O R  I N T E R A C T I V E     (for interactive mode)
D O C T O R  R E S T O R E     (for restore mode)
D O C T O R  A D O P T     (for adopt mode)
```

Final category statuses use:

| Final condition | Status text |
| --- | --- |
| Category checked and healthy | `OK` |
| Category has issues | `<n> issue detected` or `<n> issues found` |
| Category has no configured rows | `Skipped, no <category data> configured` |
| Category probe failed | `Unavailable, <reason>` |

When a category has issues, render its table directly below the category row.
Tables are compact and category-specific. They must not force every family into
the same generic shape when a clearer table is available.

Because every run targets one node, no issue table includes a `NODE` column.
The target node is already named in the panel header.

Preferred issue columns:

| Family | Columns |
| --- | --- |
| `node` | `Issue` |
| `app` | `APP`, `ISSUE` |
| `workspace` | `WORKSPACE`, `ISSUE` |
| `process` | `APP`, `PROCESS`, `ISSUE` |
| `proxy` | `DOMAIN`, `ISSUE` |
| `firewall_rule` | `RULE`, `ISSUE` |
| `tool` | `TOOL`, `ISSUE` |
| `schedule` | `SCHEDULE`, `ISSUE` |

The `Node` family table uses a single `Issue` column because every node row in
the run describes the one target node — the entity is already in the header.
Families that own multiple addressable entities on a single node (apps,
workspaces, processes, proxy routes, firewall rules, tools, schedules) keep the
entity column so operators can tell which app, workspace, or rule is affected.

Workspace names include the app suffix, such as `abc123.nckrtl.test`, so the
workspace table does not need a separate `APP` column. If a value does not
exist for a family, omit that column instead of rendering an empty column. If a
manual next step is present, render a `NEXT` column.

Action results are rendered inline below categories only when in resolution modes (interactive, restore, or adopt). Verify mode must not render action tables.

## Result with issues

Example result with issues for a operator-node target:

```text
┌─────────────────────────  D O C T O R  R E S U L T  ─────────────────────────┐
│                                                                              │
├────────────────  Successfully performed check-up on NMBP  ───────────────────┤
│                                                                              │
●  Node           1 issue detected                                             │
├──────────────────────────────────────────────────────────────────────────────┤
│ ISSUE                                                                        │
├──────────────────────────────────────────────────────────────────────────────┤
│ WireGuard peer for node NMBP is missing.                                     │
├──────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
├─────────────────────────────  S U M M A R Y  ────────────────────────────────┤
│                                                                              │
│                              1 issue detected                                │
│                                                                              │
│        Run doctor --fix manually or through an LLM to resolve issues         │
│                                                                              │
└──────────────────────────────────────────────────────────────────────────────┘
```

Example result with issues for an app-node target:

```text
┌─────────────────────────  D O C T O R  R E S U L T  ─────────────────────────┐
│                                                                              │
├────────────────  Successfully performed check-up on beast  ──────────────────┤
│                                                                              │
●  Node           OK                                                           │
│                                                                              │
●  Apps           1 issue detected                                             │
├────────┬─────────────────────────────────────────────────────────────────────┤
│ APP    │ ISSUE                                                               │
├────────┼─────────────────────────────────────────────────────────────────────┤
│ nckrtl │ https://nckrtl.test returned a 500 error response                   │
├────────┴─────────────────────────────────────────────────────────────────────┤
│                                                                              │
●  Workspaces     2 issues found                                               │
├───────────────────────────┬──────────────────────────────────────────────────┤
│ WORKSPACE                 │ ISSUE                                            │
├───────────────────────────┼──────────────────────────────────────────────────┤
│ abc123.nckrtl.test        │ Workspace should exist on node but is missing    │
├───────────────────────────┼──────────────────────────────────────────────────┤
│ ui-redesign.hauser.test   │ Workspace exists on node but is not expected     │
├───────────────────────────┴──────────────────────────────────────────────────┤
│                                                                              │
●  Processes      Skipped, no processes configured                             │
│                                                                              │
●  Proxy routes   OK                                                           │
│                                                                              │
●  Firewall       OK                                                           │
│                                                                              │
●  Tools          OK                                                           │
│                                                                              │
●  Scheduling     OK                                                           │
│                                                                              │
├─────────────────────────────  S U M M A R Y  ────────────────────────────────┤
│                                                                              │
│                            3 issues detected                                 │
│                                                                              │
│        Run doctor --fix manually or through an LLM to resolve issues         │
│                                                                              │
└──────────────────────────────────────────────────────────────────────────────┘
```

Issue categories sort by active-role-derived order. Within a category, sort by issue
kind order: `unverifiable`, `missing`, `divergent`, `extra`, then preserve probe
order.

For verify mode, the summary line when unhealthy is:

```text
<n> issue detected     (one issue)
<n> issues detected    (multiple issues)
```

The summary line never includes `across <m> categories`. Issue counts are
totals across the rendered categories for the targeted node.

The next-action line is shown when issues remain:

```text
Run doctor --fix manually or through an LLM to resolve issues
```

For resolution modes, the summary line depends on remaining issues:

```text
No issues remaining; <n> actions completed     (healthy)
<n> issues remaining                           (unhealthy)
<n> actions failed; review remaining issues    (failed actions)
```

Action verbs in the summary use `restored` for restore-mode actions and `adopted` for adopt-mode actions. Interactive mode mixes both verbs based on per-finding operator choice.

## Healthy Result

Example healthy result on a operator-node target:

```text
┌─────────────────────────  D O C T O R  R E S U L T  ─────────────────────────┐
│                                                                              │
├────────────────  Successfully performed check-up on NMBP  ───────────────────┤
│                                                                              │
●  Node           OK                                                           │
│                                                                              │
├─────────────────────────────  S U M M A R Y  ────────────────────────────────┤
│                                                                              │
│                            No issues detected                                │
│                                                                              │
└──────────────────────────────────────────────────────────────────────────────┘
```

Example healthy result on an app-node target:

```text
┌─────────────────────────  D O C T O R  R E S U L T  ─────────────────────────┐
│                                                                              │
├────────────────  Successfully performed check-up on beast  ──────────────────┤
│                                                                              │
●  Node           OK                                                           │
│                                                                              │
●  Apps           OK                                                           │
│                                                                              │
●  Workspaces     OK                                                           │
│                                                                              │
●  Processes      OK                                                           │
│                                                                              │
●  Proxy routes   OK                                                           │
│                                                                              │
●  Firewall       OK                                                           │
│                                                                              │
●  Tools          OK                                                           │
│                                                                              │
●  Scheduling     OK                                                           │
│                                                                              │
├─────────────────────────────  S U M M A R Y  ────────────────────────────────┤
│                                                                              │
│                            No issues detected                                │
│                                                                              │
└──────────────────────────────────────────────────────────────────────────────┘
```

Do not render a resolution instruction when no issues remain.

## Empty Scope Output

When the selected scope has no rows for a family, the category row remains
visible if that family was selected and uses a skip status:

```text
●  Apps           Skipped, no apps configured
```

An empty-scope row is informational and does not make the run unhealthy.

## JSON Boundary

The framed panel is human-only. JSON output keeps the existing
`success.data.doctor` / `error.data.doctor` payload. The renderer may derive
category status text and inline tables from the JSON payload, but it must not
change JSON field names to satisfy human layout needs.

## Failure Output

Validation failures, authorization failures, unsupported family selections, and
gateway connection failures occur before the framed panel can render. They keep
the shared human failure style from the command contract.

Probe failures inside a selected family render the framed result panel. The
category row status is `Unavailable, <reason>`, and the summary counts the
category as an issue category.

Action failures occur in `--fix` modes and are rendered inline below the owning
category in the same framed panel.

## Test Mapping

Required missing-file coverage:

| Path | Coverage |
| --- | --- |
| `tests/Feature/Commands/Operations/DoctorCommandContractTest.php` | Human renderer selection, framed panel structure, active-role category rendering, single `Issue` column for the node family, summary line without `across N categories`, restore/adopt action table emission, and the `doctor --fix` next-step prose. |
| `tests/Feature/Commands/Operations/DoctorRoleAwareCategoriesTest.php` | Singular `Node` label, drop of the `KEY` column from the node-family issue table, and active-role-driven category set in the rendered panel. |
