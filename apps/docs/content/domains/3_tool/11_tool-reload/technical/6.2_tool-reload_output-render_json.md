# JSON Renderer: `orbit tool:reload`

[Back to `tool-reload` technical contract.](1_tool-reload.md)

**Renderer:** JSON renderer.

## Primitive

None. JSON output is emitted as a single machine-readable envelope.

## Selection

Use this renderer when `--json` is present.

Use `--stream-json` instead of `--json` only when callers need
newline-delimited progress frames. That renderer follows the shared
[Stream JSON Frames](../../../README.md#stream-json-frames) contract.

## Envelope

This renderer uses the shared [JSON Envelope](../../../README.md#json-envelope).

## Success Shape

```json
{
  "success": {
    "data": {
      "tool": {
        "name": "caddy",
        "node": "app-1",
        "action": "reload",
        "runtime": "tool"
      }
    },
    "meta": []
  }
}
```

## Error Shape

```json
{
  "error": {
    "code": "tool.unsupported_action",
    "message": "Tool 'dns' does not support reload.",
    "meta": {
      "tool": "dns",
      "action": "reload"
    }
  }
}
```

| Field | Type | Meaning |
| --- | --- | --- |
| `error.code` | `validation_failed`, `gateway_unavailable`, `authorization_failed`, `orbit_agent_unavailable`, `tool.not_found`, `tool.unsupported_action`, `tool.unsupported_on_node`, `tool.runtime_missing`, `tool.runtime_ambiguous`, `tool.remote_action_failed` | Stable command failure code. |
| `error.message` | string | Human-readable failure summary. |
| `error.meta` | object | Field, target, caller, or runtime context for automation. |

`orbit_agent_unavailable` includes `error.meta.reason=agent_push_unavailable`
and `error.meta.platform`. On macOS the CLI also includes
`error.meta.remediation` instructing the operator to open Orbit Desktop.

## Test Mapping

| Path | Coverage |
| --- | --- |
| `apps/cli/tests/Feature/Commands/Tool/ToolWriteCommandTest.php` | Top-level success and error JSON envelopes, including exact `validation_failed` and `tool.unsupported_action` codes for reload. |
| `apps/cli/tests/Feature/GatewayApiClientTest.php` | Exact `gateway_unavailable` transport failure envelope. |
| `apps/gateway/tests/Feature/Http/Api/ToolLifecycleControllerTest.php` | Gateway JSON shape and exact `authorization_failed`, `node.agent_unreachable`, `tool.not_found`, `tool.unsupported_action`, `tool.unsupported_on_node`, and `tool.remote_action_failed` codes. |
| `apps/gateway/tests/Unit/Services/Tools/ToolCommandContractTest.php` | Stable shared failure construction, including exact `tool.runtime_missing` and `tool.runtime_ambiguous` codes and metadata. |

Documented `error.code` values named for coverage tracking: `validation_failed`, `gateway_unavailable`, `authorization_failed`, `orbit_agent_unavailable`, `tool.not_found`, `tool.unsupported_action`, `tool.unsupported_on_node`, `tool.runtime_missing`, `tool.runtime_ambiguous`, `tool.remote_action_failed`. Linked tests cover only the mapped assertions above; remaining codes stay as coverage gaps until focused tests land.
