# Human Renderer: `orbit php:list`

[Back to `php:list` technical contract.](1_php-list.md)

**Renderer:** Human renderer.

## Primitive

- Output table: [`Laravel\Prompts\table`](../../../../ux/commands/lists/table.md)
- Progress: [Progress tree](../../../../ux/commands/progress/progress-tree.md)

## Selection

Use this renderer when `--json` is not present.

## Progress Tree

Without `--live`, `php:list` is a gateway registry read. Execution is expected
to complete below one second and does not perform slow external work. No
progress tree is rendered.

With `--live`, render this progress tree after input resolution and before live
inspection begins:

```text
┌ Inspecting PHP Runtime
○ Resolve target
○ Read gateway configuration
○ Inspect available images
└ Working...
```

## Success Output

Human output presents a compact runtime table with:

- the resolved NODE;
- supported PHP versions;
- available PHP images on the selected instance, workspace-instance, or
  explicit target node;
- the selected CLI PHP version;
- the selected instance as `<app>.<instance>` with its own PHP version followed
  by the version its app template holds, when an instance context is resolved;
- workspace PHP runtime version when a workspace context is resolved.

## Failure Output

Human-rendered failures show the unresolved or unauthorized target and a concise
recovery hint, such as passing `--instance=<app.instance>`, `--workspace=<workspace>`, or
`--node=<node>`.

## Test Mapping

| Path | Coverage |
| --- | --- |
| `apps/cli/tests/Feature/Commands/Php/PhpListCommandTest.php` | Human table columns, the instance own version rendered beside the app template when the two diverge, workspace snapshot prose, em dash rendering, and absence of JSON envelopes in human mode. |

There is no gateway-side coverage for this command-local mapping: input handling and renderer behavior live in `apps/cli`. Gateway API behavior is mapped in the command contract file when a gateway-side surface exists.
