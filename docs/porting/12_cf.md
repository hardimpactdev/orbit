# 12_cf — Cloudflare Workstream

Detail file for the Cloudflare command family. Top-level command status lives
in [`PORTING.md`](PORTING.md). Doc authority: `docs/commands/12_cf/`.

## Status

Deferred until a Cloudflare test API key/account is available. This family
touches external provider state, and the command contracts should be verified
against a real test Cloudflare account instead of only local fakes. Resume this
workstream after `CLOUDFLARE_API_TOKEN` and the required test zone/record
fixtures are available for E2E.

## Commands

- [-] `cf-zone-list` — deferred pending Cloudflare test API key.
- [-] `cf-dns-list` — deferred pending Cloudflare test API key.
- [-] `cf-dns-add` — deferred pending Cloudflare test API key.
- [-] `cf-dns-remove` — deferred pending Cloudflare test API key.
- [-] `cf-cache-flush` — deferred pending Cloudflare test API key.
- [-] `cf-cache-rule-add` — deferred pending Cloudflare test API key.
- [-] `cf-cache-rule-remove` — deferred pending Cloudflare test API key.
- [-] `cf-ssl-enable` — deferred pending Cloudflare test API key.
- [-] `cf-ssl-disable` — deferred pending Cloudflare test API key.
