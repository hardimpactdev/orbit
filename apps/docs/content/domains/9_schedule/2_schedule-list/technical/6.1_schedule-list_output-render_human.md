# Human Renderer: `orbit schedule:list`

[Back to `schedule:list` technical contract.](1_schedule-list.md)

**Renderer:** Human terminal renderer.

## Primitive

- Output table: [`Laravel\Prompts\table`](../../../../ux/commands/lists/table.md)

## Selection

Use this renderer when `--json` is not present.

## Progress Tree

`schedule:list` is a read-only gateway registry query. Execution is expected to complete below one second and does not perform slow external work. No progress tree is rendered.

## Success Output

Render a table with one row per schedule.

| Column | Meaning |
| --- | --- |
| Name | Schedule slug. |
| Scope | `app`, `node`, or `orbit`. |
| Target | Concrete `app.instance`, node, or Orbit maintenance target. |
| Node | Target node. Non-gateway targets run through agent-push; gateway targets run locally. |
| Interval | Portable Orbit interval expression. |
| Execution | Command or script summary. |
| Last Run | Latest durable run status and timestamp, or `—`. |
| Status | Gateway-configuration status. |

Empty or missing cells render as `—` (em dash).

When no schedules match, render the empty state that names the selected filter:

```text
No schedules found.
No schedules found for app <app>.
No schedules found for node <node>.
```

## Failure Output

Validation, gateway, and authorization failures render one concise error line with the rejected field or selected scope when available.

## Test Mapping

| Path | Coverage |
| --- | --- |
| `apps/cli/tests/Feature/Commands/Schedule/ScheduleListCommandTest.php` | Human renderer selection, schedule table output, empty state output, last-run summary rendering, and human failure messages. |

There is no gateway-side coverage for this renderer: public command ownership
lives in `apps/cli`.
