# Technical Contract: `orbit workspace-setup-step:remove` (Human Renderer)

**Renderer:** Human.

## Primitive

None. The human renderer prints short prose/status lines only and does not render a table, spinner, or progress tree.

[Back to the canonical contract.](1_workspace-setup-step-remove.md)

## Progress Tree

No progress tree is rendered. Policy removal and order compaction perform a
single gateway database write and are expected to complete below one
second. No SSH or app-node calls are made.

## Output Strings

### Success

```text
✓ Removed setup step {id} from app '{app}'.
Remaining steps renumbered.
```

When the removed step was the only step for the app, render a hint:

```text
✓ Removed setup step {id} from app '{app}'.
! App '{app}' now has no workspace setup steps.
```

### Errors

Errors are rendered as prose messages drawn from the canonical
[Failure Semantics](1_workspace-setup-step-remove.md#failure-semantics).

| Scenario | Message |
| --- | --- |
| Step not found | `Setup step {id} not found for app '{app}'.` |
| App not found | `Could not resolve app: {app}` |
| Missing destructive consent | `This is a destructive operation. Use --force or confirm the prompt.` |
| Cancelled confirmation | `Operation cancelled.` |
| Validation (step) | `Step ID must be a positive integer.` |
| Validation (app) | `Could not resolve parent app. Pass --app=<slug> or run from inside a registered app or workspace path.` |
| Caller role not allowed | `This command may only be run from a control or gateway node.` |

## Next Steps

- List remaining steps:
  `orbit workspace-setup-step:list --app={app}`
- Inspect the surviving execution order:
  `orbit workspace-setup-step:list --app={app} --json`

## Test Mapping

| Path | Coverage |
| --- | --- |
| `tests/Feature/Commands/Workspaces/WorkspaceSetupStepRemoveCommandTest.php` | Human success-string fields (`✓ Removed setup step {id} from app '{app}'.` and the renumbering hint), the empty-list warning when the removed step was the only one, destructive consent prose, and prose error strings for `workspace.step_not_found`, `workspace.app_not_found`, `validation_failed` (`force`, `step`, and `app`), and `caller_role_not_allowed`. |
