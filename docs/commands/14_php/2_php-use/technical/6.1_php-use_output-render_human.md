# Human Renderer: `orbit php:use`

[Back to `php:use` technical contract.](1_php-use.md)

**Renderer:** Human renderer.

## Primitive

- Progress: [Progress tree](../../../ux/progress/progress-tree.md)

## Selection

Use this renderer when `--json` is not present.

## Progress Tree

Render this progress tree after input resolution and before side effects begin:

```text
┌ Updating PHP runtime to PHP 8.5
○ Resolve target
○ Validate version
○ Apply and verify PHP change
└ Working...
```

Use the resolved version and target scope in the title:

- app/workspace version change: `Updating PHP runtime to PHP <version>`;
- workspace inheritance: `Restoring workspace PHP inheritance`;
- node CLI version change: `Updating CLI PHP runtime to PHP <version>`.

Use this label lifecycle:

| Pending | Active | Completed |
| --- | --- | --- |
| `Resolve target` | `Resolving target` | `Resolved target` |
| `Validate version` | `Validating version` | `Validated version` |
| `Apply and verify PHP change` | `Applying and verifying PHP change` | `Applied and verified PHP change` |
| `Apply and verify CLI change` | `Applying and verifying CLI change` | `Applied and verified CLI change` |

When the command completes, replace the footer with the result:

```text
┌ Updating PHP runtime to PHP 8.5
● Resolved target
● Validated version
● Applied and verified PHP change
└ Successfully updated PHP runtime to PHP 8.5
```

For `--inherit`, the footer is
`Successfully restored workspace PHP inheritance`. For `--cli`, the apply step
uses the CLI labels above and the footer is
`Successfully updated CLI PHP runtime to PHP <version>`.

## Success Output

Human output states the target scope, node, previous version or inheritance
state, selected version, and whether gateway intent changed. When the command
clears a workspace override, the output states the inherited app version.

## Failure Output

Human-rendered failures show the target, field, and recovery command when
available:

- Install a missing PHP runtime with `orbit tool:install php --node=<node>`.
- Inspect available versions with `orbit php:list --node=<node>`.
- Repair partial app/workspace/node enactment with the doctor command named in
  the warning.
- Repair partial proxy backend enactment with
  `doctor --fix --family=proxy --restore`.

## Test Mapping

| Path | Coverage |
| --- | --- |
| `tests/Feature/Commands/Php/PhpUseHumanRendererTest.php` | Progress tree shape, scope-specific step labels, success prose for app/workspace/inherit/CLI targets, and failure recovery prose including proxy handoff warnings. |
