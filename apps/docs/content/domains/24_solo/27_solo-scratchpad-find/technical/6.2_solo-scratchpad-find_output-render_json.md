# JSON Renderer: `orbit solo:scratchpad:find`

[Back to `solo:scratchpad:find` technical contract.](1_solo-scratchpad-find.md)

**Renderer:** JSON renderer.

## Primitive

None. JSON output is emitted as a machine-readable envelope.

## Selection

Use this renderer for automation.

## Envelope

This renderer uses the shared [JSON Envelope](../../../README.md#json-envelope).

## Success

Success responses use a top-level `success` key:

```json
{
  "success": {
    "data": {},
    "meta": []
  }
}
```

The Solo payload is returned under `success.data`. Gateway or upstream metadata is returned under `success.meta` when present.

## Error

Error responses use a top-level `error` key:

```json
{
  "error": {
    "code": "extension_disabled",
    "message": "The Solo extension is not enabled.",
    "meta": {
      "extension": "solo"
    }
  }
}
```

Stable error codes include `extension_disabled`, `authorization_failed`, `validation_failed`, and `solo_upstream_unavailable`.

## Test Mapping

| Path | Coverage |
| --- | --- |
| `apps/cli/tests/Feature/Commands/Solo/SoloReadOnlyCommandTest.php` | JSON envelopes for read-only Solo commands. |
| `apps/cli/tests/Feature/Commands/Solo/SoloMutatingCommandTest.php` | JSON envelopes for mutating Solo commands. |
| `apps/gateway/tests/Feature/Http/Api/SoloProxyControllerTest.php` | Gateway JSON envelope shape forwarded by CLI commands. |