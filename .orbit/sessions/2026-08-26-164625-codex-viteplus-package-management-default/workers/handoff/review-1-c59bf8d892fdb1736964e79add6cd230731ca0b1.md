candidate=c59bf8d892fdb1736964e79add6cd230731ca0b1

CHECKOUT_PROOF: cwd=/Users/nckrtl/orbit/.worktrees/codex-viteplus-package-management-default; branch=codex/viteplus-package-management-default; head=c59bf8d892fdb1736964e79add6cd230731ca0b1; main=dc6dade995d3126ba4bfb697a0b13e0dff01df4f; status=clean

## DEFECT findings

1. `vp run` does not need npm's extra `--` separator. A narrow reviewer probe
   showed that the separator becomes a real child argument. Affected refs:
   `bin/orbit-build-desktop-bundle:46,72`,
   `apps/docs/content/domains/7_process/README.md:291`,
   `apps/docs/content/domains/7_process/1_process-add/technical/1_process-add.md:92`,
   `apps/docs/content/domains/3_tool/catalog/viteplus.md:36`, and
   `apps/gateway/tests/Feature/E2ESupport/NativeReleaseAssetsBuilderTest.php:170`.
   Drop the separator and update tests.
2. `VitePlusTool` overwrites and unconditionally removes generic
   `/usr/local/bin/node`, `npm`, and `npx` paths. Refs:
   `apps/gateway/app/Tools/VitePlusTool.php:12,60,112-118` and
   `apps/gateway/app/Services/Nodes/Roles/NodeRoleRegistry.php:143`. Relink and
   remove only absent paths or symlinks that resolve inside the managed Vite+
   home. Never delete an unrelated Homebrew/system Node installation.
3. Production app commands run as a per-app user, but Vite+ is installed under
   the managed node user's mode-0750 home. Refs:
   `apps/gateway/app/Services/Apps/AppRuntimeUser.php:47`,
   `apps/gateway/app/Services/Apps/AppCommandRouter.php:178`, and
   `apps/gateway/app/Tools/VitePlusTool.php:33-37`. The retained proof only ran
   as `orbit`; a direct non-managed runtime-user probe confirmed `vp: command
   not found` through the inaccessible symlink. Correct the secure runtime-user
   design and prove `vp install` plus `vp run` as a non-managed app-prod user.
4. `bin/orbit-build-desktop-bundle` uses `vp`, but the deliberately deferred
   `apps/macos/package.json` declaration is absent. Refs:
   `bin/orbit-build-desktop-bundle:45-46`, `apps/macos/package.json`, and
   `NativeReleaseAssetsBuilderTest.php:361-367`. Revert the desktop bundle to
   npm for this candidate; move the `vp` conversion with the macOS declaration
   into the later host-macOS candidate.

## POLISH findings

- Strengthen `ToolsFixerTest.php:523` to assert the Vite+ install script marker.
- Move root `AGENTS.md:155,248` Vite+ guidance out of the generated Boost block
  into Orbit-owned guidance.
- Preserve frozen semantics in `packages/sdk-typescript/README.md:209`.
- At `bin/orbit-prepare-worktree:339`, ensure dev dependencies remain installed
  even if `NODE_ENV=production` leaks into the environment.
- Add the current `.local/share/vite-plus/bin` layout to
  `AppCommandRouter.php:195`.
- Add `#[\Override]` to VitePlusTool install/update/remove script methods.

BLAST_RADIUS: gaps
HUMAN_JUDGMENT: not-required
VERDICT: FIX
