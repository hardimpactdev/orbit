# JSON Renderer: `orbit proxy:add`

[Back to `proxy:add` technical contract.](1_proxy-add.md)

**Renderer:** JSON renderer.

## Primitive

None. JSON output is emitted as a machine-readable envelope via `$this->line(...)`.

## Selection

Use this renderer when `--json` is present.

## Envelope

This renderer uses the shared [JSON Envelope](../../../README.md#json-envelope).

## Success Shape

```json
{
  "success": {
    "data": {
      "route": {
        "domain": "vite.docs.test",
        "kind": "proxy",
        "owner": {
          "type": "custom",
          "name": null
        },
        "node": "app-1",
        "target": {
          "type": "upstream",
          "value": "http://127.0.0.1:5173"
        },
        "redirect_code": null,
        "tls": {
          "managed_by": "orbit",
          "trusted_by_gateway_ca": true
        },
        "status": "enacted"
      }
    },
    "meta": {
      "action": "created"
    }
  }
}
```

Redirect success shape:

```json
{
  "success": {
    "data": {
      "route": {
        "domain": "redirect.docs.test",
        "kind": "redirect",
        "owner": {
          "type": "custom",
          "name": null
        },
        "node": "app-1",
        "target": {
          "type": "redirect",
          "value": "https://docs.test"
        },
        "redirect_code": 302,
        "tls": {
          "managed_by": "orbit",
          "trusted_by_gateway_ca": true
        },
        "status": "enacted"
      }
    },
    "meta": {
      "action": "created"
    }
  }
}
```

## Error Shape

```json
{
  "error": {
    "code": "proxy.enactment_failed",
    "message": "The proxy route was saved, but backend apply failed.",
    "data": {
      "route": {
        "domain": "vite.docs.test",
        "kind": "proxy",
        "owner": {
          "type": "custom",
          "name": null
        },
        "node": "app-1",
        "target": {
          "type": "upstream",
          "value": "http://127.0.0.1:5173"
        },
        "redirect_code": null,
        "tls": {
          "managed_by": "orbit",
          "trusted_by_gateway_ca": true
        },
        "status": "expected"
      }
    },
    "meta": {
      "next_command": "doctor --family=proxy --restore --node [--node-transport=<transport>]=app-1"
    }
  }
}
```

| Field | Type | Meaning |
| --- | --- | --- |
| `success.data.route` | object | Created or updated custom proxy route. |
| `success.meta.action` | `created`, `updated`, or `converged` | Command outcome. |
| `error.code` | `validation_failed`, `gateway_unavailable`, `authorization_failed`, `proxy.domain_conflict`, `proxy.replacement_consent_required`, `proxy.enactment_failed` | Stable command failure code. |
| `error.data.route` | object | Proxy route configuration when a post-configuration apply failure occurred. |
| `error.meta` | object | Field, node, caller, conflicts, or recovery context for automation. |

## Test Mapping

| Path | Coverage |
| --- | --- |
| `apps/cli/tests/Feature/Commands/Proxy/ProxyWriteCommandTest.php` | JSON `proxy:add` upstream success payloads, local validation envelopes, and gateway error passthrough. |


Documented `error.code` values named for coverage tracking: `validation_failed`, `gateway_unavailable`, `authorization_failed`, `proxy.domain_conflict`, `proxy.replacement_consent_required`, and `proxy.enactment_failed`.

There is no gateway-side coverage for this command-local mapping: input handling and renderer behavior live in `apps/cli`. Gateway API behavior is mapped in the command contract file when a gateway-side surface exists.
