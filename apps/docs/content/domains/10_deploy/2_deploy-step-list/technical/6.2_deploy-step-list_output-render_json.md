# JSON Renderer: `orbit deploy:step-list`

[Back to `deploy:step-list` technical contract.](1_deploy-step-list.md)

**Renderer:** JSON renderer.

## Primitive

None. JSON output is emitted as a machine-readable envelope via `$this->line(...)`.

## Selection

Use this renderer when `--json` is present.

## Envelope

This renderer uses the shared [JSON Envelope](../../../README.md#json-envelope).

## Success Shape

```json
{
  "success": {
    "data": {
      "steps": [
        {
          "id": 12,
          "app": "docs",
          "title": "Pull latest",
          "command": "git pull origin main",
          "order": 1,
          "timeout_seconds": 600,
          "retention": null
        }
      ]
    },
    "meta": {
      "app": "docs",
      "count": 1
    }
  }
}
```

## Error Shape

```json
{
  "error": {
    "code": "app.not_found",
    "message": "App 'docs' was not found.",
    "meta": {
      "app": "docs"
    }
  }
}
```

| Field | Type | Meaning |
| --- | --- | --- |
| `success.data.steps[]` | array | Deploy step entities visible for the selected app. |
| `success.meta.app` | string | Selected production app. |
| `success.meta.count` | integer | Number of returned steps. |
| `error.code` | `validation_failed`, `gateway_unavailable`, `authorization_failed`, `app.not_found`, `deploy.production_app_required` | Stable command failure code. |
| `error.meta` | object | Field, app selector, app environment, missing permission, or authorization context for automation. |

## Test Mapping

| Path | Coverage |
| --- | --- |
| `apps/gateway/tests/Feature/Commands/Deploy/DeployStepListJsonRendererTest.php` | JSON renderer selection, ordered step entity shape including timeout metadata, empty list shape, metadata, and every `error.code` value documented in this renderer. |
