# JSON Renderer: `orbit firewall:deny`

[Back to `firewall-deny` technical contract.](1_firewall-deny.md)

**Renderer:** JSON renderer.

## Primitive

None. JSON output is emitted as a machine-readable envelope via `$this->line(...)`.

## Selection

Use this renderer when `--json` is present.

## Envelope

This renderer uses the shared [JSON Envelope](../../../README.md#json-envelope).

## Success Shape

```json
{
  "success": {
    "data": {
      "rule": {
        "name": "block-vite",
        "node": "app-1",
        "direction": "incoming",
        "action": "deny",
        "source": "0.0.0.0/0",
        "destination": null,
        "port": 5173,
        "protocol": "tcp",
        "address_family": "v4",
        "interface_scope": null,
        "owner": "user",
        "protected": false,
        "reason": "not exposed publicly",
        "status": "enacted"
      }
    },
    "meta": []
  }
}
```

## Error Shape

```json
{
  "error": {
    "code": "firewall_rule.enactment_failed",
    "message": "The firewall rule was saved, but the backend apply failed.",
    "data": {
      "rule": {
        "name": "block-vite",
        "node": "app-1",
        "direction": "incoming",
        "action": "deny",
        "source": "0.0.0.0/0",
        "destination": null,
        "port": 5173,
        "protocol": "tcp",
        "address_family": "v4",
        "interface_scope": null,
        "owner": "user",
        "protected": false,
        "reason": "not exposed publicly",
        "status": "expected"
      }
    },
    "meta": {
      "next_command": "doctor --family=firewall_rule --restore --node=app-1"
    }
  }
}
```

| Field | Type | Meaning |
| --- | --- | --- |
| `error.code` | `validation_failed`, `gateway_unavailable`, `authorization_failed`, `firewall_rule.name_collision`, `firewall_rule.baseline_conflict`, `firewall_rule.enactment_failed` | Stable command failure code. |
| `error.message` | string | Human-readable failure summary. |
| `error.data.rule` | object | Firewall rule configuration when a post-write apply failure occurred. |
| `error.meta` | object | Field, node, caller, conflicts, or recovery context for automation. |

## Test Mapping

| Path | Coverage |
| --- | --- |
| `apps/cli/tests/Feature/Commands/Firewall/FirewallWriteCommandTest.php` | JSON `firewall:deny` POST payload forwarding, local default node resolution, success rule envelope, and `backend_enacted` metadata. |


Documented `error.code` values named for coverage tracking: `validation_failed`, `gateway_unavailable`, `authorization_failed`, `firewall_rule.name_collision`, `firewall_rule.baseline_conflict`, and `firewall_rule.enactment_failed`. Linked tests cover only the mapped assertions above; remaining assertions for renderer codes and partial-apply `error.data.rule` placement stay as coverage gaps until focused tests land.

There is no gateway-side coverage for this command-local mapping: input handling and renderer behavior live in `apps/cli`. Gateway API behavior is mapped in the command contract file when a gateway-side surface exists.
