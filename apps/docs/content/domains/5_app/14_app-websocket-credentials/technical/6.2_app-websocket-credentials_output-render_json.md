# JSON Renderer: `orbit app:websocket credentials`

[Back to `app:websocket credentials` technical contract.](1_app-websocket-credentials.md)

This page describes the JSON-rendered output for `app:websocket credentials`.

**Renderer:** JSON renderer.

## Primitive

None. JSON output is emitted as a machine-readable envelope via `$this->line(...)`.

## Selection

Use this renderer when `--json` is present.

## Envelope

This renderer uses the shared [JSON Envelope](../../../README.md#json-envelope).

The credentials object is returned under `success.data.credentials`.

## Success Shape

```json
{
  "success": {
    "data": {
      "credentials": {
        "app": "docs",
        "internal_host": "websocket.orbit",
        "public_hosts": ["ws.docs.example.com"],
        "allowed_origins": ["https://docs.example.com"],
        "reverb_app_id": "docs",
        "reverb_app_key": "abcdef1234567890abcdef1234567890",
        "reverb_app_secret": "abcdef1234567890abcdef1234567890abcdef1234567890"
      }
    },
    "meta": []
  }
}
```

| Field | Type | Meaning |
| --- | --- | --- |
| `credentials` | object | Full Reverb connection details for the app. |
| `credentials.app` | string | App identity slug. |
| `credentials.internal_host` | string | WireGuard-internal WebSocket service hostname. Always `websocket.orbit`. |
| `credentials.public_hosts` | array | Public WebSocket hostnames bound to this app. |
| `credentials.allowed_origins` | array | Origins permitted by Reverb for this app. |
| `credentials.reverb_app_id` | string | Reverb application identifier. Equal to the app name. |
| `credentials.reverb_app_key` | string | Reverb application key. 32 characters. |
| `credentials.reverb_app_secret` | string | Reverb application secret. 48 characters. Treat as a secret value. |

## Error Shapes

### App not found

```json
{
  "error": {
    "code": "app.not_found",
    "message": "App 'docs' not found.",
    "meta": {
      "app": "docs"
    }
  }
}
```

### Validation failed

```json
{
  "error": {
    "code": "validation_failed",
    "message": "App is required.",
    "meta": {
      "field": "app"
    }
  }
}
```

### WebSocket binding missing

```json
{
  "error": {
    "code": "websocket.binding_missing",
    "message": "App 'docs' does not have an enabled websocket binding.",
    "meta": {
      "app": "docs"
    }
  }
}
```

### Authorization failed

```json
{
  "error": {
    "code": "authorization_failed",
    "message": "This node is not authorized for 'app:credentials' on 'app-1'.",
    "meta": {
      "missing_permission": "app:credentials",
      "serving_node": "app-1"
    }
  }
}
```

## Test Mapping

| Path | Coverage |
| --- | --- |
| `apps/gateway/tests/Feature/Http/Api/AppWebSocketControllerTest.php` | JSON envelope, credentials payload shape, `reverb_app_key` and `reverb_app_secret` fields, `websocket.binding_missing` for absent and disabled bindings, `app.not_found`, and authorization failure mapping. |
