candidate=42c1708d262051a0ee03a8169928440ab6fb1818

VERDICT: PASS
HUMAN_JUDGMENT: not-required
REVIEWED SHA: 42c1708d262051a0ee03a8169928440ab6fb1818 (clean worktree, HEAD == candidate)

Checkout proof:
- git rev-parse HEAD = 42c1708d262051a0ee03a8169928440ab6fb1818
- git status --porcelain = empty
- git log main..HEAD = single commit 42c1708d2
- git diff --stat main...HEAD = apps/agent/src/http.rs +70/-3, apps/agent/src/lib.rs +5, apps/agent/src/lifetime.rs +159 (new)

Independent re-verification at the reviewed SHA:
- apps/agent cargo fmt --check: clean
- apps/agent cargo clippy --all-targets -- -D warnings: clean
- apps/agent cargo test: 67 passed / 0 failed, including all 9 new tests
  (lifetime::tests x8 and http::tests::desktop_lifetime_eof_shuts_down_http_listeners)
- receipt .orbit/quality-gates/quality-check-2026-08-23T172331Z-7e5f1f130f74.json:
  exit_code=0, git.commit=42c1708d26..., git.dirty=false, all 46 subgates 0

CLAIM 1 - desktop mode is enabled only by the exact intended marker: HOLDS
lifetime.rs:53-55 gates on matches!(value.map(str::trim), Some("1")) over
ORBIT_DESKTOP_LIFETIME. desktop_lifetime_requires_explicit_marker rejects None,
"", "0", "true", "TRUE", "yes" and accepts only "1"/" 1 ". An unrecognized or
absent value falls back to standalone, so an invalid channel setup fails closed
without changing standalone semantics.

CLAIM 2 - EOF or a non-interrupt stdin error cannot be lost before the HTTP
service starts waiting for cancellation: HOLDS
Checked against the vendored tokio 1.52.3 Notify implementation, not assumed.
request() stores the AtomicBool SeqCst and then calls notify_waiters(), which
increments a generation counter (notify.rs:755,761). cancelled() creates
notify.notified() first, snapshotting that generation (notify.rs:565-574), and
only then reads the flag; poll_notified returns Ready immediately when the
generation moved since creation (notify.rs:1124 and 1156). Consequence: if the
flag read observes false, the generation increment necessarily post-dates the
snapshot, so the first poll of the awaited future completes at once. Both the
store-before-notify order in request() and the notified-before-check order in
cancelled() are required for this and both are present. The surrounding loop
also re-checks the flag after any wakeup. Note that Notified::enable() is not
needed here because notify_waiters() uses the generation counter rather than a
stored permit. Behaviourally confirmed by
cancelled_returns_when_request_happens_before_wait,
eof_on_lifetime_reader_requests_shutdown,
reader_error_fails_closed_and_requests_shutdown, and the retained runtime proof.
watch_reader_until_eof returns on Ok(0) and on any non-Interrupted error, and
retries only ErrorKind::Interrupted, so both stop paths reach request().

CLAIM 3 - standalone macOS and Linux behavior remains unchanged: HOLDS
http.rs:67-69 spawns the stdin watcher only when the marker is set. Without it,
nothing reads stdin, cancelled() never resolves, and with_graceful_shutdown is
inert, so axum::serve behaves as before. No blocking-pool thread is occupied in
standalone. Runtime evidence: standalone Agent reported health ok after stdin
EOF, stayed alive (pid 2011), and required a signal to stop.

CLAIM 4 - shutdown covers both listeners without changing authentication,
polling, or listener identity: HOLDS
The diff touches only the two axum::serve call sites; both command_service and
local_status_service receive with_graceful_shutdown and tokio::try_join! still
awaits both. command_bind_addr, local_status_bind_addr and
command_app_with_authorizer(Arc::new(GatewayCommandAuthorizer::new(config)))
are untouched, and no auth, envelope, execution, or polling code changed.
Evidence confirms the unchanged pair 10.232.1.164:9477 and 127.0.0.1:9477.
Shutdown is graceful, so exit is bounded by the existing per-command timeouts
rather than immediate; that is the stated transition and the hard-kill
escalation belongs to the later Desktop supervisor.

CLAIM 5 - the retained proof binds to the exact candidate and exercises the
claimed final behavior: HOLDS
.orbit/evidence/agent-desktop-lifetime-incus.txt records
candidate_sha=42c1708d262051a0ee03a8169928440ab6fb1818 matching HEAD and the
receipt git.commit, plus binary sha256, instance orbit-e2e-lt42c170-dev,
lifetime.rs=present, and a one-way parent-write-end pipe with child fd0 flags=00
O_RDONLY. It shows desktop_alive_while_open=yes with health ok, then
desktop_exited_after_eof=yes exit=0 with health unreachable, and the standalone
counterpart healthy and alive after stdin EOF. Both loop transitions are
exercised end to end.

CLAIM 6 - blast-radius closure and no hidden downstream or release change:
COMPLETE, no gaps.

BLAST RADIUS: complete - evidence=repository-wide searches and file inspection
at the reviewed SHA; result=the new cross-app ownership boundary is fully
contained.
- rg -n "DESKTOP_LIFETIME" --hidden -g '!.git' returns 3 hits, all in
  apps/agent/src/lifetime.rs and apps/agent/src/lib.rs. No producer exists in
  gateway, CLI, SDK, core, reverb, docs, bin/, provisioning, or release paths.
- Linux node Agent units set only ORBIT_AGENT_CONFIG, ORBIT_AGENT_HTTP_BIND and
  ORBIT_AGENT_ORBIT_BINARY (packages/core/src/Nodes/NodeSystemdServiceRenderer.php
  :31-34), and systemd does not inherit operator shell environment, so Linux
  Agents cannot enter desktop mode.
- Orbit Desktop today runs the Agent in-process
  (apps/macos/src/main.rs:138 thread::spawn(orbit_agent::run_agent_service_blocking))
  and sets no marker, so its behavior is unchanged; macos_cargo_check,
  macos_cargo_clippy, macos_cargo_fmt and macos_cargo_test are all 0 in the receipt.
- No release, manifest, artifact, CLI exit-code, output-contract, SDK or gateway
  schema surface is touched.
- Docs need no change. The observable contract is already recorded in
  apps/docs/content/architecture.md:486-493 ("supervises one Agent child, and
  quitting the desktop app stops that Agent"), apps/docs/content/tech-stack.md:463,
  and the 2026-08-23 PRODUCT_DECISIONS.md entry, which also states that the
  native supervisor lands in a separate apps/macos slice. Internal Agent
  environment variables are not enumerated in apps/docs/content/ by existing
  convention: ORBIT_AGENT_CONFIG, ORBIT_AGENT_HTTP_BIND and
  ORBIT_AGENT_ORBIT_BINARY appear nowhere there either, so omitting
  ORBIT_DESKTOP_LIFETIME is consistent rather than drift.

Constraints for the later apps/macos supervisor slice (not defects in this
slice, recorded so the contract is not misread):
- The producer must spawn the Agent as a child process with its own stdin pipe.
  Setting ORBIT_DESKTOP_LIFETIME=1 in the Desktop process while the Agent still
  runs in-process on a thread would make the embedded Agent consume Desktop's own
  stdin, which is /dev/null for a bundled .app and would shut the embedded Agent
  down immediately and silently.
- Consume orbit_agent::DESKTOP_LIFETIME_ENV rather than re-spelling the literal.
- The supervisor owns the hard-kill escalation after graceful shutdown.

Non-blocking observation: watch_reader_until_eof discards any bytes it reads,
which is correct for a one-way lifetime channel but means the channel cannot
carry data later without a change.
