# Interactive Input Mode: `orbit cf-cache:flush`

[Back to `cf-cache:flush` technical contract.](1_cf-cache-flush.md)

**Input mode:** Interactive.

## Selection

Use this input mode only when the process has a TTY and `--json` is not
present.

## Prompt Behavior

When `--zone` is absent, render this prompt before provider side effects:

| Prompt ID | Primitive | Label | Placeholder |
| --- | --- | --- | --- |
| `cf_cache_flush_zone` | `search` | `Select Cloudflare zone` | `Type to search zones` |

The searchable option labels include the Cloudflare zone name, provider status,
and up to two Orbit instance-owned domains that reference the zone. If Cloudflare returns
no zones, fail before side effects.

## Primitive References

- [`search`](../../../../ux/commands/inputs/search-prompt.md)

## Test Mapping

| Path | Coverage |
| --- | --- |
| `apps/cli/tests/Feature/Commands/Cloudflare/CloudflareWriteCommandsTest.php` | Interactive zone prompt and validation before gateway contact. |

There is no gateway-side coverage for this command-local mapping: input handling and renderer behavior live in `apps/cli`. Gateway API behavior is mapped in the command contract file when a gateway-side surface exists.