# Technical Contract: `orbit workspace-setup-step:remove` (JSON Renderer)

**Renderer:** JSON.

[Back to the canonical contract.](1_workspace-setup-step-remove.md)

## Envelope

This renderer uses the shared [JSON Envelope](../../../README.md#json-envelope).

## Success Envelope

```json
{
  "success": {
    "data": {
      "result": {
        "action": "removed"
      },
      "step": {
        "id": 12,
        "app": "docs",
        "phase": "setup",
        "order": 2,
        "command": "composer install",
        "timeout_seconds": 600
      }
    },
    "meta": {
      "remaining_step_count": 3
    }
  }
}
```

### Fields

- `result.action`: Literal `"removed"`. The only outcome verb for this
  command. `workspace-setup-step:remove` is imperative and never converges
  toward an absent state; an already-absent target is a hard validation
  failure, not a silent no-op.
- `step.id`: The original numeric registry identifier of the removed
  record. Captured before the delete; it is no longer in the gateway
  registry by the time the response is rendered.
- `step.app`: The resolved parent app slug.
- `step.phase`: Literal `"setup"`. The discriminator that separates this
  policy list from teardown steps.
- `step.order`: The original `order` position the removed step occupied
  before compaction.
- `step.command`: The shell command string the removed step held. Captured
  before the delete.
- `step.timeout_seconds`: The strict positive integer timeout the removed
  step held. Captured before the delete.
- `meta.remaining_step_count`: The number of surviving setup-step records
  for `(app, phase=setup)` after the delete and order compaction. The
  surviving sequence is contiguous from `1` to `remaining_step_count`.

`workspace-setup-step:remove` does not emit non-fatal warnings. Drift
between gateway intent and node reality is reported by
[`doctor --family=workspace`](../../workspace-doctor.md), not by this
write command. Past executions of the removed step remain in
[`workspace:history`](../../6_workspace-history/workspace-history.md);
this command does not mutate that history and does not report on it.

## Error Envelope

### Step not found

Returned when the supplied `--step` does not match an existing setup-step
record for the resolved `(app, phase=setup)`. Already-absent removal is
not idempotent — this is the imperative validation failure shape.

```json
{
  "error": {
    "code": "workspace.step_not_found",
    "message": "Setup step '12' not found for app 'docs' in phase 'setup'.",
    "meta": {
      "step_id": 12,
      "app": "docs",
      "phase": "setup"
    }
  }
}
```

### Validation failure (step)

Returned when `--step` is missing in non-interactive mode or is not a
strict positive integer.

```json
{
  "error": {
    "code": "validation_failed",
    "message": "Step ID must be a positive integer.",
    "meta": {
      "field": "step",
      "reason": "must_be_positive_integer"
    }
  }
}
```

### Validation failure (app)

Returned when the parent app cannot be resolved from `--app`,
`.orbit/config`, or gateway path-ownership lookup, and prompting is
disabled (non-interactive mode).

```json
{
  "error": {
    "code": "validation_failed",
    "message": "Could not resolve parent app.",
    "meta": {
      "field": "app",
      "reason": "missing_required_input"
    }
  }
}
```

### App not found

```json
{
  "error": {
    "code": "workspace.app_not_found",
    "message": "App 'docs' not found.",
    "meta": {
      "app": "docs"
    }
  }
}
```

### Missing destructive consent

Returned in non-interactive input mode when `--force` is absent, and when
the operator declines the interactive confirmation prompt. `--json` never
implies `--force`.

```json
{
  "error": {
    "code": "validation_failed",
    "message": "This is a destructive operation. Use --force or confirm the prompt.",
    "meta": {
      "field": "force"
    }
  }
}
```

## Common Error Codes

| Code | Meaning |
| --- | --- |
| `validation_failed` | A supplied input failed gateway-side static validation (missing or non-positive `--step`, unresolved parent app, missing destructive consent, or cancelled confirmation). `error.meta.field` names the offending input (`step`, `app`, or `force`). |
| `workspace.app_not_found` | Resolved app slug does not exist in gateway intent. `error.meta.app`. |
| `workspace.step_not_found` | The supplied `--step` does not exist for the resolved `(app, phase=setup)`. `error.meta.{step_id, app, phase}`. |
| `authorization_failed` | The caller is not authorized to manage workspace policy for the target app. |
| `caller_role_not_allowed` | The caller role is not permitted to invoke this command. App-node callers are denied. |
| `gateway_unavailable` | A control caller has no configured gateway or cannot reach the gateway API. |

## Enum And Identifier Values

| Field | Values |
| --- | --- |
| `result.action` | `removed` |
| `step.phase` | `setup` |
| `error.code` | `validation_failed`, `workspace.app_not_found`, `workspace.step_not_found`, `authorization_failed`, `caller_role_not_allowed`, `gateway_unavailable` |

The `error.code` values listed here are exhaustive for
`workspace-setup-step:remove`. Add a new value to this renderer contract
before implementing another JSON failure code.

## Test Mapping

| Path | Coverage |
| --- | --- |
| `tests/Feature/Commands/Workspaces/WorkspaceSetupStepRemoveCommandTest.php` | JSON envelope shape (`success.data.result.action="removed"`, captured-before-delete `success.data.step` field-by-field, `success.meta.remaining_step_count` reflecting the contiguous surviving sequence), and `error` envelope shape for `workspace.step_not_found` (with `step_id` / `app` / `phase=setup`), `validation_failed` (with `field=step`/`reason=must_be_positive_integer`, `field=app`/`reason=missing_required_input`, and `field=force` for destructive consent), `workspace.app_not_found`, `authorization_failed`, and `caller_role_not_allowed`. |
| `tests/Feature/Commands/Workspaces/WorkspaceSetupStepRemoveJsonRendererTest.php` | JSON renderer selection, discriminated envelope shape, exhaustive error code enumeration, and `result.action` enum value. |
