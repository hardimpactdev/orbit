# Human Renderer: `orbit cf-dns:add`

[Back to `cf-dns:add` technical contract.](1_cf-dns-add.md)

**Renderer:** Human terminal renderer.

## Primitive

- Progress: [Progress tree](../../../../ux/commands/progress/progress-tree.md)

## Selection

Use this renderer when `--json` is not present.

## Progress Tree

`cf-dns:add` validates provider state and performs an external provider write.
After input resolution and before the Cloudflare request begins, render this
progress tree:

```text
┌ Adding Cloudflare DNS record
○ Resolve Cloudflare zone
○ Check existing address records
○ Write Cloudflare DNS record
└ Working...
```

## Success Output

Render one concise success line:

```text
Cloudflare DNS record docs.example.com A -> 203.0.113.10 created
```

When an equivalent record already exists, replace `created` with
`already present`.

## Failure Output

Validation, gateway, authorization, and Cloudflare failures render one concise
error line. Conflict errors name the existing record ID when available.

## Test Mapping

| Path | Coverage |
| --- | --- |
| `apps/cli/tests/Feature/Commands/Cloudflare/CloudflareRenderCommandsTest.php` | Human progress tree, created success output, already-present output, and gateway failure prose without a JSON envelope. |

There is no gateway-side coverage for this command-local renderer: renderer behavior lives in `apps/cli`. Conflict output and additional human failure messages remain coverage gaps until focused tests land.
