# Human Renderer: `orbit app:list`

[Back to `app:list` technical contract.](1_app-list.md)

This page describes the human-rendered output for `app:list`.

**Renderer:** Human renderer.

## Primitive

- Output table: [`Laravel\Prompts\table`](../../../../ux/commands/lists/table.md)

## Selection

Use this renderer by default when `--json` is absent.

## Progress Tree

No progress tree is rendered because `app:list` is expected to stay below one
second. It performs a fast gateway database read and does not perform slow
external work, network probes, or remote node application.

## Success Render

The success output is one table of logical apps ordered alphabetically by app
name (case-insensitive). Visible workspaces are rendered immediately below
their parent app as child rows prefixed with `├─ ` for intermediate rows and
`└─ ` for the last child row. This matches the JSON renderer's flat-array
ordering, so callers reading either output form see the same apps in the same
relative order.

### Example output

```text
+----------------+--------------------------------+----------+
| NAME           | URL                            | STATUS   |
+----------------+--------------------------------+----------+
| blog           | https://blog.test              | expected |
| docs           | https://docs.test              | expected |
| └─ feature-a   | https://feature-a.docs.test    | active   |
+----------------+--------------------------------+----------+
```

### Empty result

When no apps are visible:

```text
No apps found.
```

## Error Render

Errors are rendered as concise prose messages.

### Gateway unavailable

```text
Gateway connection is required to list apps.
```

Authorization failures originate at the API permission middleware and are
documented as part of the JSON renderer contract (see
[`6.2_app-list_output-render_json.md`](6.2_app-list_output-render_json.md)).
The command's human renderer does not own that failure shape, so it is not
covered here.

## Test Mapping

| Path | Coverage |
| --- | --- |
| `apps/gateway/tests/Feature/Http/Api/AppListControllerTest.php` | Gateway-side app list API: logical-app and workspace visibility, ordering, authorization, and empty result shape used by the human renderer. |
| `apps/cli/tests/Feature/Commands/App/AppListCommandTest.php` | Human renderer selection and gateway-backed human output coverage for the CLI command. |
