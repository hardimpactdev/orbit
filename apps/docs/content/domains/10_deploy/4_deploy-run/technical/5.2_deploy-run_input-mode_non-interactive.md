# Non-Interactive Input Mode: `orbit deploy:run`

[Back to `deploy:run` technical contract.](1_deploy-run.md)

**Input mode:** Non-interactive.

Applies when the command is running without a TTY or when `--json` is present.
`--json` forces non-interactive input mode.

## Resolution Rules

Non-interactive input mode never render prompts and never waits for user input.
The app must be supplied before side effects begin. Missing app input fails
with `validation_failed`.

`--detach` may be combined with `--json`. In that case the JSON renderer returns
the durable operation descriptor created for gateway execution and does not
subscribe to final step output.

## JSON Interaction

When `--json` is present, validation failures use the JSON renderer. No ANSI
progress output is emitted.

## Test Mapping

| Path | Coverage |
| --- | --- |
| `apps/cli/tests/Feature/Commands/Deploy/DeployWriteCommandsTest.php` | Non-interactive missing-app validation with `Http::assertNothingSent()`, and detached `--json` operation-descriptor output. |

There is no gateway-side coverage for this mapped surface; linked CLI tests cover only the mapped assertions above. Remaining behavior stays a coverage gap until focused tests land.

Coverage gaps until focused tests land: explicit non-TTY mode selection, no-prompt behavior proof, and no run-history creation before resolved input.
