# JSON Renderer: `orbit metrics:disable`

[Back to `metrics:disable` technical contract.](1_metrics-disable.md)

**Renderer:** JSON renderer.

## Primitive

None. JSON output is emitted as a machine-readable envelope.

## Selection

Use this renderer when `--json` is present.

## Envelope

This renderer uses the shared [JSON Envelope](../../../README.md#json-envelope).

## Success Shape

```json
{
  "success": {
    "data": {
      "node": "metrics-1",
      "role": "metrics",
      "purged_data": false
    }
  }
}
```

## Error Shape

```json
{
  "error": {
    "code": "validation_failed",
    "message": "Use --force to disable metrics.",
    "meta": {
      "field": "force"
    }
  }
}
```

| Field | Type | Meaning |
| --- | --- | --- |
| `success.data.node` | string | Target node. |
| `success.data.role` | string | Always `metrics`. |
| `success.data.purged_data` | boolean | Whether purge cleanup was requested. |
| `error.code` | `validation_failed`, `gateway_unavailable`, `authorization_failed`, `node_role.remove_blocked`, `node_role.remove_failed` | Stable command failure code. |

## Test Mapping

Documented error codes: `validation_failed`, `gateway_unavailable`,
`authorization_failed`, `node_role.remove_blocked`, and
`node_role.remove_failed`.

| Path | Coverage |
| --- | --- |
| `apps/cli/tests/Feature/Commands/Metrics/MetricsCommandsTest.php` | JSON forwarding, force validation, and purge flag shape. |
| `apps/gateway/tests/Feature/Services/Nodes/Roles/MetricsRoleBaselineTest.php` | Gateway role baseline removal deletes metrics process and route intent for the JSON success path. |
