# Interactive Input Mode: `orbit deploy:run`

[Back to `deploy:run` technical contract.](1_deploy-run.md)

**Input mode:** Interactive.

Applies when the command is attached to a TTY and `--json` is not present.

## Prompt Behavior

Interactive input mode prompts only for missing required fields after gateway
reachability and provided options are resolved.

| Prompt ID | Primitive | Field | Shown when | Validation |
| --- | --- | --- | --- | --- |
| `deploy.run.app` | `search` | `instance` | No app argument was supplied. | Visible production instance selector the caller may deploy. |

`--detach` is never prompted. It must be supplied explicitly when the operator
wants an asynchronous run.

## Primitive References

- [`search`](../../../../ux/commands/inputs/search-prompt.md)

## Abort Behavior

Prompt aborts exit with the shared Orbit command failure status and perform no
gateway writes, run-history creation, or node-side execution.

## Test Mapping

| Path | Coverage |
| --- | --- |
| `apps/cli/tests/Feature/Commands/Deploy/DeployInteractiveInputModeTest.php` | Interactive `deploy:run` app prompt before operation start with `detach=false`. |

There is no gateway-side coverage for this mapped surface; linked CLI tests cover only the mapped assertions above. Remaining behavior stays a coverage gap until focused tests land.

Coverage gaps until focused tests land: documented prompt ID, production app search validation, abort behavior, no run-history creation before resolved input, and explicit proof that detached mode is never prompted.
