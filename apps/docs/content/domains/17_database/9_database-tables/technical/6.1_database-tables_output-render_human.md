# Human Renderer: `orbit database:tables`

[Back to `database:tables` technical contract.](1_database-tables.md)

## Primitive

- List: [Table](../../../../ux/commands/lists/table.md)

## Progress Tree

No progress tree. The command performs one metadata read through the selected
database connection and renders the result directly. SQLite locality is handled
by the gateway/local command split; the public human renderer must not fake
progress over a blocking remote JSON response.

## Human Strings

- Success: "Showing {table_count} table(s)."
- Failure: "Failed to inspect database tables."

## Test Mapping

| Path | Coverage |
| --- | --- |
| `apps/cli/tests/Feature/Commands/Database/DatabaseReadCommandsTest.php` | Table row rendering, count success strings, and empty table count. |

There is no gateway-side coverage for this mapped surface; linked CLI tests cover only the mapped assertions above. Remaining behavior stays a coverage gap until focused tests land.
