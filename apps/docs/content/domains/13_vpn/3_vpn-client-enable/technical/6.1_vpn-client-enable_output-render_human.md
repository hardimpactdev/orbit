# Human Renderer: `orbit vpn-client:enable <name>`

[Back to `vpn-client:enable` technical contract.](1_vpn-client-enable.md)

**Renderer:** Human terminal renderer.

## Primitive

- Progress: [Progress tree](../../../../ux/commands/progress/progress-tree.md)

## Selection

Use this renderer when `--json` is not present.

## Progress Tree

`vpn-client:enable` resolves the active `vpn` role runtime, performs backend
authentication, and writes backend peer state, so the human renderer must
render a progress tree after input resolution and before side effects begin:

```text
┌ Enabling VPN client
○ Resolve VPN runtime
○ Authenticate VPN backend
○ Enable VPN client
└ Working...
```

## Success Output

```text
VPN client `laptop` enabled
```

If the client was already enabled, render:

```text
VPN client `laptop` already enabled
```

## Failure Output

Human-rendered failures use one concise error line and no JSON envelope.

## Test Mapping

| Path | Coverage |
| --- | --- |
| `apps/cli/tests/Feature/Commands/Vpn/VpnCommandsTest.php` | Human enable/disable renderer datasets for enabled success, already-enabled, validation failure, and backend failure prose. |

There is no gateway-side coverage for this command-local mapping: input handling and renderer behavior live in `apps/cli`. Gateway API behavior is mapped in the command contract file when a gateway-side surface exists.
