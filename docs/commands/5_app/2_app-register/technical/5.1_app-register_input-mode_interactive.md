# Technical Contract: `orbit app:register` (Interactive Input)

This contract defines behavior for **interactive input mode**.

[Back to the canonical contract.](1_app-register.md)

**Input mode:** Interactive.

Use this input mode only when both conditions are true:

- the command is running in a TTY;
- `--json` is not present.

When `--json` is present, use
[`5.2_app-register_input-mode_non-interactive.md`](5.2_app-register_input-mode_non-interactive.md)
even if the process has a TTY.

## Prompts

| ID | Primitive | Label | Required |
| --- | --- | --- | --- |
| `name` | `text` | App name | Yes |
| `node` | `datatable` | Select target app node | Yes (if no default) |
| `path` | `text` | App path on node | Yes (for adoption) |

## Primitive References

- [`text`](../../../ux/inputs/text-prompt.md)
- [`datatable`](../../../ux/lists/data-table-prompt.md)

## Behavior

- **Validation**: Each field is validated as it is entered.
- **Re-prompt**: Invalid values trigger a re-prompt with a validation message.
- **Node Selection**: If `--node` is missing, the user is presented with a data table of authorized app nodes.
- **Path Adoption**: If the app name is not registered and a path is provided, the command confirms intent to adopt the existing path.

## Test Mapping

| Path | Coverage |
| --- | --- |
| `tests/Feature/Commands/Apps/AppRegisterCommandTest.php` | Prompt sequence, re-prompting on invalid input, and selection list populating. |
