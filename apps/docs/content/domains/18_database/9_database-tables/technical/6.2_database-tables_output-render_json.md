# JSON Renderer: `orbit database:tables`

[Back to `database:tables` technical contract.](1_database-tables.md)

## Primitive

None. JSON output is emitted as a machine-readable envelope via `$this->line(...)`.

## Envelope

This renderer uses the shared [JSON Envelope](../../../README.md#json-envelope).

## Success Shape

The gateway returns the generic query-runner result shape: `data` holds the
driver-reported `columns` and `rows`, and the resolved connection identity
lives in `meta` (`connection`, `driver`) alongside the read-query metadata.

```json
{
  "success": {
    "data": {
      "columns": ["name", "kind"],
      "rows": [
        { "name": "users", "kind": "base_table" }
      ]
    },
    "meta": {
      "connection": "acme",
      "driver": "pgsql",
      "mode": "read",
      "limit": 50,
      "total_rows": 1,
      "returned_rows": 1,
      "truncated": false,
      "truncated_by": [],
      "max_json_bytes": 1048576,
      "duration_ms": 3
    }
  }
}
```

## Error Shape

The top-level envelope is either `success` or `error`.

## Test Mapping

| Path | Coverage |
| --- | --- |
| `apps/cli/tests/Feature/Commands/Database/DatabaseReadCommandsTest.php` | Table-list success envelope and documented error-envelope coverage. |

There is no gateway-side coverage for this renderer: public command ownership
lives in `apps/cli`.
