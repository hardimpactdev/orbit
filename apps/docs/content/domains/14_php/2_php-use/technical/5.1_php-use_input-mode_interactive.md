# Interactive Input Mode: `orbit php:use`

[Back to `php:use` technical contract.](1_php-use.md)

**Input mode:** Interactive.

Use this input mode only when both conditions are true:

- the command is running in a TTY;
- `--json` is not present.

## Prompts

| ID | Primitive | Label | Required |
| --- | --- | --- | --- |
| `target` | `select` | PHP target | Yes, when no target scope resolves from options or cwd. |
| `version` | `select` | PHP version | Yes, unless `--inherit` is supplied. |
| `workspace` | `select` | Workspace | Yes, when workspace inheritance is requested and cwd does not resolve one. |
| `instance` | `select` | Instance | Yes, when an instance target is selected and no instance is resolved. |

## Primitive References

- [`select`](../../../../ux/commands/inputs/select-prompt.md)

## Behavior

- Each prompt only shows targets the caller is authorized to manage. Instance
  choices are apps whose affected Orbit instance serving nodes can all
  be preauthorized.
- For an app target, version choices are restricted to supported images
  available on every affected Orbit instance serving node. Workspace choices
  use only their selected serving node.
- `--inherit` skips the version prompt and requires a workspace target.
- Mutually exclusive option errors are shown before prompts.
- Validation errors re-prompt the field when the user can recover within the
  same interaction; authorization failures abort.

## Test Mapping

| Path | Coverage |
| --- | --- |
| `apps/cli/tests/Feature/Commands/Php/PhpUseCommandTest.php` | Interactive missing-version prompt selection and selected version forwarding for an instance target before gateway update. |

There is no gateway-side coverage for this command-local mapping: input handling and renderer behavior live in `apps/cli`. Gateway API behavior is mapped in the command contract file when a gateway-side surface exists.

Interactive workspace selection for `--inherit` remains a coverage gap until a
focused CLI prompt test lands. The current routine test covers `--inherit`
payload forwarding in JSON mode after the workspace is provided explicitly.
